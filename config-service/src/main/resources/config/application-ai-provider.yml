ai:
  provider:
    embedding:
      bge:
        base-url: http://127.0.0.1:8908
        api-key:
        model: bge-m3
        dimension: 1024
    generation:
      deepseek:
        base-url: https://api.deepseek.com
        api-key: ${LLM_API_KEY}
        model: deepseek-v4-flash
