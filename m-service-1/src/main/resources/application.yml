server:
  port: 8180
spring:
  config:
    import: optional:configserver:http://localhost:9888
  application:
    name: m-service
#    security:
#        oauth2:
#            resourceserver:
#                jwt:
#                    issuer-uri: http://localhost:8090
