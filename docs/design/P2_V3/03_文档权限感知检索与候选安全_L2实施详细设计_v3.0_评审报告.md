# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断继续串行评审：否；P1_V2/00～06 与 P2_V3/00～07 逐文档评审已完成
- 是否阻断编码：是；目标文档已通过品审，但全集仍为 In Review，尚未由用户确认 Approved，且 ACL authority/es-query contract、服务身份/凭据与配置切换尚未取得 M0 实施授权
- 审查类型：cross_layer
- 目标文档：`docs/design/P2_V3/03_文档权限感知检索与候选安全_L2实施详细设计_v3.0.md`
- 上级文档：Agent 目标架构总览、契约与规划 L1、能力执行内核 L1、元数据与上下文安全 L1
- 直接前置：P1_V2/03～06、P2_V3/00～02
- 关联文档：P2_V3/04～07
- 实际审查轮次：5
- 主要风险摘要：自由 ACL/DSL Map、authority Profile/index 输入、protected filter 可丢失、response/candidate 无绑定、正向 stale cache、local/07 双撤权源、Projector 外部 I/O、RRF 前后 validator 层级冲突及 Domain Adapter seam 侵入均已修复；最终无 S0/S1

## 2. 文档识别结果

- 识别文档类型：L2 Document ACL evidence、protected filter、candidate security 与 final currentness 详细设计
- 识别依据：文档定义 authority scope/evidence、closed AST、跨服务 filter enforcement、response binding、Safe candidate、candidate currentness 和 Result Security evidence
- 文档状态：In Review；用户已授权修改目标文档并执行最多 5 轮评审-修复
- 是否包含修改历史：是，已记录 5 轮实质评审-修复
- 是否存在上级文档：是，四份当前 L0/L1 为只读权威
- 是否存在关联文档缺失：否；04/05/07 的当前差异已登记给后续串行评审

## 3. 审查范围

| 序号 | 文档/实现 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---:|---|
| 1 | P2_V3/03 | 目标 L2 | 是，全文 | 评审并重写 ACL/filter/candidate/currentness 安全链 |
| 2 | 四份当前 L0/L1 | 上级架构 | 是，相关章节 | 核对 Runtime 非授权、Core/metadata/Result Security、当前单 Agent与演进边界 |
| 3 | P1_V2/03～06 | 直接前置 | 是，相关章节 | 核对 current permission、ExecutionScope、operation context/outcome、limits 和迁移门禁 |
| 4 | P2_V3/00～02、04～07 | 同层关联 | 是，边界章节 | 核对 Corpus/target、Profile/limit、Handler、evidence/provider/emergency 所有权 |
| 5 | P2/03、P2_V2/03 | 历史来源 | 是，按主题核对 | 验证单路/多路 ACL 和撤权规则承接；未修改历史文档 |
| 6 | 当前 ACL DTO/client/mock/filter/Adapter/RevocationGuard | 当前 Agent 实现 | 是，全文/相关方法 | 识别 raw principals、Map DSL、profile/alias、mock 和 local blocklist |
| 7 | 当前 es-query request/service filter 路径与 01 schema v3 | 当前跨服务/索引 | 是，相关文件/章节 | 核对无 filter、Map merge、ACL 字段和 target binding 风险 |

## 4. S0 阻断问题

复审后未发现遗留 S0。已关闭 7 项：

| 序号 | 原位置 | 问题 | 风险 | 修复结果 |
|---:|---|---|---|---|
| 1 | 原 10.2/10.7 | ACL/free ES DSL 使用 `Map<String,Object>` | field/operator/script/query 可穿透，无法 strict/canonical/coverage | 改为封闭 AST、field enum、DAF-1 和 typed wire union |
| 2 | 当前 Adapter merge | Adapter 自行合并 caller/ACL Map | 重构或 fallback 可删除/覆盖 protected filter | 两字段全链分离；只在 es-query 顶层 AND；所有 channel 强制同一 compiled object |
| 3 | 原 authority request | 发送 retrievalProfile/profileVersion/indexAlias | Profile/index 反向成为 ACL authority，alias 变化影响权限结论 | 只发送 current subject/permission、Corpus、operation 与 safe bindings |
| 4 | 原 retrieval response | 无 Corpus/target/filter/evidence binding | alias race、伪响应或某 channel 无 ACL 的 hit 可进入 RRF | ES raw-hit validator在算法前绑定；Adapter validator复核 fused跨服务响应；错配整包拒绝 |
| 5 | 原 final currentness | 只有 scope version，无 candidate aclRef/version batch | 索引 ACL stale 或中途对象撤权后仍输出 | final bounded batch 校验完整 ACL object set，partial/unknown/changed 整体拒绝 |
| 6 | 第二轮 | Projector 直接外部查 currentness | P1 ResultSecurityPort 无 deadline/cancel，迫使侵入通用 seam或无界 I/O | Handler final set 后外部 Guard；DCS-1 short-lived decision；Projector no-I/O 复核 |
| 7 | 原/当前 local blocklist | `DocumentRevocationGuard` 读 AgentProperties，与 07 state 并存 | 相同 target 可出现双重且不一致 currentness | 删除 local blocklist；03 唯一算法只读 07 current governance view |

## 5. S1 严重问题

复审后未发现遗留 S1。已关闭 14 项：

| 序号 | 原位置 | 问题 | 风险 | 修复结果 |
|---:|---|---|---|---|
| 1 | 当前 `DocumentAclScope` | user/department/role/attribute raw lists 进入 Adapter | 权限正文扩散到非授权边界 | raw scope 仅 agent-service；Adapter 只接收 compiled protected binding |
| 2 | 当前 filter factory | 编译 corpusId/materialType/retrievalProfile 等 schema v3 不存在字段 | 查询失败或伪安全条件不生效 | Corpus/target request 绑定；chunk 只编译 tenant/status/visibility/principals/documentId |
| 3 | 原 10.6 | 任意 allow/deny predicate | authority 可提交未知 field/operator，无法安全表达 | scope 改为 principal sets + closed documentId constraint/deny set |
| 4 | 原 10.9/16 | ALLOW scope 短 TTL 正向缓存 | 撤权后 stale allow 继续输出 | 当前无跨 Invocation ALLOW cache；每次 resolve + final currentness |
| 5 | 原 filter-too-large | 误引用 02 limit 或可截断 | protected ACL 不是 caller resource；截断可扩权 | 独立 compiler security caps，只能拒绝；不进 DocumentResourceLimit |
| 6 | 原 candidate | 未定义 Safe candidate binding/字段 | Provider/Result 可见 raw ACL/vector/URI/全文/free metadata | Safe candidate + target/filter/evidence/ACL object/profile/limit binding 和 typed fields |
| 7 | 原 dedup/context | 可对 raw hit 合并或无 ACL 取邻接 chunk | 通过合并/上下文泄露被过滤文档 | 先 AclBound hit；相同 binding 才 dedup；neighbor 同 target/filter |
| 8 | 当前 mock/disabled | main source 可启用 mock authority或 runtime disabled bean | 生产 allow fixture误启用、启动后才失败 | mock 移 test；production 缺 authority 在 startup closure 失败 |
| 9 | 当前 client credential | ACL authority 复用 Document Provider auth header | 权限边界和 Provider credential 混用 | dedicated ACL authority credential/scope/config |
| 10 | 第二轮 | currentness port evidence/context 双参数 | Invocation/deadline/limit 可错配 | typed request 内嵌 operation context |
| 11 | 第三轮 | scope union 只有 ALLOW/DENY | timeout/invalid response 被误作业务 deny或异常泄漏 | 增加 typed FAILURE；deny/failure 均 fail closed但内部可观测分开 |
| 12 | 第三轮 | authority digest/TTL/limit ref 可重复或被信任 | hash 伪造、无限 TTL、binding 不一致 | DAS-1 本地重算、独立 TTL cap、limit ref 只从 operation context 派生 |
| 13 | 第四轮关联复审 | `DocumentRetrievalResponseBindingValidator` 被同时描述为 RRF 前置和跨服务 response validator | 若 RRF 在 es-query-service，跨服务 validator不可能位于算法前；实施者可能把算法复制到 Agent或跳过 raw-hit校验 | 拆成 ES `DocumentChannelHitBindingValidator` 与 Adapter response validator；复用 binding contract但不复制 RRF/ACL authority |
| 14 | 第五轮 | retrieval command 内嵌 operation context并引用跨模块私有类型 | 改写P1 Domain Adapter SPI，或使agent-adapter-api反向依赖agent-service/es-query-api | 恢复 `(command,operationContext)`；command只用adapter-api closed types，Adapter唯一映射ES wire DTO |

## 6. S2 一般问题

复审后未发现遗留 S2。已关闭 3 项：

| 序号 | 原位置 | 问题 | 修复结果 |
|---:|---|---|---|
| 1 | 文档信息 | 五份 L0/L1 与旧基线 | 改为四份当前 L0/L1 和 P2_V3 统一基线 |
| 2 | 原接口/落点 | 省略 path、无跨服务 DTO/canonical/删除门禁 | 补 exact module/package/class/config/test/rg 命令 |
| 3 | 原评审记录 | 首轮直接标通过 | 记录 3 轮发现/修复和最终 S0/S1=0 |

## 7. S3 建议优化

暂无当前必须修改的 S3。不建议现在预建 terms-set lookup index、ACL cache、Document execution 状态机或 Multi-Agent ACL schema；只有实际规模超过封闭 AST cap 时再以独立 ADR/contract 设计。

## 8. 架构设计审查结果

不适用；目标为 L2 详细设计。未发现必须修改 L0/L1 或 P1_V2 的遗留阻塞。final external check 放在 Handler 内，Projector 仅验证短效 evidence，因而不修改 P1 `ResultSecurityPort`；Future Multi-Agent 只更换 P1 owner/delegation/current evidence 输入。

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| ACL authority | 通过 | typed ALLOW/DENY/FAILURE、dedicated credential、one attempt、strict binding |
| Scope model | 通过 | principal/document constraints closed，无 predicate/DSL Map |
| Evidence | 通过 | DAS-1 local scope、DAE-1 Invocation/Profile/Permission/limit/metadata binding |
| Protected AST | 通过 | closed nodes/fields/operators、无 MatchAll/script/query-string/free Map |
| Compiler cap | 通过 | independent security cap，只拒绝，不截断/扩权/进入 resource resolver |
| Filter enforcement | 通过 | caller/protected 分离、es-query 一次编译、每 channel/context 同一对象 |
| Response binding | 通过 | ES raw hit算法前绑定，Adapter fused response跨服务复核；Corpus/target/filter/evidence/schema/report 全闭合 |
| Bound hit/candidate | 通过 | BoundDocumentChannelHit 供ES唯一RRF；AclBoundDocumentHit供Agent安全投影；dedup/context不混ACL version |
| Provider isolation | 通过 | rerank/generation 只接收 Safe 派生最小投影，无 identity/ACL/target正文 |
| Currentness | 通过 | pre-retrieval scope + final object batch + 07 emergency view，whole-result fail closed |
| Result Security | 通过 | Handler DCS-1 short-lived decision；Projector 本地 freshness/binding，不外部 I/O |
| 数据/缓存 | 通过 | 无新 DB、无 Context/candidate persistence、无正向 allow cache |
| 可实施性 | 通过 | exact 路径、配置、契约、删除门禁、测试和命令完整 |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| L0/L1 | 通过 | Runtime 非授权、Java typed、Core/metadata/currentness/Context 安全和单 Agent一致 |
| P1_V2/03 | 通过 | P1 current authorization 先执行；Document authority 不成为第二 UserPermission 源 |
| P1_V2/05 | 通过 | operation context/outcome/limit binding 同源；protected compiler cap 不混入 resource |
| P1_V2/06 | 通过 | external DTO/credential/config/旧 Map 删除均进入原子迁移与 M0 授权 |
| P2_V3/00 | 通过 | 03 evidence/filter/Safe candidate/Guard；07 state owner；04 Handler unique orchestration |
| P2_V3/01 | 通过 | schema v3 ACL 字段、one target 和 release binding；Profile/corpus 字段不进 chunk filter |
| P2_V3/02 | 通过 | 单 Corpus、profile/limit binding；caller filter count 与 protected cap 分离 |
| P2_V3/04 | 待对齐 | 当前仍由 Adapter `Map` merge/indexAlias 解析；下一份须采用 typed command/bound response |
| P2_V3/05 | 待对齐 | 需让 final output candidate 携带 DCS-1 decision，并保持 evidence/citation identity set |
| P2_V3/06 | 待对齐 | provider request 必须只从 Safe candidate/Evidence package 投影，不能接收 ACL/target binding正文 |
| P2_V3/07 | 待对齐 | emergency state 只持久化/供 read view；不得继续在 07 实现另一个 RevocationGuard |
| 当前代码 | 有条件通过 | migration 差异已登记，不宣称 Implemented |

## 11. 是否建议进入后续阶段

- 是否建议继续评审下一份 L2：是，进入 P2_V3/04
- 是否建议进入编码实现：否；先完成 04～07 串行评审并确认 authority/es-query contract 与凭据变更
- 是否建议先修订架构设计：否
- 是否建议先修订关联文档：是，按既定顺序修订 04～07；不修改 L0/L1/P1 或旧 P2
- 是否需要用户确认：当前评审阶段无需新增确认

## 12. 用户确认项

当前无新增文档修改确认项。实施前必须单独确认：

1. ACL authority resolve/currentness typed HTTP DTO/OpenAPI/调用方替换；
2. es-query-api protected filter/response binding DTO 破坏性切换；
3. ACL authority、Document es-query、07 governance read 的专用 service scope/credential/config；
4. main-source mock/disabled fallback 与旧 Map protocol 原子删除；
5. commit、push、PR 或发布。

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 修订内容 | 是否阻断 |
|---:|---|---|---|---:|
| 1 | S0 | 10.1～10.8 | typed authority/evidence + closed AST + protected/caller 分离 + ES 每通道强制 | 是，已修复 |
| 2 | S0 | 10.9～10.11 | target/filter/evidence response binding、AclBound hit 和 Safe candidate | 是，已修复 |
| 3 | S0 | 10.13～10.15 | candidate object/07 emergency final check、DCS-1 short-lived decision、Projector no-I/O | 是，已修复 |
| 4 | S1 | 10.2、10.6、16～17 | closed scope/caps/no positive cache/no local blocklist/Multi-Agent seam | 是，已修复 |
| 5 | S1 | 19～20 | exact paths、credential/config、旧 mock/Map 删除、测试门禁 | 是，已修复 |
| 6 | S2 | 1～2、22～24 | 文档元数据和真实复审记录 | 否，已修复 |
| 7 | S1 | 9、10.9～10.11、11、19～24 | 拆分 ES 算法前 raw-hit validator 与 Adapter response validator，保持唯一 RRF | 是，已修复 |
| 8 | S1 | 10.7～10.8、11、19～24 | 恢复P1 Domain Adapter双参数seam并清除command跨模块反向依赖 | 是，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-13 | 上级/P1/00～02/当前实现交叉初审并整体重写 | 19（S0=5、S1=11、S2=3） | 19 | 0 |
| 2 | 2026-07-13 | Result Security seam 与 final currentness 复审 | 3（S0=1、S1=2） | 3 | 0 |
| 3 | 2026-07-13 | authority failure/digest/TTL/binding 终审 | 4（S0=1、S1=3） | 4 | 0 |
| 4 | 2026-07-13 | P2_V3/04 关联复审闭合 validator/RRF 执行层级 | 1（S1=1） | 1 | 0 |
| 5 | 2026-07-13 | P1 Adapter seam 与模块依赖方向终审 | 1（S1=1） | 1 | 0 |

## 15. 最终结论

> 全集终态注记（2026-07-13）：本文保留该文档逐轮评审的时点记录；P1_V2/00～06 与 P2_V3/00～07 全集评审现已完成且 S0/S1=0。当前实施状态和授权边界以目标文档第 1、3、23、24 节为准，本文不构成 Approved 或 M0 授权。

目标文档通过品审，不阻断返回并继续 `P2_V3/04_统一文档检索编排_L2实施详细设计_v3.0.md`。本次执行 5 轮，最终 S0=0、S1=0。ES raw hit在唯一RRF前形成 `BoundDocumentChannelHit`，Adapter对fused response复核后形成 `AclBoundDocumentHit`；Agent不保留第二RRF。Domain Adapter保持P1 `(command,operationContext)` seam，command只含adapter-api closed types。当前实现仍使用 raw principal DTO、Map ACL DSL/merge、profile/indexAlias authority request、main-source mock 和 AgentProperties local blocklist；这些是已登记迁移差异。未获得 authority/es-query contract、service credential/config 和 P1_V2 原子切换授权前不得编码或启用半套协议。
