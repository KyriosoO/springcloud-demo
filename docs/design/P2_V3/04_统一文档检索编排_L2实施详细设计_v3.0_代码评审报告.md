# 代码评审报告

## 1. 执行摘要

| 项目 | 内容 |
|---|---|
| 评审模式 | review_and_fix（批量模式，第 12/15 份，严格串行） |
| 最大循环次数 | 5 |
| 实际执行轮次 | 3 |
| 评审代码范围 | `agent-service` frozen channel/command/Handler；`agent-adapter-api` retrieval contract；`agent-adapter-document` mapper/client/response validator；`es-query-api/service` hybrid request、channel/RRF/selection/context；测试与静态边界 |
| 是否修改代码 | 是 |
| 验证结果 | 定向测试与 12 模块 reactor 通过；未执行真实 ES/Feign transport cancellation 和 alias 切换集成 |
| 最终结论 | 有条件通过；6 项检索参数贯穿、跨服务响应证明和错误分类问题已修复 |

## 2. 文档依据清单

| 文档 | 角色 | 优先级 | 读取结果 | 备注 |
|---|---|---:|---|---|
| `04_统一文档检索编排_L2实施详细设计_v3.0.md` | detailed_design | 0 | 已完整读取 | 当前主文档 |
| P2_V3 `00`～`03`、`05`～`07` 及 P1_V2/03～06 | related_design | 1 | 已读取相关边界 | Corpus/Profile/ACL/evidence/Provider/currentness/原子迁移 |
| `../Agent契约与规划架构设计_v1.0.md`、`../Agent能力执行内核架构设计_v1.0.md`、`../Agent元数据与上下文安全架构设计_v1.0.md` | architecture | 2 | 已读取相关约束 | typed operation、单 Handler、deadline/security binding |
| `../Agent目标架构总览_v1.0.md` | architecture | 3 | 已读取相关约束 | L0 单 Invocation/单 Corpus 边界 |

## 3. 文档约束追踪

| 约束编号 | 约束内容 | 对应代码位置 | 评审结果 |
|---|---|---|---|
| DOC-C-001 | vector `numCandidates` 必须来自 frozen Profile 并贯穿 command/wire/ES，不硬编码 | Profile→command→DTO→executor | 修复后符合 |
| DOC-C-002 | Adapter 必须复核 candidateId、RRF、稳定顺序、channel outcome/rank、diagnostics | `DocumentRetrievalResponseBindingValidator` | 修复后符合 |
| DOC-C-003 | 同 document/chunk key 不用可碰撞字符串拼接 | RRF merger、selector | 修复后符合 |
| DOC-C-004 | response contract failure 形成一次 attempt 的 INVALID_RESPONSE，不伪装 INVALID_REQUEST | `DocumentAgentAdapter` | 修复后符合 |
| DOC-C-005 | duplicate channel 和跨服务 DAF canonical 规则 fail closed | use case boundary、ES filter guard | 修复后符合 |

## 4. 代码问题清单

| 编号 | 级别 | 类型 | 文件 | 问题描述 | 影响 | 处理结果 |
|---|---|---|---|---|---|---|
| CR-001 | high | frozen_parameter_loss | `DocumentRetrievalCommandFactory`、`DocumentRetrievalChannels`、wire DTO、executor | Profile 的 `numCandidates` 未进入 command/wire；ES 使用 `candidateCount * 4` | 实际 ANN 搜索宽度脱离冻结 projection/digest，可能溢出或产生不可复现召回 | 新增 typed `vectorNumCandidates/numCandidates` 全链字段，Profile 要求 breadth≥vectorK，ES 直接消费冻结值；mapper 测试覆盖 |
| CR-002 | high | response_integrity | `DocumentRetrievalResponseBindingValidator` | 仅重算 RRF 分数和“分数不升序”，未重算 candidateId、同分 tie-break、重复/失败 channel rank | 被篡改 identity 或等分重排可跨 Adapter 边界进入 Safe candidate/final currentness | 重算 DCI-1，完整复刻稳定 comparator，拒绝重复/未成功/越界 rank；伪造 candidate 测试覆盖 |
| CR-003 | high | diagnostics_binding | `DocumentRetrievalResponseBindingValidator` | 未闭合 channel hitCount/raw/fused/returned document/truncated diagnostics | 伪造或不一致 coverage 可进入观测/结果，掩盖丢候选与降级 | 核对 channel count cap、raw sum、fused/returned、distinct document 与 truncated 语义 |
| CR-004 | medium | failure_classification | `DocumentAgentAdapter` | validator 抛出的 `IllegalArgumentException` 被前置 catch 归为 0 attempt `INVALID_REQUEST` | 服务端畸形响应被错误归责调用方，attempt/audit/告警失真 | 拆分 request/call 与 response validation 两阶段；response rejection 返回 1 attempt INVALID_RESPONSE |
| CR-005 | medium | composite_key_collision | `DocumentRrfMerger`、`DocumentResultSelector` | 用 `|` 拼接 document/version/chunk/ACL 作为 Map key | 合法 ID 含分隔符时可碰撞，导致错误融合或 binding conflict | 改为 typed record key，不改变 canonical candidateId |
| CR-006 | medium | boundary_validation | `DocumentHybridSearchUseCase`、`DocumentProtectedFilterGuard` | wire 允许重复 channel；ES DAF value 规则未同步拒绝全部控制字符 | 重复执行同 channel 后才在 Adapter 失败；恶意内部 caller 可构造 Agent 侧不会产生的 DAF | request boundary 拒绝 duplicate channel；ES canonical 规则与 Agent 对齐 |

## 5. 文档问题清单

| 编号 | 级别 | 文档位置 | 问题类型 | 问题描述 | 影响 | 建议 |
|---|---|---|---|---|---|
| DOC-001 | low | 第 3、23、24 节 | 状态表述不完整 | 文档标记 Implemented，但未给出当前可复现验收命令，仅列测试矩阵 | 不能从正文独立复核“已完成本地验证” | 授权后补充使用 `serviceCenter/mvnw.cmd` 和 artifactId selector 的最小命令，并引用代码评审报告 |

## 6. 修改摘要

| 轮次 | 修改范围 | 修改内容 | 对应问题 | 结果 |
|---:|---|---|---|---|
| 1 | typed channel contract/ES | 贯穿 ANN numCandidates、active channel candidate count、Profile 闭包 | CR-001 | 首次测试暴露既有 inactive-vector fixture 不闭合，修正 projector/fixture 后通过 |
| 2 | Adapter validator/ES algorithms | DCI/RRF/tie/rank/diagnostics 复核、typed composite keys、duplicate channel/canonical gate | CR-002、CR-003、CR-005、CR-006 | 已修复 |
| 3 | Adapter failure mapping/tests | request 与 response rejection 分段，新增 forged identity 与 numCandidates 测试 | CR-004 | 已修复并完成回归 |

## 7. 验证结果

| 轮次 | 命令 | 结果 | 摘要 |
|---:|---|---|---|
| 1 | 检索定向 reactor | 首次失败后修复并通过 | 首次失败准确发现 vectorK=101 fixture 的 numCandidates=100 不闭合；修复后相关 11 模块 SUCCESS |
| 2～3 | Mapper/Adapter/RRF/filter/Profile 定向测试 | 通过 | 12 tests，覆盖 frozen ANN breadth、forged candidateId、RRF、inactive vector 与 protected filter |
| 3 | `serviceCenter/mvnw.cmd -pl :agent-service,:agent-adapter-document,:es-query-service,:document-provider-adapter -am test` | 通过 | 12 模块 SUCCESS；`agent-service` 469、`agent-adapter-api` 11、`agent-adapter-document` 5、`agent-api` 47 tests，ES/Provider 模块亦通过 |
| 3 | free-form wire、Handler properties、旧 contract/alias 与 RRF ownership 精确扫描 | 通过 | wire/Handler 违规零命中；RRF 算法唯一位于 es-query-service，Adapter 仅保留文档要求的响应重算验证 |
| 3 | `git diff --check`（五个相关模块） | 通过 | 无空白错误；仅仓库既有 CRLF/LF 提示 |

## 8. 剩余风险

| 编号 | 级别 | 风险 | 触发场景 | 后续建议 |
|---|---|---|---|---|
| RISK-001 | medium | OpenFeign 当前只能在调用前后观察 cancellation/deadline，未证明能中断进行中的 HTTP exchange | 用户取消或 absolute deadline 在 Feign 阻塞中到达 | 引入/配置支持 per-request deadline 和 cancellation 的 transport，并做 late/cancel 集成测试；不得用遗留结果继续执行 |
| RISK-002 | medium | context before/after 缺独立可证明的邻居总数 operational cap，当前主要由 Profile/readiness 与字符预算约束 | 错误 Profile 配置很大的 before/after 且文本为空/很短 | 在 07 capacity/readiness 中加入显式 neighbor-count cap 并在 request boundary 校验；需要配置契约决策后实施 |
| RISK-003 | medium | 未连接真实 ES 验证 alias 中途切换、vector binding、单批 context 与 transport cancel | 生产 alias 切换、KNN/neighbor 查询和 ES timeout | 运行真实 ES 集成与 fault injection；本轮仅完成本地算法/contract 回归 |

## 9. 结论

- 有条件通过。
- frozen ANN 参数、响应 identity/RRF/diagnostics、错误分类、typed key 与 duplicate channel 共 6 项问题已闭合；定向测试、12 模块 reactor 和静态边界通过。
- 主详细设计、关联文档及 L0/L1 架构正文未修改；DOC-001 仅记录，等待文档修改授权。
- 未执行生产 ES/Provider、配置发布、提交、推送或远端操作。
