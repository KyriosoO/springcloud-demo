# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断继续串行评审：否；P1_V2/00～06 与 P2_V3/00～07 逐文档评审已完成
- 是否阻断编码：是；目标文档已通过品审，但全集仍为 In Review，尚未由用户确认 Approved，且 P1 seam、内部 hybrid contract、服务身份与原子切换尚未取得 M0 实施授权
- 审查类型：cross_layer
- 目标文档：`docs/design/P2_V3/04_统一文档检索编排_L2实施详细设计_v3.0.md`
- 上级文档：Agent 目标架构总览、契约与规划 L1、能力执行内核 L1、元数据与上下文安全 L1
- 直接前置：P1_V2/03～06、P2_V3/00～03
- 关联文档：P2_V3/01～03、05～07
- 实际审查轮次：4
- 主要风险摘要：Handler重选Profile/预算、alias/model/ACL重入、自由DSL、未绑定hit参与算法、两套RRF、requiredness信息损失、Adapter SPI侵入、API模块反向依赖和Provider外发决策缺失均已修复；最终S0=0、S1=0

## 2. 文档识别结果

- 识别文档类型：L2 Document 单 Invocation 检索编排、跨服务契约和确定性算法详细设计
- 识别依据：文档定义 `ValidatedDocumentPlan` 消费、ACL/query/Provider/Adapter 调用顺序、typed hybrid contract、RRF/document selection/context/rerank、失败矩阵、实现落点和测试门禁
- 文档状态：In Review；用户已授权修改目标文档并执行最多 5 轮评审-修复
- 是否包含修改历史：是，已记录 4 轮实质评审-修复
- 是否存在上级文档：是，四份当前 L0/L1 为只读权威
- 是否存在关联文档缺失：否；01/02/03 的关联阻塞已分别在其授权轮次内闭合，05～07 按串行顺序继续评审

## 3. 审查范围

| 序号 | 文档/实现 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---:|---|
| 1 | P2_V3/04 | 目标 L2 | 是，全文 | 评审并重写唯一检索 leaf 编排 |
| 2 | 四份当前 L0/L1 | 上级架构 | 是，相关章节 | 核对 Java 权威、Core/Adapter/Result Security、单 Agent 与 Multi Agent seam |
| 3 | P1_V2/03～06 | 直接前置 | 是，相关章节 | 核对 Execution Context、Domain Adapter 双参数 SPI、typed limit、operation outcome 与迁移门禁 |
| 4 | P2_V3/00～03、05～07 | 同层关联 | 是，边界章节 | 核对 Corpus/Profile/ACL/evidence/provider/rollout 所有权和执行顺序 |
| 5 | P2/04、P2_V2/04/09/07 | 历史来源 | 是，按主题核对 | 验证有效 query/channel/RRF/context/rerank规则已承接；未修改历史文档 |
| 6 | 当前 Handler、ValidatedPlan、Adapter DTO/client/mapper、ES hybrid service/merger/tests | 当前实现 | 是，相关类/方法 | 识别可变 request、AgentProperties、Map DSL、alias、N+1 context 和旧 merger迁移差异 |

## 4. S0 阻断问题

复审后未发现遗留 S0。已关闭 7 项：

| 序号 | 原位置 | 问题 | 风险 | 修复结果 |
|---:|---|---|---|---|
| 1 | 原 5～10 | Handler 再读 Profile/AgentProperties 并决定 feature/limit/fallback | 绕过 Planning/Authorization 冻结，执行可扩大 | Handler 只消费 ValidatedPlan、ExecutionContext 和 03/05/06 ports |
| 2 | 原 request | 携带 indexAlias/schema/profileVersion/provider model/dimension | 侵入 01/02/06，多 source 漂移或 caller target 注入 | command 只含 CorpusKey、安全 binding 和 executable specs；target由ES每request解析一次 |
| 3 | 原 filter/query | caller/ACL 使用 Map/自由 DSL并由Adapter merge | 某channel/context可丢 protected filter或注入脚本/字段 | caller/protected分字段、closed DTO、ES顶层AND复用同一compiled object |
| 4 | 原执行顺序 | raw/unbound hit进入RRF/dedup/context | alias/filter/ACL错配可影响排序并泄露对象存在性 | ES `DocumentChannelHitBindingValidator` 算法前绑定，错配整包拒绝 |
| 5 | 原 04/09 | Agent/ES 均可执行RRF/dedup/context | 两套算法顺序/安全边界分裂 | 04拥有算法规范，唯一实现位于es-query-service；Agent不保留merger |
| 6 | 第二轮 | Execution projection仅有feature boolean且无requiredChannels | Handler无法确定拒绝或降级，只能重读Profile | 联动02保留closed policy和requiredChannels，failure matrix可直接执行 |
| 7 | 第三轮 | command内嵌operation context并改写Domain Adapter SPI | 侵入P1冻结seam，Adapter调用方/契约分裂 | 恢复 `retrieve(command,operationContext)`；只有capability-local Provider request内嵌context |

## 5. S1 严重问题

复审后未发现遗留 S1。已关闭 15 项：

| 序号 | 原位置 | 问题 | 风险 | 修复结果 |
|---:|---|---|---|---|
| 1 | ValidatedPlan | 直接携带 Adapter `DocumentRetrievalRequest` | Validation/执行/设施DTO耦合 | 改为 immutable validated domain fields + execution projection |
| 2 | Query preparation | rewrite文本可覆盖filter或携带DSL | Provider输出改变授权/查询结构 | plain-text normalizer；original固定；filter独立typed |
| 3 | Channel | EXACT/PHRASE语义重复、未知executor可扩展 | 双实现和配置歧义 | 收敛 `EXACT_PHRASE` + closed registry/gate |
| 4 | Embedding | expected model/dimension来自Profile且无manifest闭合 | 模型相同维度但语义不兼容 | 06 safe binding + limit校验 + 01 active manifest精确复核 |
| 5 | Channel outcome | `partial/degraded` boolean表示任意失败 | required failure可能被当optional成功 | closed channel outcome；security/required失败不可降级 |
| 6 | RRF | 数值与tie-break不完整 | 并发/重放排序漂移 | 固定rank、BigDecimal scale/rounding、stable channel order和全tie-break |
| 7 | Dedup | 模糊标题/URI去重或不同ACL binding合并 | 跨版本/权限误合并 | 固定security-aware document key，不一致整体拒绝 |
| 8 | Context | 逐document N+1、可能无filter/再解析alias | 容量放大和邻接内容泄漏 | 同target/filter的单批bounded查询，neighbor全binding |
| 9 | Context budget | 04/05可能各重置maxContextChars | 字符预算重复放大 | 冻结retrieval→EvidenceContextPackage end-to-end ceiling |
| 10 | Adapter response | 跨服务response只做null检查 | mapper/transport漂移可形成伪bound candidate | Adapter二次验证envelope/hit/order/count/context后输出AclBound hit |
| 11 | Rerank | 可新增/修改candidate或遗漏顺序未定义 | Provider扩大候选或结果不稳定 | 子集/无重复/finite校验；返回、遗漏和tail稳定拼接 |
| 12 | Multi Corpus | 04 leaf曾被旧01要求fan-out多个Corpus | ACL/limit/target无法保持单一binding | 单Plan/request/Corpus；未来fan-out仅归上层独立child Invocation |
| 13 | 模块依赖 | adapter-api command引用agent-service query/Profile/provider类型 | API反向依赖具体实现 | command只用adapter-api prepared query/embedding/executable specs |
| 14 | Corpus/wire type | Agent type直接复用es-query-api类型 | agent-adapter-api依赖ES wire模块 | DCK-1逻辑contract、模块本地strict DTO、Adapter唯一mapping |
| 15 | 第四轮Provider输入 | rewrite/embedding/rerank把Safe/minimal input直接视为可外发 | Policy/Permission禁用或分类未知时仍可能发送query/snippet | 统一消费06 current-scope outbound decision/reference；04只做operation-specific all-or-none投影 |

## 6. S2 一般问题

复审后未发现遗留 S2。已关闭 3 项：

| 序号 | 原位置 | 问题 | 修复结果 |
|---:|---|---|---|
| 1 | 文档信息 | 旧代码基线和状态字段不统一 | 对齐P2_V3基线、四份当前L0/L1和实施授权说明 |
| 2 | 原 11/19/20 | 接口/路径/测试仅列概念名 | 补模块、类、方法、迁移动作、property/contract/architecture测试 |
| 3 | 原 22～24 | 首轮直接“通过”且无真实问题记录 | 记录4轮问题、关联修订和最终S0/S1=0 |

## 7. S3 建议优化

暂无当前必须修改的 S3。不建议在单智能体阶段增加 coordinator、worker registry、跨 Corpus RRF、共享 candidate cache、第二 execution state machine或仅为 `DocumentCorpusKey` 新建公共模块；这些会增加无当前语义的层级和状态。

## 8. 架构设计审查结果

不适用；目标是 L2 详细设计。未发现必须修改 L0/L1 或 P1_V2 的遗留阻塞项。本文把 Handler保持为 capability leaf executor，Multi Agent只预留上层 child Invocation seam；Core、Planning Runtime、Profile/ACL/Provider/rollout所有权未被检索实现侵入。

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| Handler sequence | 通过 | ACL→query→rewrite/embedding→Adapter→safe→rerank→05→final guard唯一顺序 |
| Plan/limit/profile | 通过 | 单Corpus execution projection；requiredness保留；同一typed limit/ref消费 |
| Adapter SPI | 通过 | `(command,operationContext)`；command Spring-free、无Profile/raw ACL/target |
| Cross-service DTO | 通过 | module-local strict types、DCK-1/canonical mapping、无Map/DSL |
| Target/filter binding | 通过 | 一个request解析一次target；所有channel/context同filter/target |
| Raw/response security | 通过 | ES算法前binding + Adapter跨服务复核；错配整包拒绝 |
| RRF/selection | 通过 | 唯一实现、固定公式/rounding/tie-break、document/chunk上限 |
| Context | 通过 | 单批、same binding、missing/invalid语义和end-to-end chars完整 |
| Rerank | 通过 | safe subset、stable order、required/optional fallback确定 |
| Provider outbound | 通过 | rewrite/embedding/rerank均先绑定current ExecutionScope purpose-specific decision；Safe不等于Provider-visible |
| Failure/deadline | 通过 | closed outcome、security不可降级、一次attempt、cancel/late拒绝 |
| Multi-Agent seam | 通过 | leaf contract可被独立child Invocation复用，不预建fan-in状态 |
| 可实施性 | 通过 | exact落点、兼容/删除门禁、测试和风险清单完整 |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| L0/L1 | 通过 | Java权威、Runtime非执行、Core无Document分支、Result Security唯一 |
| P1_V2/03～05 | 通过 | current scope/typed limits/Domain Adapter双参数SPI/operation outcome一致 |
| P1_V2/06 | 通过 | contract/config/旧路径删除仍需M0授权和原子Release Unit |
| P2_V3/00 | 通过 | 唯一时序、预算分层和04/05/06所有权一致 |
| P2_V3/01 | 通过 | 单Plan单Corpus、DCK-1、一次target resolve和同physical target一致 |
| P2_V3/02 | 通过 | execution projection保留requiredChannels/feature policy；无alias/provider |
| P2_V3/03 | 通过 | protected filter、ES raw binding、Adapter response binding、Safe candidate/final guard一致 |
| P2_V3/05 | 待串行复评 | 必须消费ordered safe candidates并遵守同一context ceiling，不重做排序 |
| P2_V3/06 | 待串行终审 | 拥有共享outbound decision factory与typed Provider operation；不决定fallback/编排/索引target |
| P2_V3/07 | 待串行复评 | 生产启用/真实模型/Gold/容量/rollout门禁，不回流Handler配置 |
| 当前代码 | 有条件通过 | migration gap已准确登记，不宣称当前实现符合目标设计 |

## 11. 是否建议进入后续阶段

- 是否建议继续评审下一份 L2：是，05已完成，继续P2_V3/06
- 是否建议进入编码实现：否；先完成 P2_V3/05～07 串行评审，并取得内部 DTO/endpoint、P1 seam、配置和旧类删除授权
- 是否建议先修订架构设计：否
- 是否建议先修订关联文档：是，按既定顺序修订 05～07；01～03 已闭合且均达到 5 轮上限，不再修改
- 是否需要用户确认：当前文档评审阶段无需新增确认

## 12. 用户确认项

当前无新增文档修改确认项。实施前必须单独确认：

1. `DocumentRetrievalRequest/HybridSearch*`、内部 endpoint 与所有 caller/fixture 的原子替换；
2. `AgentProperties` Document Profile/limit/alias/rewrite旧配置和Runtime rewrite路径删除；
3. 专用Document服务身份、scope、secret、ES role和operational caps；
4. P1_V2 typed contribution/operation context/outcome seam已实现并可供P2使用；
5. commit、push、PR或生产发布。

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 修订内容 | 是否阻断 |
|---:|---|---|---|---:|
| 1 | S0 | 6～10.8 | ValidatedPlan/Profile/limit/ACL/target唯一权威和typed command | 是，已修复 |
| 2 | S0 | 10.8～10.15 | 同target/filter、ES raw binding、唯一RRF/selection/context、Adapter复核 | 是，已修复 |
| 3 | S0 | 10.3～10.6、10.16～10.18 | required/optional feature/channel/fallback和final guard顺序 | 是，已修复 |
| 4 | S0 | 10.6～10.7、11、19 | 恢复P1 Adapter SPI并清除command跨模块反向依赖 | 是，已修复 |
| 5 | S1 | 14～18 | deadline/cancel/attempt/idempotency/security/audit/performance闭合 | 是，已修复 |
| 6 | S1 | 17、19～20 | 兼容切换、exact类路径、属性/契约/架构测试 | 是，已修复 |
| 7 | S2 | 1～2、21～24 | 元数据、风险、真实轮次和完成摘要 | 否，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-13 | L0/L1、P1、00～03、历史来源和当前实现交叉初审并整体重写 | 18（S0=5、S1=10、S2=3） | 18 | 0 |
| 2 | 2026-07-13 | typed plan/requiredness/预算/response可验证性复审 | 5（S0=1、S1=4） | 5 | 0 |
| 3 | 2026-07-13 | P1 Adapter seam、模块依赖、01/03关联一致性终审 | 3（S0=1、S1=2） | 3 | 0 |
| 4 | 2026-07-13 | 06关联复审：rewrite/embedding/rerank Provider外发purpose/policy binding | 1（S0=0、S1=1） | 1 | 0 |

## 15. 最终结论

> 全集终态注记（2026-07-13）：本文保留该文档逐轮评审的时点记录；P1_V2/00～06 与 P2_V3/00～07 全集评审现已完成且 S0/S1=0。当前实施状态和授权边界以目标文档第 1、3、23、24 节为准，本文不构成 Approved 或 M0 授权。

目标文档通过品审，不阻断继续串行评审`P2_V3/06_文档CapabilityLocalProvider端口_L2实施详细设计_v3.0.md`。本次累计执行4轮，最终S0=0、S1=0。目标设计已经形成单Plan/单Corpus/单target/filter、ES算法前binding、唯一RRF/document selection/batch context、Adapter跨服务复核和Agent safe/rerank/05/final-currentness链；rewrite/embedding/rerank在port前统一绑定06 current-scope outbound decision，Safe candidate不再被当作外发许可。Domain Adapter保持P1双参数SPI，Multi Agent仅保留上层child Invocation seam。当前代码仍是可变request、AgentProperties、Map DSL、caller alias和旧merger实现，这些是已登记迁移差异，不是已通过的实现；未获得M0与原子切换授权前不得编码或启用半套协议。
