# 单体 Agent 应用与能力 L1 架构设计评审报告

## 1. 评审结论

| 项目 | 内容 |
|---|---|
| 结论 | 无法完成评审 |
| 评审类型 | `architecture_l1`，含 L0→L1 约束追踪 |
| 评审门禁 | L1→L2 |
| 就绪状态 | `undetermined` |
| 目标文档 | `docs/design/agent/单体Agent应用与能力架构_L1_v0.1.md`（`AGENT-APP-L1-001`，v0.1） |
| 评审日期 | 2026-07-16 |
| 评审循环 | 3 轮；最大允许 5 轮，因文档发现已全部关闭而提前停止 |
| 当前执行阻塞数 | 0；正式第3轮复评结束时为4，评审后维护人阻塞及 `EB-L1-001～003` 的决策/证据均已形成；尚未执行新的正式复评 |
| 最终未关闭 S0 / S1 / S2 / S3 | 0 / 0 / 0 / 0 |
| 初始发现 S0 / S1 / S2 / S3 | 0 / 1 / 1 / 0；第 2 轮新增 1 个 S2，均已修复 |
| 是否在独立评审阶段修改被评审文档 | 否；正式评审发现的3项修复，以及 DeepSeek、Provider 连通性、服务条款/数据使用策略、API 契约与效果 PoC、维护人、应用侧字段出域、内部服务身份/短期能力凭据机制、DOCUMENT 临时质量门禁和版本化模拟评价包，均在冻结发现后依据用户明确授权通过独立 `architecture-design-doc` authoring 阶段完成 |
| 主要结论 | L0 约束映射、职责边界、内部架构、状态权威和 L2 拆分经修复后满足 L1 架构质量要求；评审后维护人阻塞及 `EB-L1-001～003` 的决策/证据均已形成。`EB-L1-003-DOCUMENT-EVAL` `1.0.0-synthetic` 已冻结60个语料块、26个样本、8类场景、标注/指标/报告 Schema/校验和，当前执行阻塞数为0。由于这些属于评审后同步，且新增 `APP-DEC-12～APP-DEC-14` 及评价工件尚未进入正式复评，原“无法完成评审”结论和 `undetermined` 就绪状态暂不改变 |

## 2. 评审范围与基线

| 资源 | 角色 | 层级 | 状态/版本 | 是否读取 | 权威性/作用 | 置信度 |
|---|---|---|---|---|---|---|
| `docs/design/agent/单体Agent应用与能力架构_L1_v0.1.md` | target | L1 | 已评审 / 评审受阻；v0.1 | 是，初评及每次修订后均完整重读 | 本次评审对象 | high |
| `docs/design/agent/单体Agent智能体总体架构_L0_v0.1.md` | parent | L0 | 已评审 / 已通过；v0.1 | 是 | 直接链接的唯一上位权威，提供 `AD-01～AD-20`、`DEC-01～DEC-14` 及评审前置门禁；`DEC-14` 为评审后新增决策 | high |
| 当前用户需求 | requirement/authorization | — | 2026-07-16 | 是 | 授权最多5轮评审—修复、生成报告、更新目标状态；评审后确认既有各项决策，并要求使用模拟数据完成版本化 DOCUMENT 评价集 | high |
| 全局 `AGENTS.md` 指令 | project rule | — | 当前会话 | 是 | 约束修改范围、验证、Git 和输出要求 | high |
| 规划中的“检索与索引基础设施架构 L1” | peer | L1 | 尚未创建 | 否，不存在可读目标 | 仅用于验证本文是否明确声明消费边界和联合门禁，不能证明双方契约已达成 | high（不存在事实）；低（未来内容） |

### 2.1 范围内

- 目标 L1 的治理信息、目标、现状/目标架构、职责与非职责、同层边界、依赖方向；
- Java/Python、Agent/Adapter、auth、业务服务、检索查询面的语义契约；
- 数据/状态权威、QUERY/AGGREGATE/DOCUMENT 核心流程、失败闭环和横切机制；
- `AD-01～AD-20`、`DEC-01～DEC-14` 的继承以及 `APP-DEC-01～APP-DEC-14` 到 L2 的交付追踪；
- 目标文档内发现的修订、每轮复评、评审状态同步和本报告。

### 2.2 范围外

- 既有实验性 `agent-*`、`es-*` 项目及早期设计/评审文档；它们被用户和 L0 明确排除为目标基线；
- L0、未来检索 L1、外部业务/权限/模型契约、代码、测试、配置、Schema 和 SQL 的修改；
- 对 employee/transaction 外部接口实现正确性、检索实现、生产容量和代码实施状态的评审；
- 对 DeepSeek 公开条款之外的企业合同、DPA、零留存/不训练承诺或生产 SLA 作外部背书；把纯模拟评价包解释为真实业务代表性或生产质量验收结果。

### 2.3 基线优先级与冲突

采用“当前明确用户需求 > 已通过 L0 > 目标 L1 > 项目规则 > 一般工程实践”的评审顺序。仓库发现的其他 Agent 架构文档与当前用户/L0 的“实验资产非基线”要求冲突时，按明确排除处理，未将其作为替代上位权威。

未发现目标 L1 与 L0 的已定架构约束冲突。评审结束后，用户已把 L0/L1 文档及 Agent 模块维护人确认为 Dylan；该事实已同步到 L0/L1，不改变外部系统各自的责任所有权。

## 3. 执行阻塞

| 编号 | 类型 | 位置/证据 | 对评审覆盖的影响 | 已完成的安全检查 | 解除条件 |
|---|---|---|---|---|---|
| EB-L1-001 | 评审后已关闭 | 用户已确认接受 DeepSeek 公开服务条款和数据使用策略；目标文档第16.3～16.4.1节已给出官方政策/API 链接、4项契约实测和当前模型端口7项 L1 最小效果 PoC 的可追踪摘要 | 已形成“供应商已披露事实 + 明确风险接受 + Java 目标级白名单/转换/最小化/默认拒绝”的确定边界；实测证明 JSON、Tool、上下文、错误语义和 QUERY/AGGREGATE/DOCUMENT 最小可行性 | 全部实测使用合成数据；应用侧仍不依赖供应商删除，模型输出保持不可信；生产容量、政策变化和 DOCUMENT 正式质量由后续门禁承接 | 已关闭；下一次正式复评验证证据追踪与安全 L2 交付完整性，供应商政策变化作为持续风险管理 |
| EB-L1-002 | 评审后已关闭 | 用户已确认固定 Runtime 服务密钥 + Java 生成的不透明短期 `capabilityId` + Java Tool Gateway 服务端校验；目标第 10.2、12、13、16.3、16.4.2 节已同步适用边界 | 架构层已能验证服务级身份与逐执行最小授权边界；物理存储、密钥轮换、撤销、多实例一致性和测试仍由安全 L2 落实 | 已限定只读 Tool，绑定 execution/fence/tool/主体范围/filterHash/expiry，终态失败关闭；凭据禁止进入 Prompt/LLM/日志；未来写能力不得直接复用 | 已关闭；不再计入当前执行阻塞，下一次正式复评验证 `APP-DEC-13` 及 L2 交付完整性 |
| EB-L1-003 | 评审后已关闭 | `docs/design/agent/evidence/eb-l1-003/document-eval-v1.0.0/` 已冻结 `EB-L1-003-DOCUMENT-EVAL` `1.0.0-synthetic`：60个语料块、26个样本、8类场景，以及 qrel、答案/要点/引用、权限/撤权/时间版本、9项指标公式、报告 Schema、版本记录和 SHA-256 | 已形成固定且可复算的架构评价工件，且语料规模超过 Recall@50 截断位；能够验证 DOCUMENT L2 的检索、回答、引用、拒答和零越权门禁，不声明系统已经跑分 | 纯模拟数据不含真实个人或业务数据；结构、引用、角色、有效期、撤权和文件校验和已验证；生产批准前须新增经授权脱敏的业务代表性扩展版本 | 已关闭；下一次正式复评确认工件追踪与 `APP-DEC-14` 闭环，实际跑分由 DOCUMENT L2 执行 |

当前执行阻塞数为0。`EB-L1-001` 已依据官方公开条款事实、用户明确风险接受、API 契约实测和模型端口最小效果 PoC 关闭；`EB-L1-002` 已依据内部服务身份/短期能力凭据机制关闭；`EB-L1-003` 已依据用户授权完成的固定版本化模拟评价包关闭；原 `EB-L1-004` 已依据实名维护人 Dylan 关闭。这些均属于评审后事实和证据同步，不等同于完成新的正式复评。

## 4. 发现汇总

| 严重级别 | 初始/新增数量 | 最终未关闭数量 | 对评审门禁的影响 |
|---|---:|---:|---|
| S0 | 0 | 0 | 无 |
| S1 | 1 | 0 | 第 5.2 节发现初始阻塞 L1→L2 的审计所有权确定性，现已关闭 |
| S2 | 2 | 0 | 其中 1 个在第 2 轮新增；均为下位治理与追踪确定性问题，现已关闭 |
| S3 | 0 | 0 | 无 |

## 5. 评审发现

### 5.1 S0 阻断缺陷

未发现 S0 文档缺陷。

### 5.2 S1 严重问题

| ID | 层级 | 位置 | 证据 | 期望与实际差异 | 触发场景和影响 | 修改建议/责任方 | 门禁影响 | 最终状态 |
|---|---|---|---|---|---|---|---|---|
| REV-L1-001 | L1 | 初始第 8 节“数据、状态与运行时所有权”、第 10.5 节 | Tool 调用记录的权威写为“Java Agent/MySQL 或审计端口”，同时指标/追踪后端允许 No-op | 强制安全审计事实必须有唯一持久权威；可丢遥测不能与审计事实二选一 | No-op 生效或两个 L2 分别实现时，Tool 越权/重放证据可能未持久化或形成双权威 | 由 Agent L1 文档维护人分离“Tool 调用审计事实”和“遥测派生事件”，明确 Java/MySQL 持久权威及 L2 分工 | 初始阻塞 L1→L2 | 已修复：第 8、10.5、13 节已建立唯一权威、No-op 边界和语义/物理分工；第 2、3 轮复评通过 |

### 5.3 S2 一般问题

| ID | 层级 | 位置 | 证据 | 期望与实际差异 | 触发场景和影响 | 修改建议/责任方 | 门禁影响 | 最终状态 |
|---|---|---|---|---|---|---|---|---|
| REV-L1-002 | L1 | 初始第 13 节“L2 下位交付拆分” | `APP-DEC-01` 未落到 L2；`AD-01/AD-07` 的资产隔离和服务收敛没有下位验证所有者 | L1 的局部决策和实施性约束应有 L2 交付或明确不适用理由 | L2 并行编写时可能遗漏双运行时拓扑验证、alpha 隔离或误建 `es-document-service` | 由 Agent L1 文档维护人补入部署/扩展验证 L2 的约束和门禁 | 不单独阻断，但削弱下位治理 | 已修复：第 13 节已承接 `AD-01`、`AD-07`、`APP-DEC-01` 并增加验证门禁；第 2、3 轮复评通过 |
| REV-L1-003 | L1 | 修订后第 13 节“承接的 L0/L1 约束”列 | 使用 `AD-03/05/19`、`APP-DEC-03/05` 等缩写，自动追踪只能识别首个稳定 ID | 所有稳定 ID 应逐项完整书写，使下位生成、审查和验证可确定追踪 | 自动抽取或后续 L2 逐项映射时产生假遗漏/假覆盖 | 由 Agent L1 文档维护人将缩写改为完整 ID 序列 | 不单独阻断，但影响追踪证据 | 已修复：第 13 节完整书写全部 ID；第 3 轮复评和自定义覆盖检查通过 |

### 5.4 S3 建议优化

未记录不影响门禁的风格性建议。

## 6. 分层评审结果

### 6.1 L1 模块/领域架构

| 检查域 | 结论 | 证据/发现 ID |
|---|---|---|
| L0 约束映射 | 通过（正式评审基线）/待复评（新增决策） | 正式评审时第4、15节逐项承接 `AD-01～AD-20`、`DEC-01～DEC-13`；评审后新增 `DEC-14→APP-DEC-13` 映射，追踪结构完整但尚未正式复评 |
| 唯一职责和非职责 | 通过 | 第 3、6 节明确 Java/Python、Adapter、auth、业务源和检索域边界 |
| 同层协作与契约 | 受限通过 | 第 6.3、7 节已定义消费边界、禁止侵入和联合门禁；检索 L1 尚未创建，双方契约未验证 |
| 内部架构和依赖方向 | 通过 | 第 5.3、6.2、6.4 节无反向代码依赖或 Python 直连业务/DB/ES |
| 数据/状态和横切策略 | 通过 | 第 8～11 节明确 auth/业务/文档/Java/Python/检索权威；第 5.2 节发现已关闭 |
| L2 交付约束 | 通过（内容）/待正式复评 | 第13节覆盖 `AD-01～AD-20`、`APP-DEC-01～APP-DEC-14`；全部评审发现和执行阻塞均已关闭，新增决策及评价工件尚待正式复评 |

## 7. 跨层追踪结果

| 上游 ID/位置 | 下游 ID/位置 | 映射状态 | 缺陷类型 | 责任方 | 发现 ID |
|---|---|---|---|---|---|
| L0 `AD-01～AD-20` | L1 第 4 节及第 13 节 | 已覆盖 | 无 | Agent L1 | — |
| L0 `DEC-01～DEC-14` | L1 第12、15.1节 | 已覆盖 | `DEC-14` 为评审后新增，需正式复评 | Agent L1 | 评审后信息同步 |
| L1 `APP-DEC-01～APP-DEC-14` | L1 第13节治理的 L2 交付 | 已覆盖 | 原不可追踪，已修复；评审后新增的 `APP-DEC-12～APP-DEC-14` 已分别落到安全或 DOCUMENT L2，其中 `APP-DEC-13` 还同时落到部署与扩展验证 L2 | Agent L1 | 第5.3节两项 S2；评审后信息同步 |
| L0 正式评审前置门禁 | L1 第5.5、16.2～16.4节及版本化评价包 | 评审后证据已满足、待正式复评确认 | LLM、内部安全、维护人及 DOCUMENT 固定评价工件均已形成；评价包只解决架构前置输入，不替代 L2 实际跑分 | 产品 / AI / 安全 / 检索责任方 | 评审后信息同步 |
| L0 Agent/检索同层边界 | L1 第 3.4、6.3、7.2 节 | 本文侧已覆盖、对方侧未验证 | 同层文档尚未创建 | Agent 与检索 L1 共同责任方 | 证据限制，不计文档发现 |

## 8. 实现就绪判断

- 当前门禁：L1→L2。
- 允许的下一步：使用 `design-doc-review` 对当前 L1、评审后新增决策及 `EB-L1-003-DOCUMENT-EVAL` 执行正式复评；复评通过后再把 L1 状态更新为正式可进入 L2。
- 暂不允许的动作：将本 L1 标记为“已通过”，把其作为正式 L2 治理基线，或据此直接进入生产实现批准。
- 进入下一步前必须完成：对当前文档做一次正式复评，验证评审后新增的 `APP-DEC-12～APP-DEC-14`、全部关闭证据和评价包追踪。若复评发现边界变化，须先按授权修订 L0/外部契约。
- 后续是否需要代码对照评审：L2 完成并进入实现后需要；本次未评审代码，也不对既有实验实现背书。

## 9. 证据限制与未验证项

| 项目 | 原因 | 对结论的影响 | 建议验证方式 |
|---|---|---|---|
| 检索与索引基础设施 L1 | 尚未创建 | 只能确认本文侧未侵入，不能确认查询面语义、撤权传播和兼容门禁已被对方承接 | 创建并评审关联 L1 后执行双向一致性评审 |
| employee/transaction 外部接口语义 | 本次未将外部契约作为评审目标，目标也标记需在 L2 核实 | 不影响当前职责边界判断，但阻塞对应 QUERY/AGGREGATE L2 的契约定稿 | 在业务适配 L2 前做接口差距与调用方影响评审 |
| 文档源契约 | 稳定 ID、业务版本、资源属性和撤权契约尚未冻结 | 不影响 DOCUMENT 归属判断，但阻塞 DOCUMENT/检索详细设计和联合验收 | 由来源方、安全、检索共同发布权威契约 |
| 数值化性能与生产拓扑 | L0 将其截止条件设为 L2 基线/生产批准前 | 不影响 L1 结构门禁，但当前不能声称生产就绪 | 在 L2 压测与生产架构评审中确认 |
| 代码/测试/运行环境 | 用户要求新架构不以现有实验项目为基线，本次为文档评审 | 不对实现正确性或当前运行状态作结论 | 完成 L2 后使用代码对文档评审及最小相关测试 |

## 10. 推荐处理顺序

| 顺序 | 发现/阻塞 ID | 目标文件和责任方 | 所需授权 | 建议处理方式 |
|---:|---|---|---|---|
| 1 | EB-L1-001（已完成） | DeepSeek 供应商侧处理、API 契约兼容性与效果证据；Dylan / AI 平台 / 安全负责人 | 用户已确认接受公开服务条款及数据使用策略 | 保留官方条款基线、API 契约4项实测和模型端口7项最小效果 PoC；应用侧继续以目标级白名单、转换、最小化和默认拒绝为基线 |
| 2 | EB-L1-002（已完成） | 固定服务密钥 + Java 不透明短期执行凭据 + Java 服务端校验；Dylan / 安全 / Agent 负责人 | 已由用户确认，不引入新的身份基础设施 | 安全 L2 固化密钥轮换、摘要存储、撤销、多实例一致性、传输、重放审计和测试 |
| 3 | EB-L1-003（已完成） | `EB-L1-003-DOCUMENT-EVAL`；Dylan 协调产品/检索/AI/安全负责人 | 用户已授权使用模拟数据完成版本化评价集 | 保留 `1.0.0-synthetic` 冻结目录和校验和；实际跑分与业务扩展版本由 L2/生产门禁承接 |
| 4 | 正式复评 | 当前 L1；Dylan | 用户授权复评；若边界变化再授权修订 | 使用 `design-doc-review` 验证关闭证据、`APP-DEC-12～APP-DEC-14` 和评价包追踪，并同步正式状态 |

## 11. 复审记录

| 轮次 | 基线版本/日期 | 已修复 | 部分修复 | 未修复 | 新增问题 | 结论 |
|---:|---|---|---|---|---|---|
| 1 | v0.1 / 2026-07-16 | 无 | 无 | 第 5.2 节 S1、第 5.3 节首项 S2；`EB-L1-001～004` | 无 | 无法完成评审；授权修复目标内发现 |
| 2 | v0.1 修订 5 / 2026-07-16 | 第 5.2 节 S1、第 5.3 节首项 S2 | 无 | `EB-L1-001～004` | 第 5.3 节第二项 S2 | 无法完成评审；继续修复新增 S2 |
| 3 | v0.1 修订 6 / 2026-07-16 | 第 5.3 节第二项 S2 | 无 | `EB-L1-001～004` | 无 | 无法完成评审；文档发现归零，因外部阻塞提前结束循环 |
| 评审后同步 | v0.1 修订 8 / 2026-07-16 | `EB-L1-004`；`EB-L1-001` 的供应商/模型/API 选型部分 | `EB-L1-001` | `EB-L1-002～003` | 无 | 非复评；继续保持评审受阻，等待 3 项门禁证据 |
| 评审后同步 | v0.1 修订 9 / 2026-07-16 | `EB-L1-001` 的应用侧字段出域机制部分 | `EB-L1-001` | `EB-L1-002～003` | 无 | 非复评；应用侧边界已确定，仍等待供应商侧/兼容性/效果及其余 2 项门禁证据 |
| 评审后同步 | v0.1 修订 10 / 2026-07-16 | `EB-L1-001` 的测试环境 Provider 基本连通性部分 | `EB-L1-001` | `EB-L1-002～003` | 无 | 非复评；调用链已确认可达，仍等待供应商侧/API 契约/效果及其余 2 项门禁证据 |
| 评审后同步 | v0.1 修订 11 / 2026-07-16 | `EB-L1-002` | 无 | `EB-L1-001`、`EB-L1-003` | 无 | 非复评；内部调用安全机制已确认，当前剩余2项门禁证据 |
| 评审后同步 | v0.1 修订 12 / 2026-07-16 | `EB-L1-003` 的指标与阈值部分 | `EB-L1-003` | `EB-L1-001`、`EB-L1-003` | 无 | 非复评；临时质量门禁已确认，评价集工件仍缺失，当前剩余2项门禁证据 |
| 评审后同步 | v0.1 修订 14 / 2026-07-16 | `EB-L1-001` | 无 | `EB-L1-003` | 无 | 非复评；服务条款及数据使用策略已接受，API 契约与最小效果 PoC 已通过，当前剩余1项门禁证据 |
| 评审后同步 | v0.1 修订 15 / 2026-07-16 | `EB-L1-003` | 无 | 无 | 无 | 非复评；版本化模拟评价包已冻结，当前执行阻塞数为0，等待正式复评 |

## 12. 最终结论

目标 L1 经3轮正式评审—修复后，S0/S1/S2/S3 最终未关闭数量为0/0/0/0。评审后维护人阻塞及 `EB-L1-001～003` 的决策/证据均已形成，当前执行阻塞数为0；其中 `EB-L1-003-DOCUMENT-EVAL` `1.0.0-synthetic` 已提供固定、可复算、带权限与版本标注的评价工件。由于该工件及 `APP-DEC-12～APP-DEC-14` 尚未进入新的正式复评，本报告原“无法完成评审”结论和 `undetermined` 就绪状态暂不改变。正式复评通过前，文档状态继续保持“已评审 / 评审受阻 / 未实施 / 未生效”；评价包也不代表系统已达到9项阈值或生产就绪。
