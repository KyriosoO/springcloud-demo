# 检索与索引基础设施 L1 v1.0 架构设计评审报告

## 1. 报告治理信息

| 项目 | 内容 |
|---|---|
| 报告标识 | `AGENT-RETRIEVAL-L1-V1.0-REVIEW-001` |
| 报告状态 | 已完成 |
| 评审日期 | 2026-07-17 |
| 评审对象 | [检索与索引基础设施架构_L1_v1.0.md](./检索与索引基础设施架构_L1_v1.0.md) |
| 评审类型 | `architecture_l1 + cross_layer` |
| 评审方式 | 逐文档独立评审、与 L0/Agent L1 交叉评审、授权修复与复评 |
| 最大轮次 / 实际轮次 | 5 / 3 |
| 基线提交 | `a60675ba1c12a070acfb16f5afab132aa9a9fbf4`，2026-07-17T18:21:56+08:00 |
| 报告作用 | 记录检索与索引域的评审发现、修复证据和 `L1→L2` 门禁；不替代目标文档的架构权威 |

## 2. 评审结论

| 项目 | 内容 |
|---|---|
| 结论 | 通过 |
| 评审门禁 | `L1→L2`，并核对与 L0 及 Agent L1 的交接一致性 |
| 就绪状态 | `ready` |
| 目标文档状态 | 已评审 / 已通过 / 未实施 / 未生效 |
| 执行阻塞数 | 0 |
| 最终 S0 / S1 / S2 / S3 | 0 / 0 / 0 / 0 |
| 评审阶段文档改动 | 是；独立发现集冻结后，由 `architecture-design-doc` 在用户授权下完成最小修订，再由 `design-doc-review` 复评 |
| 主要判断 | 检索查询/管理边界、状态所有权、一致性、版本发布、韧性和评价门禁已清晰；架构发现和状态同步后的治理表述问题均已关闭 |

本结论允许在文档规定的前置条件下开展检索域 L2 设计，不表示索引、服务、数据和生产发布已经实现或批准。

## 3. 评审范围与基线

| 资源 | 角色 | 版本/指纹 | 评审用途 | 置信度 |
|---|---|---|---|---|
| `检索与索引基础设施架构_L1_v1.0.md` | target | v1.0；修订后 SHA-256 `8F7B517549A50586CBABB8FAB5EE10E0E1FC4F6B2F6A46C3942EAA4B610C8069` | 检索 L1 逐文档评审对象 | 高 |
| `单体Agent智能体总体架构_L0_v1.0.md` | parent | v1.0；同轮评审 | 验证上位原则、决策和门禁承接 | 高 |
| `单体Agent应用与能力架构_L1_v1.0.md` | peer | v1.0；同轮评审 | 验证 DOCUMENT 查询消费、重试和联合评价边界 | 高 |
| `检索与索引基础设施架构_L1_v0.1.md` | historical_source | SHA-256 `9BAF18C83CE94FFBDB20A3D18DEFC25D857FB1316F149386C4D923DE0783F93B` | 检查 v1.0 压缩表达是否丢失有效韧性和门禁约束 | 高 |
| `evidence/eb-l1-003/document-eval-v3.0.0/README.md` 及同目录评价包 | evidence | v3.0.0；README SHA-256 `B8131EBA9768CC2AC8DBE2B2A449EB9D4BC1E4ED153C7A024FDCEBC1543D6B2A` | 核对检索评价事实、版本化状态和限制 | 高 |

### 3.1 范围内

- 检索查询面、索引管理面、数据和制品所有权及安全边界；
- 原子检索、增量、撤销、缩权、全量构建、发布、回滚、恢复和对账；
- 幂等、并发、顺序、表示兼容、重试、降级、可观测和审计；
- 检索域与 Agent DOCUMENT 能力的查询契约、责任分配和联合评价；
- L0 约束承接和检索域 L2 就绪性。

### 3.2 范围外

- API/DTO、数据库表、ES Mapping、Analyzer、Embedding 模型参数和代码级实现；
- 真实数据同步链路、ES 集群、容量、性能和灾备的现场验证；
- 外部文档来源、权限服务和 Agent 调用方的字段级契约；
- 生产业务数据评价和发布批准。

### 3.3 权威顺序

本轮采用：用户明确要求 → L0 v1.0 → 本 L1 v1.0 → Agent L1 v1.0 的对等交接 → v0.1 冻结来源 → 版本化证据中的客观事实 → 一般工程原则。未发现需要修改范围外上位或外部契约文档的冲突。

## 4. 执行阻塞

未发现执行阻塞。目标、上位、对等文档及版本化评价包均可读取；修订范围和报告输出已获授权；逐文档与交叉评审证据充分。

## 5. 发现汇总

### 5.1 最终未关闭发现

| 严重级别 | 数量 | 门禁影响 |
|---|---:|---|
| S0 | 0 | 无 |
| S1 | 0 | 无 |
| S2 | 0 | 无 |
| S3 | 0 | 无 |

### 5.2 评审循环历史

| 阶段 | 严重级别 | 数量 | 结果 |
|---|---|---:|---|
| 第 1 轮初评 | S1 | 2 | 冻结后授权修复 |
| 第 2 轮复评与状态同步 | S2 | 1 | 架构发现已关闭；状态回写后发现内部自评历史表述与当前状态冲突 |
| 第 3 轮复评 | S0～S3 | 0 | 全部发现关闭，无新增问题，达到停止条件 |

## 6. 第 1 轮冻结发现与关闭证据

| ID | 位置与证据 | 期望 / 实际差异 | 触发场景与影响 | 建议、责任方与授权 | 关闭证据及状态 |
|---|---|---|---|---|---|
| `REV-XDOC-101` | L0 保留“纯合成 v3 实跑不能替代生产业务代表性扩展”的门禁，检索 L1 初稿只保留目标系统运行和指标阈值 | 期望检索域 L2 能区分算法/管线验证和生产分布验证；实际下位门禁缺少真实业务代表性要求 | 合成查询和文档通过时，可能漏掉真实权限组合、文档类型、查询分布和长尾问题，却被误作生产放行 | 在联合评价、L2 交付和风险章节补充经授权、脱敏、分层且具业务代表性的扩展要求；检索与评价责任方负责；修改已获授权 | 第 11.2、13、15 节已说明 Oracle/纯合成实跑不能替代生产批准，并列出业务代表性扩展门禁；第 2 轮复评关闭 |
| `REV-XDOC-102` | v0.1 定义查询入口、ES/Embedding 内部调用的唯一重试责任和共享总预算，检索 L1 初稿在压缩时未保留 | 期望 `es-query-service` 独占检索内部重试，Agent 入口仅在无可见副作用且预算剩余时有限重试；实际各层可独立重试 | ES/Embedding 瞬态故障时可能形成乘法请求、耗尽连接与线程、扩大尾延迟；管理写透明重试还可能重复副作用 | 明确查询/管理两类调用的责任差异、共享 deadline/attempt 预算、诊断和禁止乘法重试；检索服务与 Agent 入口责任方负责；修改已获授权 | 第 10.4 节已规定检索内部唯一重试责任、Agent 入口边界、共享总预算和管理写恢复机制；第 2 轮复评关闭 |
| `REV-GOV-103` | 正式状态回写后，三份文档顶部为“已评审/已通过”，编写期内部自评表仍以现在时写“仍为未评审” | 期望内部自评明确为正式评审前的历史记录；实际同一文档同时出现互斥的当前状态 | L2 作者或审计者可能误判本 L1 尚未批准，导致门禁状态和引用基线不确定 | 只调整治理状态、修订历史、内部自评说明和正式记录，不改变架构语义；三份文档维护者负责；修改已获授权 | 本 L1 第 17～18 节及另外两份文档已区分历史候选状态和当前正式状态；第 3 轮复评关闭 |

## 7. 检索 L1 逐文档评审结果

| 检查域 | 结论 | 主要证据 |
|---|---|---|
| 文档治理、范围与目标 | 通过 | 第 1～4 节定义状态、版本迁移、阅读导引、非目标和量化验收方向 |
| 上位约束承接 | 通过 | 第 5 节映射 L0 全局原则和决策，明确本域与 Agent/来源/权限域的分工 |
| 目标架构与责任边界 | 通过 | 第 6 节明确查询网关、混合检索、Embedding、索引编排、发布和对账职责 |
| 查询与管理契约 | 通过 | 第 7 节区分查询面、管理面、版本/指纹和错误语义，防止写权限泄漏到查询链路 |
| 数据、制品与状态所有权 | 通过 | 第 8 节定义来源事实、索引派生物、版本、任务、幂等和审计所有权 |
| 流程与失败闭环 | 通过 | 第 9～10 节覆盖检索、增量、撤销、全量、发布、回滚、恢复、对账和降级 |
| 质量、决策与 L2 门禁 | 通过 | 第 11～15 节定义检索/联合评价、核心决策、L2 交付和未关闭实施门禁 |
| 可读性与注意力引导 | 通过 | 查询/管理双面、版本发布和质量门禁被集中呈现，避免实现细节冲淡核心边界 |

## 8. 三文档交叉评审结果

| 交叉检查 | 结论 | 证据与判断 |
|---|---|---|
| L0 约束承接 | 通过 | 检索 L1 第 5 节承接 `AD-01～AD-24` 和相关 `DEC`，追踪抽取无缺失或孤立下位 ID |
| 查询/管理双面 | 通过 | L0 与本 L1 均规定 Agent/Python 只消费查询面，管理面只接受受控来源和服务身份 |
| 数据与权限权威 | 通过 | 来源系统拥有原始内容/业务版本，Auth 拥有主体权限决策，检索只执行不可扩大的安全过滤并维护派生索引 |
| DOCUMENT 交接 | 通过 | Agent L1 负责意图、候选预算、引用和最终响应；本 L1 负责检索执行、索引版本和内部诊断 |
| 重试与恢复 | 通过 | 三份文档统一唯一重试责任和总预算；索引管理写通过幂等、状态机、对账恢复而非透明查询重试 |
| 评价门禁 | 通过 | 本 L1 与 L0、Agent L1 均区分离线合成事实、目标系统实跑和生产业务代表性扩展 |
| 状态与生效 | 通过 | 三份文档均已评审/已通过但仍未实施/未生效，架构状态与运行状态一致 |

## 9. 实现就绪与允许的下一步

| 判断 | 结论 |
|---|---|
| 是否建议进入检索域 L2 | 是，但仅限第 13 节列出的 L2 主题和前置契约 |
| 是否已实现 | 否 |
| 是否允许直接生产发布 | 否；仍需代码、数据、性能、恢复、目标系统评价和生产审批证据 |

允许的下一步包括：细化查询/管理 API、索引状态机、幂等与顺序、Mapping/Embedding 兼容、发布回滚、权限过滤、重试预算、对账及评价测试。L2 不得把来源事实、Auth 决策或 Agent 业务语义迁入检索域。

## 10. 证据限制与未验证项

- 未对 `es-query-service`、索引管理实现、ES 集群和 Embedding 服务进行代码或运行符合性评审；
- 未验证真实来源系统的增量、撤销、缩权、乱序、重放和全量切换行为；
- 未验证查询/管理凭据隔离、网络策略、审计留存和高风险操作审批；
- v3 评价包不能替代真实目标系统、生产业务分布、容量和故障恢复验证；
- 未执行性能压测、故障注入、索引回滚、灾备恢复及长周期对账测试。

## 11. 评审－修改循环记录

| 轮次 | 动作 | 结果 |
|---|---|---|
| 1 | 独立完成检索 L1 逐文档评审，并与 L0、Agent L1 交叉核对 | 发现 2 个阻断 `L1→L2` 的 S1 |
| 1 修订 | 补回唯一重试责任、共享总预算和生产业务代表性评价门禁 | 修改范围限于三份已授权目标文档 |
| 2 | 全量重读三份文档并回写正式状态 | 原架构发现关闭；状态同步后的全局扫描发现 1 个 S2 治理表述冲突 |
| 2 修订 | 把编写期内部自评明确为正式评审前的历史记录 | 未改变架构约束、决策或门禁 |
| 3 | 复核三份文档当前状态、历史记录、报告、追踪和证据 | 全部发现关闭，无新增 S0/S1/S2/S3，提前结束 5 轮上限 |

## 12. 最终结论

检索 L1 v1.0 的逐文档评审和三文档交叉评审均通过，`L1→L2` 状态为 `ready`。本轮在第 3/5 轮收敛，最终没有未关闭发现；未实施、未生效以及 L2/生产门禁继续有效。
