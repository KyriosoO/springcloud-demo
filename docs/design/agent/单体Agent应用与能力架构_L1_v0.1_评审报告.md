# 单体 Agent 应用与能力 L1 架构设计评审报告

## 1. 评审结论

| 项目 | 内容 |
|---|---|
| 结论 | 无法完成评审 |
| 评审类型 | `architecture_l1`，含 L0→L1 约束追踪 |
| 评审门禁 | L1→L2 |
| 就绪状态 | `undetermined` |
| 目标文档 | `docs/design/agent/单体Agent应用与能力架构_L1_v0.1.md`（`AGENT-APP-L1-001`，v0.1） |
| 评审日期 | 2026-07-16 |
| 评审循环 | 3 轮；最大允许 5 轮，因文档发现已全部关闭而提前停止 |
| 当前执行阻塞数 | 3；正式第 3 轮复评结束时为 4，评审后已关闭维护人阻塞并部分解除 LLM 选型阻塞 |
| 最终未关闭 S0 / S1 / S2 / S3 | 0 / 0 / 0 / 0 |
| 初始发现 S0 / S1 / S2 / S3 | 0 / 1 / 1 / 0；第 2 轮新增 1 个 S2，均已修复 |
| 是否在独立评审阶段修改被评审文档 | 否；3 项修复和状态同步均在冻结发现后，依据用户明确授权通过独立 `architecture-design-doc` authoring 阶段完成 |
| 主要结论 | L0 约束映射、职责边界、内部架构、状态权威和 L2 拆分经修复后满足 L1 架构质量要求；评审后已确认 DeepSeek 目标配置及维护人 Dylan，但仍有 3 项决策/证据门禁未解除，因此原“无法完成评审”结论尚不能改为“通过”或“有条件通过” |

## 2. 评审范围与基线

| 资源 | 角色 | 层级 | 状态/版本 | 是否读取 | 权威性/作用 | 置信度 |
|---|---|---|---|---|---|---|
| `docs/design/agent/单体Agent应用与能力架构_L1_v0.1.md` | target | L1 | 已评审 / 评审受阻；v0.1 | 是，初评及每次修订后均完整重读 | 本次评审对象 | high |
| `docs/design/agent/单体Agent智能体总体架构_L0_v0.1.md` | parent | L0 | 已评审 / 已通过；v0.1 | 是 | 直接链接的唯一上位权威，提供 `AD-01～AD-20`、`DEC-01～DEC-13` 及评审前置门禁 | high |
| 当前用户需求 | requirement/authorization | — | 2026-07-16 | 是 | 授权最多 5 轮评审—修复、生成报告、更新目标状态；评审后确认 DeepSeek `deepseek-v4-flash`、API Base URL `https://api.deepseek.com` 和维护人 Dylan | high |
| 全局 `AGENTS.md` 指令 | project rule | — | 当前会话 | 是 | 约束修改范围、验证、Git 和输出要求 | high |
| 规划中的“检索与索引基础设施架构 L1” | peer | L1 | 尚未创建 | 否，不存在可读目标 | 仅用于验证本文是否明确声明消费边界和联合门禁，不能证明双方契约已达成 | high（不存在事实）；低（未来内容） |

### 2.1 范围内

- 目标 L1 的治理信息、目标、现状/目标架构、职责与非职责、同层边界、依赖方向；
- Java/Python、Agent/Adapter、auth、业务服务、检索查询面的语义契约；
- 数据/状态权威、QUERY/AGGREGATE/DOCUMENT 核心流程、失败闭环和横切机制；
- `AD-01～AD-20`、`DEC-01～DEC-13` 的继承以及 `APP-DEC-01～APP-DEC-11` 到 L2 的交付追踪；
- 目标文档内发现的修订、每轮复评、评审状态同步和本报告。

### 2.2 范围外

- 既有实验性 `agent-*`、`es-*` 项目及早期设计/评审文档；它们被用户和 L0 明确排除为目标基线；
- L0、未来检索 L1、外部业务/权限/模型契约、代码、测试、配置、Schema 和 SQL 的修改；
- 对 employee/transaction 外部接口实现正确性、检索实现、生产容量和代码实施状态的评审；
- 对 DeepSeek 服务条款、数据留存/训练政策、模型实际能力和 API 兼容性作外部背书；替用户冻结内部服务身份机制或 DOCUMENT 评价阈值。

### 2.3 基线优先级与冲突

采用“当前明确用户需求 > 已通过 L0 > 目标 L1 > 项目规则 > 一般工程实践”的评审顺序。仓库发现的其他 Agent 架构文档与当前用户/L0 的“实验资产非基线”要求冲突时，按明确排除处理，未将其作为替代上位权威。

未发现目标 L1 与 L0 的已定架构约束冲突。评审结束后，用户已把 L0/L1 文档及 Agent 模块维护人确认为 Dylan；该事实已同步到 L0/L1，不改变外部系统各自的责任所有权。

## 3. 执行阻塞

| 编号 | 类型 | 位置/证据 | 对评审覆盖的影响 | 已完成的安全检查 | 解除条件 |
|---|---|---|---|---|---|
| EB-L1-001 | 外部决策部分完成 | 用户已确认 DeepSeek、`deepseek-v4-flash`、API Base URL `https://api.deepseek.com`；目标第 16.2、16.4.1 节仍将数据处理边界、API 兼容性和效果 PoC 标记为待确认 | 无法验证哪些问题、历史、业务结果和文档片段可发送给 DeepSeek，也无法确认模型端口、Tool/上下文能力与质量预算 | 已确认目标配置；已验证目标使用模型端口隔离、总预算、不可信输出和最小化边界 | 由 Dylan / AI 平台 / 安全责任方冻结数据分类与发送矩阵、留存/训练/地域结论、API 契约测试和效果 PoC |
| EB-L1-002 | 缺少安全机制决策 | L0 第 15.2 节要求在 Agent L1 正式评审前确认内部服务身份与能力凭据；目标第 16.2 节仍标记待确认 | 无法验证 Runtime→Tool 的防伪、最小权限、过期和重放边界 | 已验证目标要求服务身份、execution/fence/能力/授权过滤绑定并失败关闭 | 由安全/平台责任方确认机制及适用契约边界 |
| EB-L1-003 | 缺少验收基线 | L0 第 15.2 节要求两份 L1 正式评审前建立 DOCUMENT 固定评价集和 Recall/NDCG/回答阈值；目标第 11、16.2 节仍无数值基线 | 无法判断多路召回、RRF、Reranker 降级和回答质量门禁是否可验收 | 已验证目标明确证据不足拒答、引用终检和评价维度 | 由产品/检索/AI 责任方冻结评价集、指标定义、阈值和样本版本 |

当前 3 项阻塞不是目标文档缺陷，不能通过擅自补写供应商处理政策、安全机制或质量阈值关闭，也不计入 S0～S3。原 `EB-L1-004` 已根据用户明确指定的 Dylan 关闭；这属于评审后事实同步，不等同于完成新的正式复评。

## 4. 发现汇总

| 严重级别 | 初始/新增数量 | 最终未关闭数量 | 对评审门禁的影响 |
|---|---:|---:|---|
| S0 | 0 | 0 | 无 |
| S1 | 1 | 0 | 第 5.2 节发现初始阻塞 L1→L2 的审计所有权确定性，现已关闭 |
| S2 | 2 | 0 | 其中 1 个在第 2 轮新增；均为下位治理与追踪确定性问题，现已关闭 |
| S3 | 0 | 0 | 无 |

## 5. 评审发现

### 5.1 S0 阻断缺陷

未发现 S0 文档缺陷。

### 5.2 S1 严重问题

| ID | 层级 | 位置 | 证据 | 期望与实际差异 | 触发场景和影响 | 修改建议/责任方 | 门禁影响 | 最终状态 |
|---|---|---|---|---|---|---|---|---|
| REV-L1-001 | L1 | 初始第 8 节“数据、状态与运行时所有权”、第 10.5 节 | Tool 调用记录的权威写为“Java Agent/MySQL 或审计端口”，同时指标/追踪后端允许 No-op | 强制安全审计事实必须有唯一持久权威；可丢遥测不能与审计事实二选一 | No-op 生效或两个 L2 分别实现时，Tool 越权/重放证据可能未持久化或形成双权威 | 由 Agent L1 文档维护人分离“Tool 调用审计事实”和“遥测派生事件”，明确 Java/MySQL 持久权威及 L2 分工 | 初始阻塞 L1→L2 | 已修复：第 8、10.5、13 节已建立唯一权威、No-op 边界和语义/物理分工；第 2、3 轮复评通过 |

### 5.3 S2 一般问题

| ID | 层级 | 位置 | 证据 | 期望与实际差异 | 触发场景和影响 | 修改建议/责任方 | 门禁影响 | 最终状态 |
|---|---|---|---|---|---|---|---|---|
| REV-L1-002 | L1 | 初始第 13 节“L2 下位交付拆分” | `APP-DEC-01` 未落到 L2；`AD-01/AD-07` 的资产隔离和服务收敛没有下位验证所有者 | L1 的局部决策和实施性约束应有 L2 交付或明确不适用理由 | L2 并行编写时可能遗漏双运行时拓扑验证、alpha 隔离或误建 `es-document-service` | 由 Agent L1 文档维护人补入部署/扩展验证 L2 的约束和门禁 | 不单独阻断，但削弱下位治理 | 已修复：第 13 节已承接 `AD-01`、`AD-07`、`APP-DEC-01` 并增加验证门禁；第 2、3 轮复评通过 |
| REV-L1-003 | L1 | 修订后第 13 节“承接的 L0/L1 约束”列 | 使用 `AD-03/05/19`、`APP-DEC-03/05` 等缩写，自动追踪只能识别首个稳定 ID | 所有稳定 ID 应逐项完整书写，使下位生成、审查和验证可确定追踪 | 自动抽取或后续 L2 逐项映射时产生假遗漏/假覆盖 | 由 Agent L1 文档维护人将缩写改为完整 ID 序列 | 不单独阻断，但影响追踪证据 | 已修复：第 13 节完整书写全部 ID；第 3 轮复评和自定义覆盖检查通过 |

### 5.4 S3 建议优化

未记录不影响门禁的风格性建议。

## 6. 分层评审结果

### 6.1 L1 模块/领域架构

| 检查域 | 结论 | 证据/发现 ID |
|---|---|---|
| L0 约束映射 | 通过 | 第 4、15 节逐项承接 `AD-01～AD-20`、`DEC-01～DEC-13`；追踪脚本无缺失 |
| 唯一职责和非职责 | 通过 | 第 3、6 节明确 Java/Python、Adapter、auth、业务源和检索域边界 |
| 同层协作与契约 | 受限通过 | 第 6.3、7 节已定义消费边界、禁止侵入和联合门禁；检索 L1 尚未创建，双方契约未验证 |
| 内部架构和依赖方向 | 通过 | 第 5.3、6.2、6.4 节无反向代码依赖或 Python 直连业务/DB/ES |
| 数据/状态和横切策略 | 通过 | 第 8～11 节明确 auth/业务/文档/Java/Python/检索权威；第 5.2 节发现已关闭 |
| L2 交付约束 | 通过（内容）/受阻（正式门禁） | 第 13 节覆盖 `AD-01～AD-20`、`APP-DEC-01～APP-DEC-11`；第 5.3 节两项发现已关闭，但当前 `EB-L1-001～003` 未解除 |

## 7. 跨层追踪结果

| 上游 ID/位置 | 下游 ID/位置 | 映射状态 | 缺陷类型 | 责任方 | 发现 ID |
|---|---|---|---|---|---|
| L0 `AD-01～AD-20` | L1 第 4 节及第 13 节 | 已覆盖 | 无 | Agent L1 | — |
| L0 `DEC-01～DEC-13` | L1 第 12、15.1 节 | 已覆盖 | 无 | Agent L1 | — |
| L1 `APP-DEC-01～APP-DEC-11` | L1 第 13 节治理的 L2 交付 | 已覆盖 | 原不可追踪，已修复 | Agent L1 | 第 5.3 节两项 S2 |
| L0 正式评审前置门禁 | L1 第 5.5、16.2～16.4 节 | 部分满足 | LLM 目标配置和维护人已确认；数据边界/兼容性/效果、内部服务身份、DOCUMENT 评价门禁仍缺失 | Dylan / AI / 安全 / 检索责任方 | EB-L1-001～003 |
| L0 Agent/检索同层边界 | L1 第 3.4、6.3、7.2 节 | 本文侧已覆盖、对方侧未验证 | 同层文档尚未创建 | Agent 与检索 L1 共同责任方 | 证据限制，不计文档发现 |

## 8. 实现就绪判断

- 当前门禁：L1→L2。
- 允许的下一步：形成 `EB-L1-001～003` 所需的数据处理/兼容性/效果、内部服务身份机制和 DOCUMENT 评价基线证据；可做不具权威性的技术验证或材料准备。
- 暂不允许的动作：将本 L1 标记为“已通过”，把其作为正式 L2 治理基线，或据此直接进入生产实现批准。
- 进入下一步前必须完成：解除当前 3 项执行阻塞，并对当前文档做一次正式复评；若外部决策改变现有边界，须先按授权修订 L0/外部契约。
- 后续是否需要代码对照评审：L2 完成并进入实现后需要；本次未评审代码，也不对既有实验实现背书。

## 9. 证据限制与未验证项

| 项目 | 原因 | 对结论的影响 | 建议验证方式 |
|---|---|---|---|
| 检索与索引基础设施 L1 | 尚未创建 | 只能确认本文侧未侵入，不能确认查询面语义、撤权传播和兼容门禁已被对方承接 | 创建并评审关联 L1 后执行双向一致性评审 |
| employee/transaction 外部接口语义 | 本次未将外部契约作为评审目标，目标也标记需在 L2 核实 | 不影响当前职责边界判断，但阻塞对应 QUERY/AGGREGATE L2 的契约定稿 | 在业务适配 L2 前做接口差距与调用方影响评审 |
| 文档源契约 | 稳定 ID、业务版本、资源属性和撤权契约尚未冻结 | 不影响 DOCUMENT 归属判断，但阻塞 DOCUMENT/检索详细设计和联合验收 | 由来源方、安全、检索共同发布权威契约 |
| 数值化性能与生产拓扑 | L0 将其截止条件设为 L2 基线/生产批准前 | 不影响 L1 结构门禁，但当前不能声称生产就绪 | 在 L2 压测与生产架构评审中确认 |
| 代码/测试/运行环境 | 用户要求新架构不以现有实验项目为基线，本次为文档评审 | 不对实现正确性或当前运行状态作结论 | 完成 L2 后使用代码对文档评审及最小相关测试 |

## 10. 推荐处理顺序

| 顺序 | 发现/阻塞 ID | 目标文件和责任方 | 所需授权 | 建议处理方式 |
|---:|---|---|---|---|
| 1 | EB-L1-001 | DeepSeek 数据处理边界、API 兼容性与效果证据；Dylan / AI 平台 / 安全负责人 | 涉及数据外发、服务条款或生产依赖时需用户/责任方确认 | 完成数据分类与发送矩阵、留存/训练/地域结论、契约测试和效果 PoC |
| 2 | EB-L1-002 | 内部服务身份与能力凭据机制；安全/平台负责人 | 涉及新安全基础设施或公共契约时需单独授权 | 冻结机制、凭据生命周期、重放防护和失败语义 |
| 3 | EB-L1-003 | DOCUMENT 评价基线；产品/检索/AI 负责人 | 需业务验收责任方确认 | 冻结评价集版本、指标定义、阈值和降级门禁 |
| 4 | EB-L1-001～003 | 当前 L1；Dylan | 用户授权复评；若边界变化再授权修订 | 使用 `design-doc-review` 执行一次正式复评并同步状态 |

## 11. 复审记录

| 轮次 | 基线版本/日期 | 已修复 | 部分修复 | 未修复 | 新增问题 | 结论 |
|---:|---|---|---|---|---|---|
| 1 | v0.1 / 2026-07-16 | 无 | 无 | 第 5.2 节 S1、第 5.3 节首项 S2；`EB-L1-001～004` | 无 | 无法完成评审；授权修复目标内发现 |
| 2 | v0.1 修订 5 / 2026-07-16 | 第 5.2 节 S1、第 5.3 节首项 S2 | 无 | `EB-L1-001～004` | 第 5.3 节第二项 S2 | 无法完成评审；继续修复新增 S2 |
| 3 | v0.1 修订 6 / 2026-07-16 | 第 5.3 节第二项 S2 | 无 | `EB-L1-001～004` | 无 | 无法完成评审；文档发现归零，因外部阻塞提前结束循环 |
| 评审后同步 | v0.1 修订 8 / 2026-07-16 | `EB-L1-004`；`EB-L1-001` 的供应商/模型/API 选型部分 | `EB-L1-001` | `EB-L1-002～003` | 无 | 非复评；继续保持评审受阻，等待 3 项门禁证据 |

## 12. 最终结论

目标 L1 经 3 轮正式评审—修复后，S0/S1/S2/S3 最终未关闭数量为 0/0/0/0，L0 约束、职责边界、状态权威及 L2 交付内容已达到可治理质量。评审后已确认 DeepSeek `deepseek-v4-flash`、API Base URL `https://api.deepseek.com` 和维护人 Dylan；原维护人阻塞已关闭，LLM 选型阻塞部分解除。但当前仍有 3 项正式前置输入缺失，故 L1→L2 门禁结论继续为“无法完成评审”，就绪状态为 `undetermined`。在解除 `EB-L1-001～003` 并完成复评前，文档状态应保持“已评审 / 评审受阻 / 未实施 / 未生效”。
