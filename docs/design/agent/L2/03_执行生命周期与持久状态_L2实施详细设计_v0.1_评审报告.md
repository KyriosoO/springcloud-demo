# 执行生命周期与持久状态 L2 实施详细设计 v0.1 评审报告

## 1. 评审结论

| 项目 | 内容 |
|---|---|
| 结论 | 通过 |
| 评审类型 | detailed_l2；包含适用的跨层一致性核对 |
| 评审门禁 | L2→实现评审；P1→P2 设计输入冻结 |
| 就绪状态 | ready |
| 目标文档 | `docs/design/agent/L2/03_执行生命周期与持久状态_L2实施详细设计_v0.1.md` |
| 目标状态 | `In Review`；独立评审不替代项目正式批准 |
| 执行阻塞数 | 0 |
| 最终 S0 / S1 / S2 / S3 | 0 / 0 / 0 / 0 |
| 历史已关闭 S0 / S1 / S2 / S3 | 0 / 8 / 3 / 0 |
| 是否修改被评审文档 | 每一轮独立评审阶段均为只读；冻结发现后，经用户授权由 `detailed-design-document` 修订目标，再重新读取并复审 |
| 主要结论 | 生命周期、状态、事务、deadline、fence、幂等、重试、恢复、数据约束、审计与验证已达到实现评审所需完整度；设计通过不授予代码、Schema、契约、流量或生产动作 |

## 2. 评审范围与基线

| 资源 | 角色 | 层级 | 状态/版本 | 是否读取 | 权威性/作用 | 置信度 |
|---|---|---|---|---|---|---|
| `docs/design/agent/单体Agent智能体总体架构_L0_v1.0.md` | parent | L0 | 已评审/已通过/未实施/未生效，v1.0 | 是 | Java/MySQL 权威、短事务、受控 SQL、deadline、重试、终态/fence | high |
| `docs/design/agent/单体Agent应用与能力架构_L1_v1.0.md` | parent | L1 | 已评审/已通过/未实施/未生效，v1.0 | 是 | Agent 生命周期、状态、重试、恢复和 SQL 下位约束 | high |
| `docs/design/agent/L2/00_单体Agent目标架构L2实施详细设计总览_v1.0.md` | parent | L2 总览 | In Review，v1.0 | 是 | 03 专题职责、跨专题门禁和四类证据边界 | high |
| `docs/plans/M2-A-0_03-04-05共同输入确认记录.md` | source | 计划记录 | M2-A-0 已完成 | 是 | 公共术语、迁移输入和临时追踪；非运行权威 | high |
| `docs/plans/M2-A_03-04-05详细设计实施计划.md` | source | 计划 | Draft | 是 | P1 任务、退出门禁和原子同步范围 | high |
| `docs/design/agent/L2/01_跨运行时契约与生成治理_L2实施详细设计_v1.0.md` | peer/contract owner | L2 | Implementing，v1.0 | 是 | 唯一 OpenAPI、七项贡献、生成和兼容治理 | high |
| `agent-api/src/main/resources/openapi/agent-runtime-openapi.json` | external_contract | OpenAPI | 当前活动源 | 是 | 验证当前仅有 readiness，生命周期 operation 尚未实施 | high |
| `agent-service`、`agent-api`、`agent-runtime` 当前目标模块 | implementation_evidence | 代码基线 | P0 后绿地目标 | 是，限于路径/依赖/契约事实 | 验证生命周期、MySQL 和相关测试尚未实施 | high |
| `docs/design/P1_V2/02_可信执行内核与Invocation生命周期_L2实施详细设计_v2.0.md`、`agent-service_alpha` | migration source | 历史 | 隔离输入 | 仅作机制候选核对 | 不提供目标语义，不参与合规基线 | high |

### 2.1 范围内

- 完整评审 03 当前文档的功能、责任、契约贡献、数据/状态、并发、一致性、安全、运维、实现落点、测试、追踪和治理。
- 对 L0、Agent L1、L2 总览、01 与 M2-A 输入进行适用约束映射。
- 执行 5 轮“只读评审→冻结发现→授权修订→重读复审”。

### 2.2 范围外

- 不评审或修改 04、05 的未来内容，不代替 P4 联合收敛。
- 不评审实际生命周期代码正确性；当前实现尚不存在。
- 不修改唯一 OpenAPI、生成物、fixture、代码、POM、SQL、配置、测试或运行环境。
- 不作项目正式批准、目标质量验证、生产批准或流量放行。

### 2.3 基线优先级与冲突

采用“用户本次授权 → 当前 L0 → Agent L1 → L2 总览 → 01 契约治理 → 03 → 当前实现事实 → 工程实践”的顺序。历史 `P1_V2/02` 与 `*_alpha` 仅用于发现候选机制；任何差异均以当前权威链判定，未让历史实现覆盖目标设计。

## 3. 执行阻塞

未发现影响本次核心结论的执行阻塞。目标、上位链、契约所有者、实现基线、报告路径和修订权限均可确定。

## 4. 发现汇总

| 严重级别 | 最终未关闭 | 历史已关闭 | 对门禁的最终影响 |
|---|---:|---:|---|
| S0 | 0 | 0 | 无 |
| S1 | 0 | 8 | 均经目标内修订和重新评审关闭 |
| S2 | 0 | 3 | 均经目标内修订和原子治理同步关闭 |
| S3 | 0 | 0 | 无 |

## 5. 已关闭评审发现

### 5.1 S1 严重问题

| ID | 层级/位置 | 证据与期望 | 实际差异 | 触发场景和影响 | 最小修复、责任方与授权 | 复审状态 |
|---|---|---|---|---|---|---|
| REV-03-001 | cross-layer；原第 9.2.4、IMPL-03-007 | L0/Agent L1 `AD-12` 要求只交付受控 SQL、DB 责任方执行、应用启动不迁移 | 原设计使用 `resources/db/migration` 并等待应用迁移执行器，存在上位冲突 | 引入 Flyway 或启动 DDL 时越权修改生产 Schema | 改为非自动扫描的受控 SQL 源、DB owner/checksum 证据和启动只读门禁；03 维护者，目标修订已授权 | fixed；DR-03-016、TEST-03-016、VAL-03-007 |
| REV-03-002 | L2；原第 8.1、8.5、9.2 | 幂等摘要必须可跨语言重现、版本化并具有确定窗口，安全摘要不得泄漏 | 原设计只写 canonical SHA-256，未固定投影、canonicalization、scheme、窗口，且允许日志记录 digest 前缀 | 混合版本/字段顺序变化可能误判冲突或重复执行，摘要可被关联 | 固定 RFC 8785、字段投影、fingerprint、scheme、墓碑窗口和禁止输出；Java Lifecycle + 01 贡献，目标修订已授权 | fixed；DR-03-017、TEST-03-001/005 |
| REV-03-003 | L2 data/state；原 attempt 表和 recovery | execution 与 attempt 同时保存可变 lease owner/expiry | 恢复权存在双事实源 | 崩溃/续租竞争时不同实例可能依据不同 lease 重放 | execution row 成为唯一可变 lease 权威，attempt 仅保留 runner 诊断引用；Repository owner，目标修订已授权 | fixed；DR-03-019、TEST-03-014 |
| REV-03-004 | L2 consistency；原终态事务 | L0/L1 要求结果、引用与终态由 Java/MySQL 持久权威控制 | 原成功事务只保证 execution/attempt/event，resultRef 可能悬空或指向未提交结果 | 结果写入失败但 SUCCEEDED 已提交，调用方读取不到权威结果 | 要求结果同事务提交或预提交不可变引用经世代/digest 校验；Lifecycle + 后续结果 owner，目标修订已授权 | fixed；DR-03-018、TEST-03-013 |
| REV-03-005 | L2 recovery；原预算预约与恢复 | 恢复必须区分“确定未调用”和“可能已调用”，commit-unknown 不得猜测 | 原设计只有全局计数，缺少 attempt 级预约证据 | 分配 attempt 后崩溃会被误判 unsafe，或把已发调用误作未发而重复执行 | 预约与唯一 reservation event 同事务，恢复按当前 attempt 证据裁决；Lifecycle/Repository/Audit，目标修订已授权 | fixed；DR-03-019/020、TEST-03-014/015 |
| REV-03-007 | L2 resilience；原第 8.6～8.7、9.8 | SAFE_REPLAY 新 attempt 必须在固化预算内可达，并绑定可审计策略 | 原配置把 maxAttempts 永久限制为 1，安全恢复分支不可达；数值预算无 policy ref/digest | 已证明可重放的工作仍只能失败，或策略变化后恢复语义漂移 | 固化 execution policy 证据，默认 1/1，已评审能力策略才允许大于 1；目标修订已授权 | fixed；DR-03-021、TEST-03-017 |
| REV-03-008 | L2 config；原第 9.8 | 默认值和硬上限必须有单一、不重叠语义 | 第 2 轮修订中 `defaults.*` 同时被称为默认值与系统硬上限 | 不同实现会把 32 解释为可配置默认或不可越过 cap | 拆分 `defaults.*` 与 `limits.*`，固定关系和生效边界；配置 owner，目标修订已授权 | fixed；第 9.8 节、TEST-03-017 |
| REV-03-009 | L2 data/operations；原第 9.2～9.3 | L2→实现评审要求数据库能强制核心状态不变量；每个物理连接的 UTC 语义必须稳定 | 原模型主要依赖 Java 约定，且启动抽查 UTC 不能覆盖连接池重连 | mapper 缺陷可写入双 ACTIVE/非法终态；重连后 session 时区漂移导致 deadline 错判 | 固定 NOT NULL/CHECK/UNIQUE/FK/RESTRICT、generated active slot、逐连接 init SQL 和重连测试；DB/DataSource owner，目标修订已授权 | fixed；DR-03-022、TEST-03-018 |

### 5.2 S2 一般问题

| ID | 层级/位置 | 证据与期望 | 实际差异 | 触发场景和影响 | 最小修复、责任方与授权 | 复审状态 |
|---|---|---|---|---|---|---|
| REV-03-006 | L2 audit；原 event 设计 | 同事务审计应能确定排序、绑定提交版本并支持 commit-unknown 诊断 | 原事件只有 eventId/time 与开放 safeRefs，缺少 execution 内顺序和封闭字段 | 并发事件无法确定权威顺序，任意文本可能进入审计 | 增加 eventSeq/stateVersionAfter、固定 actor/预约字段和 safeRefs 白名单；Audit owner，目标修订已授权 | fixed；DR-03-020、TEST-03-015 |
| REV-03-010 | L2 traceability；第 4、10、11 节 | 新增 DR 必须在具体 IMPL/TEST 行中保持一致映射 | 第 1～4 轮新增 DR 后，部分旧范围表达未显式包含新增规则 | 实现/代码评审者可能遗漏摘要、结果、恢复或数据库规则 | 补齐 CON、IMPL、TEST 的实际映射；03 维护者，目标修订已授权 | fixed；最终严格校验与追踪抽取无孤立 DR/IMPL/TEST |
| REV-03-011 | governance；文档信息、VAL-03-006、风险和结论 | 当前状态、评审证据、冻结边界和授权边界必须一致 | 完成第 5 轮后仍保留 `Draft/未授权独立评审/可申请评审` 等历史当前态表述 | 下游可能错误阻塞 P2，或把评审通过误作实现授权 | 原子同步 `In Review`、报告入口、P1 gate 和禁止实施边界；03/计划/M2-A-0 维护者，用户已授权 | fixed；最终全文陈旧状态扫描通过 |

不存在未关闭 S0、S1、S2 或 S3 发现。

## 6. L2 分层评审结果

| 检查域 | 结论 | 证据/发现 ID |
|---|---|---|
| 功能完整性 | 通过 | REQ-03-001～011；REV-03-002/004/005/007 已关闭 |
| 职责、内聚和耦合 | 通过 | 第 5～7 节；Java/MySQL、Runner、Recovery、DB owner 边界唯一 |
| 契约和跨语言一致性 | 通过 | DR-03-013/017；01 七项贡献及 `IMPL-01-013` 边界明确 |
| 数据、状态、并发和一致性 | 通过 | DR-03-001～012、018～022；REV-03-003/004/005/009 已关闭 |
| 安全、可观测和运维 | 通过 | 第 9.4～9.8 节；REV-03-006/009 已关闭 |
| 实现落点和最小变更 | 通过 | IMPL-03-001～015；无代码/契约越权和历史基线回流 |
| 测试和验证 | 通过 | TEST-03-001～018、VAL-03-001～007；REV-03-010 已关闭 |

## 7. 跨层追踪结果

| 上游 ID/位置 | 下游 ID/位置 | 映射状态 | 缺陷类型 | 责任方 | 发现 ID |
|---|---|---|---|---|---|
| L0 `AD-04`、Agent L1 `AD-04` | CON-03-001；DR-03-001/004/018 | 已覆盖 | 无 | 03 维护者 | REV-03-004 已关闭 |
| L0 `AD-10`、Agent L1 `AD-10` | CON-03-002/007；DR-03-006 | 已覆盖 | 无 | 03 维护者 | 无 |
| L0/Agent L1 `AD-12` | CON-03-013；DR-03-016/022 | 已覆盖 | 原冲突已消除 | 03 + DB 责任方 | REV-03-001 已关闭 |
| L0 `AD-18`、Agent L1 `AD-18` | CON-03-003；DR-03-003/008/018 | 已覆盖 | 无 | 03 维护者 | REV-03-004 已关闭 |
| L0/Agent L1 deadline 与唯一重试 owner | CON-03-004/006；DR-03-007/009～012/019/021 | 已覆盖 | 原弱化已消除 | 03 + 能力 owner | REV-03-005/007/008 已关闭 |
| L2 总览 03 责任 | REQ-03-001～011；DR-03-001～022 | 已覆盖 | 无 | 03 维护者 | 无 |
| 01 第 8.2.3 节 | DR-03-013/017；IMPL-03-013/014 | 已覆盖，待实施授权 | unverified implementation | 01 + 03 维护者 | 非文档缺陷 |

## 8. 实现就绪判断

- 当前门禁：`L2→实现评审` 与 `P1→P2 设计输入冻结` 均满足。
- 允许的下一步：以 03 为只读生命周期基线编写 P2（04）；在另获代码授权后，可进入 Java 纯域/持久化实现及代码对照评审。
- 暂不允许的动作：修改 OpenAPI/生成物、实施代码/POM/SQL/配置/测试、执行 Schema、启用 recovery/Runtime/Tool 流量、声称目标质量或生产批准。
- 进入 Schema 应用前必须完成：指派 DB 责任方，形成受控 SQL 清单/checksum/环境核验和 VAL-03-007 证据。
- 进入跨运行时生命周期实现前必须完成：03 七项 change set 经 01 评审并单独取得 `IMPL-01-013` 授权。
- 后续是否需要代码对照评审：需要；代码存在后使用 `code-review-against-docs` 全量核对 DR/IMPL/TEST/VAL。

## 9. 证据限制与未验证项

| 项目 | 原因 | 对结论的影响 | 建议验证方式 |
|---|---|---|---|
| 生命周期 Java/MySQL 实现 | 尚未实施且本次未授权代码修改 | 不影响设计通过；实际正确性未知 | 实施后运行 VAL-03-002/003 并执行代码对照评审 |
| 生命周期 OpenAPI operation/Schema | `IMPL-01-013` 未授权 | 不影响 03 业务语义冻结；阻塞跨运行时实现 | 按 01 七项清单提交独立 change set |
| DB 责任方和 SQL 执行证据 | 尚未指派/执行 | 不影响 L2→实现评审；阻塞 Schema 应用 | 执行 VAL-03-007 |
| 04 continuation decision、05 capabilityId/fence | 下游专题尚未创建 | 不影响 03 评审；阻塞 M2-A 和相关联调 | P2/P3 分别设计并独立评审 |
| retention、SLO、告警阈值 | 由 12/12A 量化 | 不影响生命周期骨架；阻塞生产门禁 | 后续生产专题量化并实测 |

## 10. 推荐处理顺序

| 顺序 | 目标 | 责任方 | 所需授权 | 建议处理方式 |
|---:|---|---|---|---|
| 1 | 以 03 冻结语义进入 04 详细设计 | 04 文档维护者 | 04 文档编写授权 | `detailed-design-document` |
| 2 | 指派 DB 责任方并冻结受控 SQL 执行证据格式 | DB/Agent 责任方 | DB 治理授权；不等于执行授权 | 专题设计/治理确认 |
| 3 | 提交 03 的 01 七项契约贡献 | 03 + 01 维护者 | `IMPL-01-013` 单独授权 | 契约 change set 与兼容评审 |
| 4 | 实施 03 并运行 MySQL 8.0 验证 | Java/DB 实施者 | 代码、依赖、SQL、配置、测试授权 | `implement-from-detailed-design` |
| 5 | 对实际实现做文档符合性评审 | 独立代码评审者 | 只读评审或 review-and-fix 授权 | `code-review-against-docs` |

## 11. 复审记录

| 轮次 | 基线版本/日期 | 已修复 | 部分修复 | 未修复 | 新增问题 | 结论 |
|---:|---|---|---|---|---|---|
| 1 | `bb4de34` 后的 03 v0.1，2026-07-21 | REV-03-001～006 | 0 | 0 | 0 | 原发现关闭；进入第 2 轮 |
| 2 | 第 1 轮修订，2026-07-21 | REV-03-007 | 0 | 0 | REV-03-007 | 关闭；进入第 3 轮 |
| 3 | 第 2 轮修订，2026-07-21 | REV-03-008 | 0 | 0 | REV-03-008 | 关闭；进入第 4 轮 |
| 4 | 第 3 轮修订，2026-07-21 | REV-03-009 | 0 | 0 | REV-03-009 | 关闭；进入第 5 轮 |
| 5 | 第 4 轮修订，2026-07-21 | REV-03-010、REV-03-011 | 0 | 0 | REV-03-010、REV-03-011 | 最终通过 |

## 12. 最终结论

03 v0.1 的独立评审结论为“通过”，就绪状态为 `ready`。5 轮评审-修复后无执行阻塞、无未关闭 S0/S1/S2/S3，满足 L2→实现评审及 P1→P2 设计门禁。允许把冻结的生命周期公共语义作为 04 的只读输入；实际代码、Schema、唯一 OpenAPI、目标质量、流量和生产动作继续受各自门禁与授权约束。
