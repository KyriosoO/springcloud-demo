# 02A 方案 B 实施代码评审报告

## 1. 执行摘要

| 项目 | 内容 |
|---|---|
| 评审模式 | `review_and_fix` |
| 最大循环次数 | 5 |
| 实际执行轮次 | 5 |
| 主依据文档 | `02A_外部消费者活动基线退出与DB-02-001关闭_L2实施详细设计_v1.0.md` |
| 上位依据 | `02_目标基线隔离与迁移门禁_L2实施详细设计_v1.0.md`、`00_单体Agent目标架构L2实施详细设计总览_v1.0.md` |
| 评审范围 | 02A 的4个既有文件、验证脚本、Gateway 测试、固定关闭证据及只读活动入口/分类引用 |
| 是否修改代码 | 是；只修复验证脚本及其固定证据，未扩大业务实现范围 |
| 问题统计 | High 5、Medium 6；共11项，全部已修复 |
| 验证结果 | 通过；完整 reactor、Gateway 测试、依赖树、负向门禁和恢复门禁均已执行 |
| 最终结论 | 通过；02A 本地实现满足方案 B，`DB-02-001` 已形成可供02消费的关闭证据，但尚未满足提交后干净 HEAD/P0 Preflight 门禁 |

评审基线为 Git HEAD `2a9939f955fe2dc000b2b2617689468fd3489474` 加当前未提交的02A实施差异。依据文档哈希分别为：00 `21ED707E...6499`、02 `A0D587C3...73`、02A `8CE1DB4F...02D`。

## 2. 文档依据清单

| 文档 | 角色 | 优先级 | 是否必需 | 读取结果 | 备注 |
|---|---|---:|---|---|---|
| `02A_外部消费者活动基线退出与DB-02-001关闭_L2实施详细设计_v1.0.md` | detailed_design | 0 | 是 | 已读取并严格校验 | 主追踪锚点 |
| `02_目标基线隔离与迁移门禁_L2实施详细设计_v1.0.md` | governing detailed_design | 1 | 是 | 已读取 | 固定P0报告、DB关闭和P0门禁 |
| `00_单体Agent目标架构L2实施详细设计总览_v1.0.md` | governing_overview | 2 | 是 | 已读取 | 阶段顺序、旧资产和证据边界 |
| 当前仓库规则 | repository_rules | 3 | 是 | 已读取 | 最小修改、验证、Git和文档边界 |

未发现阻断代码评审的文档冲突。三份L2仍为 `Draft`；本报告不修改其状态，也不将代码评审通过解释为P0或生产批准。

## 3. 文档约束追踪

| 约束编号 | 来源 | 约束内容 | 对应实现/证据 | 最终结果 |
|---|---|---|---|---|
| DOC-C-001 | 02A DR-004、DR-005 | 只移除 `employee-service` 根 module，保留 Provider，禁止 alpha/假兼容 | `serviceCenter/pom.xml`、完整 reactor、dependency tree | 符合 |
| DOC-C-002 | 02A DR-006、DR-016 | 默认启动/Eureka/UI 退出 employee，保留 P0 Generation 等待项和其他入口 | `start-all-services.ps1`、`home.html`、静态核验 | 符合 |
| DOC-C-003 | 02A DR-007 | 删除完整 `emp` 路由且不提供伪兼容 | `GatewayRouter.java`、`GatewayEmployeeRouteExitTest.java` | 符合 |
| DOC-C-004 | 02A DR-008、TEST-005 | `employee-service` 字节级保持不变并记录跟踪文件数/SHA | 43个跟踪文件；聚合 SHA `414E2B5B...4E3A` | 符合 |
| DOC-C-005 | 02A DR-009、DR-010、VAL-007 | 扫描 tracked/untracked 活动入口并唯一分类允许残留 | 验证器扩展扫描和三类 `referenceClassifications` | 符合 |
| DOC-C-006 | 02A DR-012 | 完整 reactor、Gateway 测试、依赖树和完整性均通过后才发布 `CONSUMER_EXIT_VERIFIED` | `PostApply` 内执行三项 Maven 验证后原子写报告 | 符合 |
| DOC-C-007 | 02A DR-013 | 报告 schema 校验、独占锁、原子替换且验证可重跑 | schema/manifest 强校验；重复 `PostApply` 已通过 | 符合 |
| DOC-C-008 | 02A DR-014；02 固定P0报告 | P0前允许逆序恢复；P0消费后拒绝单独恢复 | 固定 `baseline-isolation-report.json`；正负恢复测试 | 符合 |
| DOC-C-009 | 02A DR-015 | 关闭仅表示 `CLOSED_FOR_P0_BY_INACTIVITY` | 固定证据状态与 assertions | 符合 |
| DOC-C-010 | 00 DR-006、DR-008 | 不把代码/固定证据混同为P0、质量或生产批准 | 证据和本报告均保留门禁声明 | 符合 |

## 4. 代码问题清单

| 编号 | 级别 | 类型 | 文件 | 依据 | 问题描述与触发影响 | 修复与状态 |
|---|---|---|---|---|---|---|
| CR-02A-001 | high | implementation_completeness | `scripts/verify-agent-external-consumer-exit.ps1` | DR-012、VAL-003/005/008/009 | `PostApply` 未实际运行构建、测试和依赖树即可写 `PASSED`，可能假阳性关闭 blocker | 将三项 Maven 命令纳入 `PostApply`，任一失败不发布报告；已修复 |
| CR-02A-002 | high | design_consistency | 同上 | DR-009、DR-010、VAL-007 | 只扫描已跟踪的少量扩展名，漏掉未跟踪 `.sh`、Java/JSON/构建文件等活动入口 | 使用 `git ls-files -co --exclude-standard`，扩展文本/构建文件类型并执行负向测试；已修复 |
| CR-02A-003 | medium | evidence_completeness | 同上、固定证据 | TEST-005、VAL-006 | 只检查 `git diff`，未记录文件数和聚合SHA | 记录43个跟踪文件和聚合SHA，并写入消费者证据；已修复 |
| CR-02A-004 | medium | idempotency | 同上 | DR-013 | 删除临时 Preflight 状态后无法重复 `PostApply` | 允许从既有有效报告恢复只读上下文，并校验 schema/manifest/设计哈希；重复运行通过；已修复 |
| CR-02A-005 | medium | classification_boundary | 同上 | DR-010、TEST-006 | 按整个旧 Agent 目录豁免，可能隐藏目录内新增的 build/deploy 脚本 | 只豁免实际分类证据文件；旧 Agent 目录内临时脚本被正确拒绝；已修复 |
| CR-02A-006 | medium | validation | 同上 | DR-013、TEST-007 | schema 只检查数量，不检查状态值、分类唯一性和清单唯一性 | 增加强状态、分类、消费者字段和manifest唯一性断言；已修复 |
| CR-02A-007 | high | security/scope | 同上 | DR-011、DR-013 | `OutputPath` 可写仓库外任意路径 | 固定为唯一 `external-consumer-resolution.json` 路径；仓库外路径负向测试通过；已修复 |
| CR-02A-008 | medium | recovery_consistency | 同上 | DR-014 | `RecoveryCheck` 未复核HEAD、设计哈希、manifest和现场 | 恢复检查新增完整schema/manifest/HEAD/现场扫描；已修复 |
| CR-02A-009 | medium | security/scope | 同上 | DR-011、DR-014 | `P0ReportPath` 可读取任意本地文件 | 先限制在P0证据目录，随后进一步收紧为02固定报告；已修复 |
| CR-02A-010 | high | recovery_gate | 同上 | 02固定报告、DR-014 | 可传不存在的任意JSON绕过真正P0报告 | 强制使用 `baseline-isolation-report.json`；已修复 |
| CR-02A-011 | high | recovery_gate | 同上 | DR-014 | 绝对 `OutputPath` 与P0相对引用字符串不一致时可能漏判P0已消费 | 结构化解析固定引用并规范路径；绝对路径负向测试返回 `ROLLBACK_ORDER_VIOLATION`；已修复 |

本轮未发现冗余业务层、冗余DTO、重复业务规则或与02A无关的重构。Gateway 测试采用轻量真实路径谓词上下文，不启动Eureka或完整应用栈。

## 5. 文档问题清单

无阻断或需修改的文档问题。现有设计明确限定了能力退出、P0固定报告、恢复顺序和生产非授权语义；因此不建议修改00、02或02A正文。

## 6. 修改摘要

| 轮次 | 修改文件 | 修改内容 | 对应问题 | 结果 |
|---:|---|---|---|---|
| 1 | 验证脚本、固定证据 | `PostApply` 执行构建/测试/依赖树；补全入口扫描、分类和员工目录摘要；支持重验 | CR-001～004 | 已修复 |
| 2 | 验证脚本、固定证据 | 分类豁免收窄到实际证据文件；强化schema状态和唯一性 | CR-005～006 | 已修复 |
| 3 | 验证脚本、固定证据 | 固定输出路径；恢复时复核HEAD/manifest/现场；限制P0读取范围 | CR-007～009 | 已修复 |
| 4 | 验证脚本、固定证据 | P0报告路径固定为02权威 `baseline-isolation-report.json` | CR-010 | 已修复 |
| 5 | 验证脚本、固定证据 | 结构化解析P0消费引用，消除绝对/相对路径漏判 | CR-011 | 已修复 |

未修改 `serviceCenter/pom.xml`、启动脚本、Gateway 路由、默认首页和Gateway测试的方案B业务行为；这些实现经评审保持原状。未修改 `employee-service`、Provider/Generation资产、旧Agent调用方、Config Server员工配置、L0/L1/L2正文或工程依赖。

## 7. 验证结果

| 轮次/阶段 | 命令或检查 | 结果 | 摘要 |
|---|---|---|---|
| 每轮 | PowerShell AST、`git diff --check` | 通过 | 验证脚本语法有效，无空白错误 |
| 1 | 未跟踪 `scripts/*.sh` 活动入口负向测试 | 通过 | 返回 `CONSUMER_STILL_ACTIVE` |
| 2 | 旧 Agent 目录内活动脚本负向测试 | 通过 | 未被分类豁免，返回 `CONSUMER_STILL_ACTIVE` |
| 3 | 仓库外 `OutputPath`、P0目录外路径负向测试 | 通过 | 分别返回 `OUTPUT_PATH_NOT_ALLOWED`、`P0_REPORT_PATH_NOT_ALLOWED` |
| 4 | 任意不存在P0 JSON路径负向测试 | 通过 | 仅接受02固定报告路径 |
| 5 | 绝对 OutputPath + 已开始P0固定报告 | 通过 | 返回 `ROLLBACK_ORDER_VIOLATION` |
| 最终 PostApply | `mvnw ... -DskipTests install` | 通过 | 28/28 reactor模块成功；无employee；Provider成功 |
| 最终 PostApply | `mvnw ... -pl ../gateway-service -am test` | 通过 | `common-security` 18项、`gateway-service` 3项通过，0失败/0错误 |
| 最终 PostApply | `mvnw ... -Dincludes=... dependency:tree` | 通过 | 无 `employee-service`/`employee-service_alpha` 活动依赖 |
| 最终 | 02A严格文档校验 | 通过 | 0错误、0警告 |
| 最终 | 证据schema、manifest和禁止范围检查 | 通过 | 6项清单一致；`employee-service`差异0；禁止范围差异0 |

最终固定证据：

- 状态：`CONSUMER_EXIT_VERIFIED`
- 关闭语义：`CLOSED_FOR_P0_BY_INACTIVITY`
- 未决项：0
- 证据 SHA-256：`15D2B72DE6A9DD335B757DE9E767316D6EF11C2FBACE915B63C0A0F30AB2EBDC`

## 8. 剩余风险

| 编号 | 级别 | 风险 | 原因 | 后续建议 |
|---|---|---|---|---|
| RISK-CR-001 | blocker（仅对P0） | 当前仍不能进入P0 | 方案B代码、证据和本报告尚未提交为干净新HEAD，02 Preflight尚未消费证据 | 用户审阅本地diff后另行授权commit，再执行02 Preflight |
| RISK-CR-002 | high（生产范围） | 仓库外调用方、生产部署和消息积压影响未验证 | 02A授权只覆盖仓库默认基线 | 生产变更另行盘点、评审和授权 |
| RISK-CR-003 | medium | 未运行完整服务栈做真实HTTP 404/Eureka观测 | 当前以Gateway路由行为测试和静态启动清单证明 | 如进入部署验证阶段，补充非生产环境运行态验收 |
| RISK-CR-004 | low | Git提示两个既有CRLF文件未来可能转换为LF | 工作区Git换行策略提示 | 提交前继续以diff核对，避免无关格式化 |

## 9. 结论

最终结论：**通过**。

02A方案B的本地代码、测试、验证器和固定关闭证据符合主设计及00/02上位边界；5轮共11项问题全部关闭，无剩余代码问题。该结论只证明 `DB-02-001` 已具备 `CLOSED_FOR_P0_BY_INACTIVITY` 的本地证据，不代表功能迁移、员工能力替代、P0完成、目标系统质量或生产批准。

在用户另行授权提交并形成干净新HEAD、且02 Preflight成功消费固定证据之前，不得开始P0。
