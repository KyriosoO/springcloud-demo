# 外部消费者活动基线退出与 DB-02-001 关闭 L2 实施详细设计 v1.0 评审报告

## 1. 评审结论

| 项目 | 内容 |
|---|---|
| 结论 | 有条件通过 |
| 评审类型 | `detailed_l2 + cross_layer` |
| 评审门禁 | `L2→实现评审` |
| 就绪状态 | `ready_with_conditions` |
| 目标文档 | [02A_外部消费者活动基线退出与DB-02-001关闭_L2实施详细设计_v1.0.md](./02A_外部消费者活动基线退出与DB-02-001关闭_L2实施详细设计_v1.0.md) |
| 最终 SHA-256 | `47210DEC764D33B704CF457AA0DCB30F46599B9173EA3C3C16DB27FB97639CE9` |
| 执行阻塞数 | 0 |
| 最终 S0 / S1 / S2 / S3 | 0 / 0 / 0 / 0 |
| 是否修改被评审文档 | 否；每次独立评审 pass 均为只读。第 1 轮发现冻结后，在两次评审之间按用户预授权切换 `detailed-design-document` 修订 |
| 最大轮次 / 实际轮次 | 5 / 4 |
| 主要结论 | 独立设计评审保持有条件通过；后续方案 B、固定关闭报告及上位 02 P0 验证已完成，`DB-02-001` 保持关闭，尚未生产生效 |

本报告保留设计层面的有条件通过结论；目标文档后续已实施并关闭 `DB-02-001`，但状态同步不替代生产变更审批。

## 2. 评审范围与基线

| 资源 | 角色 | 层级 | 状态/版本 | 是否读取 | 权威性/作用 | 置信度 |
|---|---|---|---|---|---|---|
| 02A 方案 B v1.0 | target | L2 | 已实施 / `DB-02-001 Closed` / 未生效；SHA-256 `47210DEC764D33B704CF457AA0DCB30F46599B9173EA3C3C16DB27FB97639CE9`；独立设计评审有条件通过 | 是，全文 | `DB-02-001` 关闭设计和证据契约 | 高 |
| 02 基线隔离 v1.0 | parent | L2 | 已实施 / `P0_VERIFIED` / 未生效；SHA-256 `2A75E29B681D646915B1AE88F05B2B0B1E1C000EC69F13B548E01BAB53CC490F` | 是，全文 | blocker、P0 Preflight 和引用消费方 | 高 |
| 00 总览 v1.0 | governance parent | L2 | `Draft`；SHA-256 `DF4C38165B64F4E2386F7F1EE407E72AE8D0BE5A676CBA6BE2B15B916B760800` | 是，全文 | 阶段、绿地和授权边界 | 高 |
| `serviceCenter/pom.xml`、`employee-service/pom.xml` | implementation baseline | Maven | 当前 Git 工作区 | 是 | 旧 ES 编译消费边和 reactor 事实 | 高 |
| 默认启动脚本、GatewayRouter、auth 首页 | caller/runtime baseline | 脚本/Java/HTML | 当前 Git 工作区 | 是 | 默认启动、路由和可见入口 | 高 |
| 旧 Agent 调用方、Config Server employee 配置 | P0 pending evidence | Java/配置 | 当前 Git 工作区 | 是，限定读取 | 判定 P0 前允许残留和责任归属 | 高 |
| 当前 CI 工作流 | downstream test | CI | 当前 Git 工作区 | 是 | 核对是否显式选择 employee | 高 |

### 2.1 范围内

- 从根 reactor、默认启动/Eureka、Gateway 和默认首页退出 `employee-service`。
- 保留 `employee-service` 业务身份、源码和外部数据不变。
- 区分 employee 出站旧 ES 编译边与 P0 待处理入站旧 Agent 调用引用。
- `ExternalConsumerResolutionRef`、验证器、错误、审计、恢复和 P0 消费门禁。

### 2.2 范围外

- 修改 `employee-service` 内文件，或将其改名为 alpha。
- 修改旧 Agent 调用方、employee 专属 Config Server 配置、Provider/Generation 资产。
- 实际修改 POM、脚本、Gateway、首页、测试、证据或 Git 历史。
- 生产停机、外部调用方迁移、业务替代和数据变更。

### 2.3 基线优先级与冲突

采用“用户选定方案 B → 00 总览 → 02 blocker/Preflight → 02A → 当前实现事实”的顺序。未发现需要修改 L0/L1 或 02 的冲突；初稿内部关于“所有调用引用消失”的要求与 02 P0 所有权冲突，已在 02A 本地按 blocker 的真实边界修复。

## 3. 执行阻塞

未发现影响本次核心结论的执行阻塞。仓库外调用方和生产部署未获准核对，但它们只限制生产结论，不妨碍评审仓库默认基线的方案 B 设计。

## 4. 发现汇总

### 4.1 最终未关闭发现

| 严重级别 | 数量 | 对评审门禁的影响 |
|---|---:|---|
| S0 | 0 | 第 1 轮不可达关闭条件已修复 |
| S1 | 0 | 第 1 轮两个严重问题已修复 |
| S2 | 0 | 第 1 轮一个追踪问题已修复 |
| S3 | 0 | 无 |

### 4.2 评审循环历史

| 轮次 | S0 | S1 | S2 | S3 | 结果 |
|---:|---:|---:|---:|---:|---|
| 1 | 1 | 2 | 1 | 0 | 冻结四项发现后修订 |
| 2 | 0 | 0 | 0 | 0 | 全文、引用分类、调用方、恢复和追踪复评无设计问题；生成报告后继续治理一致性核对 |
| 3 | 0 | 0 | 1 | 0 | 发现正文仍称本专题/上位“尚未独立评审”，与已生成报告冲突；冻结后轮间修订 |
| 4 | 0 | 0 | 0 | 0 | 状态、父子哈希、报告和实施门禁一致，停止循环 |

## 5. 评审发现与关闭证据

### 5.1 S0 阻断缺陷

| ID | 层级 | 位置 | 证据 | 期望与实际差异 | 触发场景和影响 | 修改建议/责任方 | 关闭证据及状态 |
|---|---|---|---|---|---|---|---|
| `REV-02A-003` | L2/可达性与责任 | 初稿第 7.2、8.2、8.4、11 节；当前 `agent-adapter-employee`/`agent-service` 和 `application-emp.yml` | 初稿禁止“活动模块→employee”，且 TEST-006 要求 employee 仅在自身目录以 `evidence_only` 出现；但 02A 又禁止修改 P0 所有的旧 Agent 调用方，而 P0 必须在 blocker 关闭后才可开始 | 关闭条件应只消除 employee→旧 `es-query-api` 的活动 Maven 边；实际要求先消除 P0 才能处理的入站引用，形成循环前置 | PostApply 无法在合法范围内通过，`ExternalConsumerResolutionRef` 永远不可生成；放宽扫描又会产生假阳性证据 | 将旧 Agent 入站引用分类为 `p0_pending_legacy_caller`、专属配置分类为 `inactive_business_configuration`，限期由 P0 消除并阻塞生产而非本地 P0；02A 维护者 | 第 7.2、8.2、8.4、9.8、10～12 节已修复，未知/多重分类仍失败；第 2 轮复评确认关闭状态可达，已关闭 |

### 5.2 S1 严重问题

| ID | 层级 | 位置 | 证据 | 期望与实际差异 | 触发场景和影响 | 修改建议/责任方 | 关闭证据及状态 |
|---|---|---|---|---|---|---|---|
| `REV-02A-001` | L2/治理 | 初稿第 4.1 节 | 00、02 经前序评审修订后，02A 仍绑定旧 SHA；其规则明确哈希变化须停止 | 应绑定最新直接上位；实际评审/关闭证据基线已失效 | 方案 B 实施或 P0 消费时会引用错误设计版本 | 更新两个上位哈希并全量复评；02A 维护者 | 已绑定 00 `43A6028...`、02 `5B477D62...`，已关闭 |
| `REV-02A-004` | L2/调用方兼容 | 初稿第 3.3、6.1、8.3、10～11 节；`auth-service/.../home.html` 第 30～32、172～180 行 | 初稿只删 Gateway 路由，却遗漏默认首页仍展示 `Employee ES`、`Employee workflow` 并跳转到将返回 404 的页面 | 能力退出应同时移除默认可见入口；实际把受控退出表现成用户可点击的失效功能 | 默认用户仍被引导到 404，能力状态和 UI 契约不一致，回归测试也无法发现 | 将首页两个按钮/函数纳入最小修改、测试、验证和恢复顺序；UI/Gateway 责任人 | 新增 `DR-016`、`IMPL-013`，TEST-004/VAL-005 和恢复链已覆盖，已关闭 |

### 5.3 S2 一般问题

| ID | 层级 | 位置 | 证据 | 期望与实际差异 | 触发场景和影响 | 修改建议/责任方 | 关闭证据及状态 |
|---|---|---|---|---|---|---|---|
| `REV-02A-002` | L2/追踪 | 初稿第 4.2～4.3 节 | 本地 `CON-*` 只按章节转述，且 `VAL-011`、`VAL-012` 未进入端到端矩阵 | 应以“文档限定 ID”指向 00/02，并覆盖 Git/提交前置验证 | 重号 `REQ/DR` 容易被工具误匹配，提交门禁与关闭证据难以追溯 | 增加上位来源、补齐 VAL 映射并保持本地 ID 稳定；02A 维护者 | 第 4.2～4.3 节已修订，本地重要 `DR→IMPL/TEST/VAL` 无孤立项，已关闭 |
| `REV-02A-005` | L2/治理 | 文档信息、第 12、14 节 | 本专题和直接上位报告均已形成，但正文仍称“尚未/可以进入独立评审”，且没有把评审通过与实施授权分开 | 应记录两份文档已评审但仍为 `Draft`、未授权、blocker 未关闭；实际状态陈述相互冲突 | 实施申请人可能重复评审、跳过影响接受，或误把报告当成代码授权 | 回写两份报告入口/结论，拆分评审和实施批准检查项，更新风险但不改变 blocker；02A 维护者 | 文档信息、第 12、14 节已同步，最终状态和父子哈希一致，已关闭 |

### 5.4 S3 建议优化

未记录不影响当前门禁的可选优化。

## 6. L2 详细设计评审结果

| 检查域 | 结论 | 证据/发现 ID |
|---|---|---|
| 功能完整性 | 通过 | 第 3、8、14 节覆盖退出、可见结果、关闭和恢复 |
| 职责、内聚和耦合 | 通过 | 第 5、7 节；循环前置与 P0 责任问题已关闭 |
| 契约和跨语言一致性 | 通过 | 第 8.3、9.1、9.8 节明确 404、无默认实例和证据 schema |
| 数据、状态、并发和一致性 | 通过 | 第 8.1、8.4、9.2～9.3 节定义单写者、原子报告和 P0 前后恢复资格 |
| 安全、可观测和运维 | 通过 | 第 8.4、9.4、9.7、12 节；外部数据不变、失败码和风险边界明确 |
| 实现落点和最小变更 | 通过 | 第 6.3、10 节；只修改四个既有文件并新增必要脚本/证据/测试 |
| 测试和验证 | 通过 | 第 11 节；活动边、分类、UI/Gateway、完整性、恢复和提交前置均可失败 |

## 7. 跨层追踪结果

| 上游 ID/位置 | 下游 ID/位置 | 映射状态 | 缺陷类型 | 责任方 | 发现 ID |
|---|---|---|---|---|---|
| 02 `REQ-004`、`DR-003`、第 5.2 节 | 02A `CON-001`、`DR-004`～`DR-010` | 已覆盖 | 无 | 02A 维护者 | 第 5.3 节问题已关闭 |
| 02 `DR-019` | 02A `DR-012`～`DR-015` 和报告 schema | 已覆盖 | 无 | 02A/02 验证责任人 | 无 |
| 02 P0 旧项目隔离责任 | 02A `p0_pending_legacy_caller` 限期分类 | 已覆盖 | 无越权 | 02 P0 责任人 | 第 5.1 节问题已关闭 |
| 默认 UI/Gateway 调用关系 | 02A `DR-007`、`DR-016` | 已覆盖 | 无 | UI/Gateway 责任人 | 第 5.2 节第二项已关闭 |

## 8. 实现就绪判断

- 当前门禁：方案B设计评审结论保持`ready_with_conditions`；方案B、关闭证据、02 Preflight、P0隔离和代码评审均已完成，目标仍未生产生效。
- 允许的下一步：将固定关闭证据和`P0_VERIFIED`报告作为后续专题的只读前置证据。
- 暂不允许的动作：宣称员工能力已替代、生产已下线，或把P0完成解释为其他专题/生产授权。
- 进入下一步前必须完成：P0前置已完成；生产变更和后续专题继续受各自独立门禁约束。
- 后续是否需要代码对照评审：方案B和P0代码评审已完成；后续专题仍须独立评审。

## 9. 证据限制与未验证项

| 项目 | 原因 | 对结论的影响 | 建议验证方式 |
|---|---|---|---|
| 仓库外/生产调用方 | 未在本次授权范围 | 阻塞生产下线结论，不阻塞仓库默认基线设计 | 生产变更前做调用方、消息积压和部署清单评审 |
| 方案B代码和测试 | 已授权、实施并验证 | `DB-02-001`保持关闭 | 保留固定报告和结果清单 |
| 干净新HEAD与P0 | 原子联动、Preflight、P0实施和推送均已完成 | 不再阻塞P0 | 当前完成提交为`772a2c24811674a58766b205bd9e4e57c34e570f` |
| P0旧Agent引用 | 已由02 P0统一隔离并经活动边界检查 | 不再阻塞P0；仍不形成生产功能替代结论 | 后续专题和生产流程独立验证 |

## 10. 推荐处理顺序

| 顺序 | 发现/阻塞 ID | 目标文件和责任方 | 所需授权 | 建议处理技能 |
|---:|---|---|---|---|
| 1 | 生产影响核验 | 仓库外调用方、部署和消息积压责任人 | 独立生产评审与授权 | 生产变更流程 |
| 2 | 后续专题设计 | 01、03及其他专题维护者 | 各专题独立设计授权 | `detailed-design-document` |
| 3 | 后续专题实施 | 对应工程和证据责任人 | 各专题独立实施授权 | `implement-from-detailed-design` |

## 11. 复审记录

| 轮次 | 基线版本/日期 | 已修复 | 部分修复 | 未修复 | 新增问题 | 结论 |
|---:|---|---|---|---|---|---|
| 1 | v1.0 / 2026-07-20 | 0 | 0 | 第 5.1 一项 S0、第 5.2 两项 S1、第 5.3 第一项 S2 | 0 | 关闭条件不可达，进入轮间授权修订 |
| 2 | v1.0 / 2026-07-20 | 首轮四项发现 | 0 | 0 | 0 | 设计内容无问题，继续报告一致性核对 |
| 3 | v1.0 / 2026-07-20 | 0 | 0 | 第 5.3 第二项 S2 | 0 | 进入轮间治理修订 |
| 4 | v1.0 / 2026-07-20 | 第 5.3 第二项 S2 | 0 | 0 | 0 | 有条件通过；停止循环 |
| 状态同步 | v1.0 / 2026-07-21 | 方案B、固定关闭报告、上位P0、代码评审、提交推送和远端CI已完成 | 0 | 0 | 0 | 设计评审结论不变；同步目标/上位状态、当前HEAD与SHA-256 |

## 12. 最终结论

02A v1.0 的独立设计评审在第 4/5 轮收敛，结论保持 `ready_with_conditions`。后续状态为：方案 B、固定关闭证据及上位 02 P0 验证均已完成，`DB-02-001` 保持关闭；目标仍未生产生效。
