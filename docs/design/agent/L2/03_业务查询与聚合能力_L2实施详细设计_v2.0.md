# 业务查询与聚合能力 L2 实施详细设计 v2.0

## 1. 文档信息与范围

| 项目 | 内容 |
|---|---|
| 文档标识 | `AGENT-L2-03` |
| 文档层级 | L2 详细设计 |
| 文档状态 | In Review |
| 内部结论 | Ready for Implementation Review with Conditions（内部评审结论；不代表已批准实施） |
| 当前版本 | v2.0 |
| 适用基线 | `AGENT-L0-001`、`AGENT-APP-L1-001` v2.0；现有业务端点仅为勘察事实，不是目标合同 |
| 维护责任角色 | Agent 业务查询/聚合实现负责人（具体人员由项目治理指定） |
| 是否可作为实现依据 | 当前仅可进入实现评审；专用上游合同、请求绑定授权投影和单租户边界获批后，才可实施 Agent 侧及真实域接入 |
| 上位约束 | AD-03、AD-04、AD-06、AD-09、AD-10、AD-14；APP-DEC-03、APP-DEC-04、APP-DEC-05、APP-DEC-08 |
| 范围 | QUERY/AGGREGATE 计划、白名单、上游映射、结果正确性和安全投影 |
| 非目标 | 业务写入、跨域 join、任意 SQL/DSL、模型计算业务结果 |

## 2. 修改历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v2.0 | 2026-07-22 | 将 QUERY/AGGREGATE 收敛为显式能力合同和域 Adapter，不继承旧插件/元数据平台。 |
| v2.0-r1 | 2026-07-22 | 补齐治理/上位约束，核实业务端点仅为勘察输入并统一章节编号。 |
| v2.0-r2 | 2026-07-22 | 分离字段用途授权，冻结业务版本/时间/数值语义并暂禁无单位金额指标。 |
| v2.0-r3 | 2026-07-22 | 增加服务身份+用户范围绑定及上游低基数抑制门禁。 |
| v2.0-r4 | 2026-07-22 | 声明 CON/DR，补齐严格合同、推断、时区/精度测试和验证命令。 |
| v2.0-r5 | 2026-07-22 | 同步五轮治理口径并明确上游合同条件。 |
| v2.0-r6 | 2026-07-22 | 展开 Employee/Transaction Java 严格合同及 Python Validator/Client/节点的完整文件、签名、模型和枚举。 |
| v2.0-r7 | 2026-07-22 | 按当前 Employee 实体/Mapper 基线，将首批字段收敛为 `position/workBaseSi`，移除不存在的 department/status/hireDate。 |
| v2.0-r8 | 2026-07-23 | 完成本次串行五轮评审：增加请求绑定 Business JWS、单租户边界、完整 DTO/错误/配置和 Servlet/WebFlux 独立安全链，展开原子追踪。 |

## 3. 设计目标与范围

目标是确定性执行两个只读业务能力；范围外如上表。最小必要抽象是两个 Validator、共享元数据类型和每域一个 Adapter，不建立动态插件或通用查询语言。

## 4. 当前实现基线、关联资源与责任边界

新 Agent 能力代码不存在；`employee-service`与`mq-procedure-service`是关联上游。当前`GET /employees`返回过宽业务实体、`GET /employees/count`不支持受控过滤，`POST /txn/aggregate`返回宽松 Map；这些只证明路径存在，不满足目标严格 DTO/错误/授权合同。当前 Employee/Transaction 模型与 Mapper 中均未发现 tenant 行级字段，因此首版只能在部署配置锁定的单一 tenant 下使用`SINGLE_TENANT_ALL`资源范围；任何多租户或资源集合范围必须失败关闭，不能凭请求中的 tenant 字符串伪造过滤能力。业务服务唯一负责数据与计算，Agent 只负责计划校验/映射/投影，不负责数据库。首个 PoC 必须先冻结专用只读合同；若需修改上游，按已授权的关联文档同步范围处理并单独验证调用方兼容性。

## 5. 模块职责、依赖方向与调用边界

`application -> Query/AggregateCapability -> Validator -> BusinessCapabilityAdapter -> upstream`。禁止 Adapter 反向依赖规划候选，禁止能力绕过权限/Validator，禁止模型读取结果。稳定 canonical 合同降低域服务耦合并保持能力内聚。

## 6. 需求

| 编号 | 可验收要求 |
|---|---|
| REQ-03-01 | 模型只能从获准域、字段、操作符、函数、排序和分页中形成计划。 |
| REQ-03-02 | LangGraph 确定性能力节点将已校验计划映射为 Java 上游合同，不透传模型 JSON。 |
| REQ-03-03 | QUERY/AGGREGATE 结果由业务服务计算，模型不读取或改写原始结果。 |
| REQ-03-04 | 字段权限在计划和返回两处校验，禁止通过排序/分组/错误旁路推断隐藏字段。 |
| REQ-03-05 | 每个域先具备真实合同测试，合同不足时失败而非本地猜测。 |

### 6.1 上位约束与设计规则

| 编号 | 来源/规则 | 责任主体 | 可观察结果 |
|---|---|---|---|
| CON-03-01 | AD-03/10：严格 canonical 计划和封闭上游合同 | validators/metadata | 非法计划在 Client 前终止，未知字段拒绝 |
| CON-03-02 | AD-06/14：业务服务拥有数据/计算，服务身份与用户范围绑定 | domain Clients/upstream | Agent 不直连数据库、不调用宽实体/Map 合同 |
| CON-03-03 | AD-04：用途分权、结果重验和推断控制 | validators/result projector | 隐藏字段、低基数桶和错误细节不外泄 |
| DR-03-01 | QUERY/AGGREGATE 分别校验并产出不可变 canonical DTO | Query/Aggregate Validator | 上游只接收已校验字段和预算 |
| DR-03-02 | 每域 Adapter 固定映射严格 DTO、认证、时间/数值语义 | BusinessCapabilityClient | Python/Java 夹具逐字段一致，不接宽端点 |
| DR-03-03 | 字段用途分权、上游低基数抑制、返回前安全投影 | policy/upstream/result projector | 推断泄漏负向集为 0，数值不经模型/浮点改写 |

## 7. 能力元数据

首阶段元数据来自受版本控制的配置并在启动时严格校验，不建设动态注册中心：

```yaml
domains:
  EMPLOYEE:
    queryFields:
      position: {type: STRING, operators: [EQ, IN]}
      workBaseSi: {type: STRING, operators: [EQ, IN]}
    displayFields: [position, workBaseSi]
    sortFields: [position]
    groupFields: [position, workBaseSi]
    metrics: [COUNT]
  TRANSACTION:
    queryFields: {}
    displayFields: []
    sortFields: []
    groupFields: []
    metrics: [COUNT]
```

字段名是 Agent canonical 名称，上游映射由域 Adapter 明确维护。权限策略可进一步缩小元数据范围。类型/枚举/别名冲突、未知映射或空 Adapter 在启动时失败。

`SUM_AMOUNT/AVG/MIN/MAX`等金额指标只有在上游合同明确金额单位/币种、跨币种处理、精度和舍入责任后才能加入元数据；当前 Transaction 模型未提供币种权威，首个基线不得启用这些指标或假定默认币种。

启用任一域的 AGGREGATE 还必须在元数据中声明`minGroupSize`，并由对应上游合同在返回前执行低基数抑制；缺少该字段、上游不支持或返回任一低于阈值的桶时，该域 AGGREGATE 失败关闭。Agent 不在本地合并小桶，因为这不能替代业务数据所有者的推断控制。

## 8. QUERY 合同

模型计划 payload：

```json
{
  "filters":[{"field":"workBaseSi","operator":"EQ","value":"SHANGHAI"}],
  "select":["position","workBaseSi"],
  "sorts":[{"field":"position","direction":"ASC"}],
  "page":{"number":0,"size":20}
}
```

规则：

- filters 最多 10 个，首阶段只支持 AND；不支持任意嵌套逻辑。
- operator 仅 `EQ|IN|GT|GTE|LT|LTE|CONTAINS`，且必须被字段声明；IN 最多 50 项。
- 值先按字段类型严格解析，再做枚举/长度/日期范围校验；不接受脚本、表达式、SQL 片段。
- select 必须非空且全在 `displayFields ∩ effectiveAuthorization`；最多 20 项。
- sorts 最多 2 个且全在获准 `sortFields`；稳定结果必须由 Adapter 补充上游唯一键排序，但该键不因此可显示。
- page.number 0..100，size 1..100；更大范围返回限制错误，不自动全量导出。

输出 `QueryResult(items, page, total?, observedAt, sourceVersion?)`；`observedAt/sourceVersion`只能透传上游提供且有合同定义的观测/快照事实，Agent 不得用本地时钟伪造数据版本。Adapter 返回的额外字段在结果投影时删除。若上游不提供可靠 total 或 sourceVersion，则对应字段为空，不由 Agent 猜测。

## 9. AGGREGATE 合同

模型计划 payload：

```json
{
  "filters":[],
  "groupBy":["position"],
  "metrics":[{"function":"COUNT","field":null,"alias":"employeeCount"}],
  "orderBy":[{"metric":"employeeCount","direction":"DESC"}],
  "limit":20
}
```

规则：

- filters 与 QUERY 相同；groupBy 最多 2 个且属于获准 groupFields。
- function 只允许 `COUNT|SUM|AVG|MIN|MAX` 与元数据声明的业务指标；除 COUNT 外 field 必填且类型匹配。
- alias 由确定性节点生成或按严格标识符校验，不由模型注入表达式。
- orderBy 只能引用 groupBy 或已声明 metric alias；它与 QUERY `sorts` 是不同合同。
- limit 1..100；首阶段不支持 having、窗口函数、百分位或跨域聚合。

输出 `AggregateResult(columns, rows, observedAt, sourceVersion?)`。数值由上游计算并以明确 decimal/integer 类型返回；金额指标还必须携带合同定义的单位/币种，禁止二进制浮点重算。Agent 不经模型重算、舍入、币种合并或自然语言改写，也不伪造 sourceVersion。

## 10. 详细功能与核心处理流程：校验与映射

```text
PlanCandidate
 -> capability/domain authorization
 -> metadata lookup
 -> field/operator/function/value validation
 -> resource budget validation
 -> canonical ValidatedQuery/ValidatedAggregate
 -> domain adapter mapping
 -> upstream client
 -> response contract validation
 -> result security projection
```

每个域实现一个 Python `BusinessCapabilityClient`，暴露`query`和/或`aggregate`；不建立可动态加载插件。Client 只映射 canonical Pydantic DTO，并调用 02 的确定性 projector 生成`typ=agent-business-authz+jwt`短时`authorizationProjection`；投影对除自身外的完整请求、deadline、domain/capability、subject/tenant、字段用途、`resourceScopeMode`、证据版本和低基数阈值精确绑定。业务上游必须独立验证 Agent HTTP 服务 token 与该 JWS 的签名/typ/aud/sub/jti/digest/exp，并要求 tenant 等于本服务启动配置、`resourceScopeMode=SINGLE_TENANT_ALL`；任一失败在数据库调用前拒绝。JWS 不代替服务认证，固定 query/aggregate 图节点是唯一调用方。

## 11. 接口与契约设计：初始上游落点

| 域 | 当前勘察输入 | 实施要求 |
|---|---|---|
| EMPLOYEE | `employee-service` 的 `/employees`、`/employees/count` 等控制器 | 冻结专用严格只读合同；验证 Agent 服务 scope、请求绑定 JWS 和部署锁定的单 tenant；无行级 tenant 基线时禁止宣称多租户过滤 |
| TRANSACTION | `mq-procedure-service` 的 `/txn/query`、`/txn/search`、`/txn/aggregate` | 只勘察`/txn/search|aggregate`；冻结严格 DTO、请求绑定 JWS、单 tenant、低基数、错误和服务鉴权语义，禁止调用宽`/txn/query` |

这些路径不是最终合同承诺。若需要新增/修改公共 DTO 或接口，必须单独取得上游文档/代码变更授权并验证现有调用方；Agent L2 不擅自修改。

两个目标合同统一使用以下 HTTP/错误语义：400 `AGENT_BUSINESS_INVALID_REQUEST`，401 `AGENT_BUSINESS_UNAUTHORIZED`，403 `AGENT_BUSINESS_FORBIDDEN`，408 `AGENT_BUSINESS_DEADLINE_EXCEEDED`，503 `AGENT_BUSINESS_UNAVAILABLE`，500 `AGENT_BUSINESS_INTERNAL_ERROR`。错误响应只含`requestId/code/message/diagnosticId`，message 是服务端通用模板，不暴露字段存在性、SQL、JWS 规则、tenant 或资源 ID。Python Client 对未知 code、未知字段、响应 requestId 不匹配一律按合同失败关闭；业务 400 表示实现/合同漂移，不能原样归因给最终调用方。

`observedAt`定义为业务服务完成本次数据库读取的 UTC 时刻，只表示读取完成时间，不表示数据库快照版本；`sourceVersion`在业务数据源尚无可证明版本时必须为 null。Transaction `transDate`按 IMPL-03-J12 的`databaseZone`解释后序列化为 UTC Instant；配置缺失时 Transaction 能力不得启用。

## 12. 权限与推断控制

- filter、select、sort、groupBy、metric field 必须分别与`filterFields/displayFields/sortFields/groupFields/metricFields`用途权限求交；“可过滤”不等于“可显示/排序/分组/聚合”，禁止使用一个通用 fields 集合替代分权。
- 对敏感字段禁止通过排序位置、分组桶、count 差分或详细错误泄漏。低基数阈值由域策略声明并由业务上游在返回前执行；Agent 发现违反阈值的桶时整体返回通用`FORBIDDEN`，不本地合并、不暴露哪个桶触发。
- 结果投影使用当前 `EffectiveAuthorization`；上游返回未知字段、错误 tenant 或无法解析版本时整体失败关闭。
- 首版业务数据只支持`SINGLE_TENANT_ALL`：授权投影 tenant 必须与 Employee/Transaction 服务各自的`singleTenantRef`配置相等；若授权资源事实为`RESOURCE_SET`、配置缺失或未来数据出现 tenant 行级隔离需求，域能力保持关闭并先修订合同/数据设计。
- 上游错误只映射为稳定码，不返回 SQL、字段存在性或资源标识细节。

## 13. 数据生命周期、超时、重试与事务边界

Client 取得剩余 deadline 并设置单次 HTTP 超时。QUERY/AGGREGATE 是只读能力，只有连接建立前失败或上游明确声明幂等时最多重试一次；超时/断连导致结果未知时直接失败。Agent 不对上游分页做隐式循环，不自动扩大 page/limit。

错误分类与调用方可见语义：非法计划为`MODEL_OUTPUT_INVALID`，字段/推断限制为`FORBIDDEN`，上游超时为`TIMEOUT`，稳定 5xx/合同解析失败为`UPSTREAM_UNAVAILABLE`；外部错误不得暴露字段存在性、SQL 或上游正文。权限与安全审计设计继承 02，能力节点只记录域、计划摘要、条数、耗时和安全码。

## 14. 实施落点

路径均相对仓库根目录 `D:/codex`。现有 `/employees`、`/txn/search`、`/txn/aggregate`及其宽 DTO 只标记为`已存在`勘察输入，不作为目标 Client 合同。新增公共内部端点/DTO 在编码前仍需上游代码变更授权和调用方兼容检查。

### 14.1 Java：Employee 严格只读合同

| 编号 | 状态 | 完整路径/类 | 入口与主要方法签名 | 责任 |
|---|---|---|---|---|
| IMPL-03-J01 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeQueryController.java`；`com.dylan.employee.agent.query.AgentEmployeeQueryController` | `public EmployeeQueryResponse query(Authentication authentication, EmployeeQueryRequest request)`；`public EmployeeAggregateResponse aggregate(Authentication authentication, EmployeeAggregateRequest request)` | 暴露 `POST /internal/agent/employees/query|aggregate`；先验证 Agent 服务 scope 与请求主体范围，再调用 Service。 |
| IMPL-03-J02 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeQueryService.java` | `public EmployeeQueryResponse query(AuthorizedEmployeeScope scope, EmployeeQueryRequest request)`；`public EmployeeAggregateResponse aggregate(AuthorizedEmployeeScope scope, EmployeeAggregateRequest request)`；内部`EmployeeQueryCommand validateAndCompile(AuthorizedEmployeeScope scope, EmployeeQueryRequest request)`、`EmployeeAggregateCommand validateAndCompile(AuthorizedEmployeeScope scope, EmployeeAggregateRequest request)` | 只接受 J04 已验证 scope，对固定字段/操作符/分页/低基数阈值执行域侧二次校验，调用专用 Mapper并组装严格响应。 |
| IMPL-03-J03 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeQueryMapper.java`、`employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeSqlProvider.java` | `List<EmployeeQueryItem> query(EmployeeQueryCommand command)`；`long count(EmployeeQueryCommand command)`；`List<EmployeeAggregateRow> aggregate(EmployeeAggregateCommand command)`；Provider：`String query(Map<String,Object> parameters)`、`String count(Map<String,Object> parameters)`、`String aggregate(Map<String,Object> parameters)` | 仅从枚举映射构造 SQL；不接受调用方列名、SQL、表达式或任意 order by。 |
| IMPL-03-J04 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeAuthorizationVerifier.java`、`EmployeeRequestBinding.java`、`AuthorizedEmployeeScope.java`、`EmployeeBusinessAuthorizationClaims.java` | `public AuthorizedEmployeeScope verify(Authentication authentication, EmployeeRequestBinding binding, String authorizationProjection, String requiredScope, Instant now)`；内部`Jwt extractServiceJwt(Authentication authentication)`、`void verifyRequestDigest(EmployeeRequestBinding binding, EmployeeBusinessAuthorizationClaims claims)` | 在 DB 前验证服务 token 与 Business JWS 的签名/typ/aud/sub/jti/digest/exp/domain/capability/tenant/resourceScopeMode；返回已验证 scope，不信任请求头或自填 subject。 |
| IMPL-03-J05 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/api/`；每个下列类型为该目录下同名 `.java` 文件 | `EmployeeQueryRequest(String requestId, Instant deadlineAt, String authorizationProjection, List<EmployeeFilter> filters, Set<EmployeeField> select, List<EmployeeSort> sorts, PageRequest page)`；`EmployeeQueryResponse(List<EmployeeQueryItem> items, PageMetadata page, Long total, Instant observedAt, String sourceVersion)`；`EmployeeAggregateRequest(String requestId, Instant deadlineAt, String authorizationProjection, List<EmployeeFilter> filters, List<EmployeeField> groupBy, List<EmployeeMetric> metrics, List<EmployeeAggregateOrder> orderBy, int limit)`；`EmployeeAggregateResponse(List<EmployeeAggregateRow> rows, Instant observedAt, String sourceVersion)`；`EmployeeQueryItem(String position, String workBaseSi)`；`EmployeeAggregateRow(String position, String workBaseSi, long count)`；`EmployeeFilter(EmployeeField field, FilterOperator operator, List<String> values)`；`EmployeeSort(EmployeeField field, SortDirection direction)`；`PageRequest(int number, int size)`；`PageMetadata(int number, int size, boolean hasNext)`；`EmployeeMetric(AggregateFunction function, EmployeeField field, String alias)`；`EmployeeAggregateOrder(String target, SortDirection direction)`；枚举：`EmployeeField{POSITION,WORK_BASE_SI}`、`FilterOperator{EQ,IN}`、`SortDirection{ASC,DESC}`、`AggregateFunction{COUNT}` | `authorizationProjection`是唯一用户范围证明且不参与自身 digest；这些业务字段已在当前`Employee`实体/Mapper核实。严格 Jackson DTO 未知字段/枚举拒绝；未请求字段不序列化，聚合行只允许获准 group 字段与`count`。 |
| IMPL-03-J11 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeQueryProperties.java` | `@ConfigurationProperties("employee.agent-query") record AgentEmployeeQueryProperties(boolean queryEnabled, boolean aggregateEnabled, String singleTenantRef, int minGroupSize, String authorizationAudience)` | 两能力默认关闭；任一启用时 tenant/audience 非空、`minGroupSize>=1`，并要求 claims tenant 完全相等、scope mode 为`SINGLE_TENANT_ALL`；配置非法启动失败。 |
| IMPL-03-J13 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/api/AgentEmployeeErrorCode.java`、`AgentEmployeeErrorResponse.java`、`employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeExceptionHandler.java` | `AgentEmployeeErrorCode{AGENT_BUSINESS_INVALID_REQUEST,AGENT_BUSINESS_UNAUTHORIZED,AGENT_BUSINESS_FORBIDDEN,AGENT_BUSINESS_DEADLINE_EXCEEDED,AGENT_BUSINESS_UNAVAILABLE,AGENT_BUSINESS_INTERNAL_ERROR}`；`AgentEmployeeErrorResponse(String requestId, String code, String message, String diagnosticId)`；`ResponseEntity<AgentEmployeeErrorResponse> handle(AgentEmployeeException exception)` | 统一安全 HTTP 映射；未知异常只返回 INTERNAL，禁止回显 SQL/JWS/字段/tenant 细节。 |

现有 `employee-service/src/main/java/com/dylan/employee/controller/EmployeeController.java` 的 `List<Employee> page(int page, int size)`/`long count()`、`employee-service/src/main/java/com/dylan/employee/service/EmployeeService.java` 和 `employee-service/src/main/java/com/dylan/employee/security/CapabilityAccessGuard.java` 保持`已存在`但不是目标 Agent 合同；Python Client 禁止调用这些宽端点。

### 14.2 Java：Transaction 严格只读合同

| 编号 | 状态 | 完整路径/类 | 入口与主要方法签名 | 责任 |
|---|---|---|---|---|
| IMPL-03-J06 | 建议新增 | `transaction-api/src/main/java/com/dylan/transaction/api/agent/`；每个下列类型为该目录下同名 `.java` 文件 | `AgentTransactionQueryRequest(String requestId, Instant deadlineAt, String authorizationProjection, List<AgentTransactionFilter> filters, Set<TransactionField> select, List<AgentTransactionSort> sorts, PageRequest page)`；`AgentTransactionQueryResponse(List<AgentTransactionQueryItem> items, PageMetadata page, Long total, Instant observedAt, String sourceVersion)`；`AgentTransactionAggregateRequest(String requestId, Instant deadlineAt, String authorizationProjection, List<AgentTransactionFilter> filters, List<TransactionField> groupBy, List<AgentTransactionMetric> metrics, List<AgentTransactionAggregateOrder> orderBy, int limit)`；`AgentTransactionAggregateResponse(List<AgentTransactionAggregateRow> rows, Instant observedAt, String sourceVersion)`；`AgentTransactionQueryItem(String transId, String transType, Instant transDate)`；`AgentTransactionAggregateRow(String transType, Instant transDate, long count)`；`AgentTransactionFilter(TransactionField field, FilterOperator operator, List<String> values)`；`AgentTransactionSort(TransactionField field, SortDirection direction)`；`AgentTransactionMetric(AggregateFunction function, TransactionField field, String alias)`；`AgentTransactionAggregateOrder(String target, SortDirection direction)`；`PageRequest(int number, int size)`；`PageMetadata(int number, int size, boolean hasNext)`；枚举`TransactionField{TRANS_ID,TRANS_TYPE,TRANS_DATE}`、`FilterOperator{EQ,IN,GT,GTE,LT,LTE}`、`SortDirection{ASC,DESC}`、`AggregateFunction{COUNT}` | `transaction-api`只承载分布式依赖所需 DTO、枚举和值对象，不放 Controller、Service、Mapper、授权验证或业务规则；运行实现仍全部位于`mq-procedure-service`。当前 Transaction QUERY/groupBy 保持关闭，只启用全局 COUNT；不得声明金额字段或`SUM/AVG/MIN/MAX`。 |
| IMPL-03-J07 | 建议新增 | `mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionQueryController.java` | `public AgentTransactionQueryResponse query(Authentication authentication, AgentTransactionQueryRequest request)`；`public AgentTransactionAggregateResponse aggregate(Authentication authentication, AgentTransactionAggregateRequest request)` | 暴露 `POST /internal/agent/transactions/query|aggregate`；不复用返回 `Transaction`/`Map<String,Object>` 的宽入口。 |
| IMPL-03-J08 | 建议新增 | `mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionQueryService.java` | `public AgentTransactionQueryResponse query(AuthorizedTransactionScope scope, AgentTransactionQueryRequest request)`；`public AgentTransactionAggregateResponse aggregate(AuthorizedTransactionScope scope, AgentTransactionAggregateRequest request)`；内部`TransactionQueryCommand validateAndCompile(AuthorizedTransactionScope scope, AgentTransactionQueryRequest request)`、`TransactionAggregateCommand validateAndCompile(AuthorizedTransactionScope scope, AgentTransactionAggregateRequest request)` | 只接受 J10 已验证 scope；固定映射、UTC 时间语义、稳定排序和`max(configMinGroupSize, claimMinGroupSize)`抑制；未知或低基数结果整体失败关闭。 |
| IMPL-03-J09 | 建议新增 | `mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionQueryMapper.java`、`mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionSqlProvider.java` | `List<AgentTransactionQueryItem> query(TransactionQueryCommand command)`；`long count(TransactionQueryCommand command)`；`List<AgentTransactionAggregateRow> aggregate(TransactionAggregateCommand command)`；Provider：`String query(Map<String,Object> parameters)`、`String count(Map<String,Object> parameters)`、`String aggregate(Map<String,Object> parameters)` | 只使用 `TransactionField -> DB column`不可变映射，禁止原生 SQL/DSL/任意字段。 |
| IMPL-03-J10 | 建议新增 | `mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionAuthorizationVerifier.java`、`TransactionRequestBinding.java`、`AuthorizedTransactionScope.java`、`TransactionBusinessAuthorizationClaims.java` | `public AuthorizedTransactionScope verify(Authentication authentication, TransactionRequestBinding binding, String authorizationProjection, String requiredScope, Instant now)`；内部`Jwt extractServiceJwt(Authentication authentication)`、`void verifyRequestDigest(TransactionRequestBinding binding, TransactionBusinessAuthorizationClaims claims)` | 在 DB 前独立验证服务 token 与 Business JWS 的签名/typ/aud/sub/jti/digest/exp/domain/capability/tenant/resourceScopeMode；不把验证类放入`transaction-api`。 |
| IMPL-03-J12 | 建议新增 | `mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionQueryProperties.java` | `@ConfigurationProperties("transaction.agent-query") record AgentTransactionQueryProperties(boolean queryEnabled, boolean aggregateEnabled, boolean groupByEnabled, String singleTenantRef, int minGroupSize, String authorizationAudience, ZoneId databaseZone)` | 三开关默认关闭；首阶段只允许`aggregateEnabled=true`且`groupByEnabled=false`。任一启用时 tenant/audience/databaseZone 非空、`minGroupSize>=1`；`transDate`按`databaseZone`读取后转 UTC Instant，tenant/scope mode 不匹配时 DB 调用为 0。 |
| IMPL-03-J14 | 建议新增 | 共享错误模型：`transaction-api/src/main/java/com/dylan/transaction/api/agent/AgentTransactionErrorCode.java`、`AgentTransactionErrorResponse.java`；域处理：`mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionExceptionHandler.java` | 枚举值同第 11 节六类稳定码；`AgentTransactionErrorResponse(String requestId, String code, String message, String diagnosticId)`；`ResponseEntity<AgentTransactionErrorResponse> handle(AgentTransactionException exception)` | `transaction-api`只承载分布式共享错误 DTO/枚举，异常处理与业务映射仍在`mq-procedure-service`；未知异常安全收口。 |
| IMPL-03-J15 | 建议新增 | `employee-service/src/main/java/com/dylan/employee/agent/query/AgentEmployeeSecurityConfig.java` | `@Bean @Order(1) SecurityFilterChain agentEmployeeSecurityFilterChain(HttpSecurity http, @Qualifier("agentServiceJwtDecoder") JwtDecoder decoder)`仅匹配`/internal/agent/**`；`@Bean @Order(2) SecurityFilterChain employeeUserSecurityFilterChain(HttpSecurity http, JwtDecoder jwtDecoder)`保留其他端点用户认证；`@Bean("agentServiceJwtDecoder") JwtDecoder agentServiceJwtDecoder(...)`使用 02 的`AGENT_SERVICE_JWT` keyset | Employee 是 Servlet 应用；新增任一链会替代 common-security 默认链，因此必须同时显式保留普通用户链。内部链只接受独立服务 token，Controller 再校验 subject/scope。 |
| IMPL-03-J16 | 建议新增 | `mq-procedure-service/src/main/java/com/dylan/mqprocedureserver/agent/AgentTransactionSecurityConfig.java` | `@Bean @Order(1) SecurityWebFilterChain agentTransactionSecurityWebFilterChain(ServerHttpSecurity http, @Qualifier("agentServiceReactiveJwtDecoder") ReactiveJwtDecoder decoder)`仅匹配`/internal/agent/**`；`@Bean @Order(2) SecurityWebFilterChain transactionUserSecurityWebFilterChain(ServerHttpSecurity http, ReactiveJwtDecoder jwtDecoder)`保留其他 HTTP/WebSocket 行为；`@Bean("agentServiceReactiveJwtDecoder") ReactiveJwtDecoder agentServiceReactiveJwtDecoder(...)`使用 02 的独立 keyset | Transaction 是 WebFlux 应用，不得复制 Servlet 配置。两链均需回归既有 WebSocket 放行/自验边界；内部端点只接受 Agent 服务 token。 |

现有 `TransactionController.search(Authentication, TransactionSearchRequest) -> TransactionSearchResponse`、`aggregate(Authentication, AggregateRequest) -> Map<String,Object>` 及 `TransactionService` 标记为`已存在`勘察输入。由于其复用宽实体、宽 Map、页码和授权语义不满足本设计，目标 Python Client 不得回退调用。

### 14.3 Python 文件、类与函数

| 编号 | 状态 | 完整路径 | 类型/函数签名 | 责任 |
|---|---|---|---|---|
| IMPL-03-P01 | 建议新增 | `agent-service/agent_service/capabilities/query/validator.py` | `def validate_query(candidate: QueryPlanCandidate, authorization: EffectiveAuthorization, metadata: CapabilityMetadata) -> ValidatedQuery`；`def parse_filter_value(field: FieldDefinition, operator: FilterOperator, value: JsonValue) -> ScalarValue | tuple[ScalarValue, ...]` | 校验字段用途、类型、操作符、select/sort/page 和预算；非法计划在 Client 前终止。 |
| IMPL-03-P02 | 建议新增 | `agent-service/agent_service/capabilities/aggregate/validator.py` | `def validate_aggregate(candidate: AggregatePlanCandidate, authorization: EffectiveAuthorization, metadata: CapabilityMetadata) -> ValidatedAggregate`；`def validate_metric(metric: MetricCandidate, domain: DomainDefinition) -> ValidatedMetric` | 校验 group/metric/order/limit、金额单位门禁和 minGroupSize。 |
| IMPL-03-P03 | 建议新增 | `agent-service/agent_service/capabilities/metadata.py` | `def load_capability_metadata(path: Path) -> CapabilityMetadata`；`def validate_capability_metadata(metadata: CapabilityMetadata, clients: BusinessClientRegistry) -> None`；`def metadata_for(domain: BusinessDomain, authorization: EffectiveAuthorization) -> DomainDefinition` | 启动时冻结版本化元数据；未知域、类型、枚举、映射或缺 Client 失败。 |
| IMPL-03-P04 | 建议新增 | `agent-service/agent_service/clients/business.py` | `class BusinessCapabilityClient(Protocol): async def query(self, plan: ValidatedQuery, authorization: EffectiveAuthorization, deadline: Deadline) -> QueryResult`; `async def aggregate(self, plan: ValidatedAggregate, authorization: EffectiveAuthorization, deadline: Deadline) -> AggregateResult`；`def client_for(self, domain: BusinessDomain) -> BusinessCapabilityClient`；`def map_business_error(status: int, body: BusinessErrorResponseDto, request_id: UUID) -> AgentError` | 定义封闭 Port 和固定域注册表；未知错误码/字段或 requestId 不匹配失败关闭，不是动态插件系统。 |
| IMPL-03-P05 | 建议新增 | `agent-service/agent_service/clients/employee.py` | `class EmployeeCapabilityClient(BusinessCapabilityClient)`；`async def query(self, plan: ValidatedQuery, authorization: EffectiveAuthorization, deadline: Deadline) -> QueryResult`；`async def aggregate(self, plan: ValidatedAggregate, authorization: EffectiveAuthorization, deadline: Deadline) -> AggregateResult`；内部`def to_employee_query_request(plan: ValidatedQuery, authorization: EffectiveAuthorization, request_id: UUID, deadline_at: datetime, signer: JwsSigner) -> EmployeeQueryRequestDto`、`def to_employee_aggregate_request(plan: ValidatedAggregate, authorization: EffectiveAuthorization, request_id: UUID, deadline_at: datetime, signer: JwsSigner) -> EmployeeAggregateRequestDto` | 映射 IMPL-03-J05，先构造无投影请求再由 02 对完整摘要签发 Business JWS；装配服务 token，使用 0-based 分页并严格验证响应。 |
| IMPL-03-P06 | 建议新增 | `agent-service/agent_service/clients/transaction.py` | `class TransactionCapabilityClient(BusinessCapabilityClient)`；`async def query(self, plan: ValidatedQuery, authorization: EffectiveAuthorization, deadline: Deadline) -> QueryResult`；`async def aggregate(self, plan: ValidatedAggregate, authorization: EffectiveAuthorization, deadline: Deadline) -> AggregateResult`；内部`def to_transaction_query_request(plan: ValidatedQuery, authorization: EffectiveAuthorization, request_id: UUID, deadline_at: datetime, signer: JwsSigner) -> TransactionQueryRequestDto`、`def to_transaction_aggregate_request(plan: ValidatedAggregate, authorization: EffectiveAuthorization, request_id: UUID, deadline_at: datetime, signer: JwsSigner) -> TransactionAggregateRequestDto` | 映射 IMPL-03-J06并签发目标 audience 的 Business JWS；不得调用`/txn/query`或宽 Map aggregate。 |
| IMPL-03-P07 | 建议新增 | `agent-service/agent_service/graph/nodes/query.py`、`agent-service/agent_service/graph/nodes/aggregate.py` | `async def query_node(state: AgentState, runtime: Runtime[GraphContext]) -> QueryResultUpdate`；`async def aggregate_node(state: AgentState, runtime: Runtime[GraphContext]) -> AggregateResultUpdate` | 只接收 `ValidatedQuery/ValidatedAggregate`，选择固定域 Client，执行一次上游调用并拒绝迟到结果。 |
| IMPL-03-P08 | 建议新增 | `agent-service/agent_service/security/business_results.py` | `def project_query_result(result: QueryResult, plan: ValidatedQuery, authorization: EffectiveAuthorization) -> SafeQueryResult`；`def project_aggregate_result(result: AggregateResult, plan: ValidatedAggregate, authorization: EffectiveAuthorization) -> SafeAggregateResult`；`def enforce_min_group_size(result: AggregateResult, minimum: int) -> None` | 返回前重验字段、tenant、版本、条数、低基数和 Decimal 完整性；不经模型加工。 |

### 14.4 Python 模型、枚举与 Java DTO 镜像

| 编号 | 状态 | 完整路径 | 模型/枚举（主要字段或值） |
|---|---|---|---|
| IMPL-03-M01 | 建议新增 | `agent-service/agent_service/capabilities/common/models.py` | `BusinessDomain{EMPLOYEE,TRANSACTION}`、`FieldType{STRING,ENUM,DATE,INTEGER,DECIMAL}`、`FilterOperator{EQ,IN,GT,GTE,LT,LTE,CONTAINS}`、`SortDirection{ASC,DESC}`、`AggregateFunction{COUNT,SUM,AVG,MIN,MAX}`；`FieldDefinition`、`DomainDefinition`、`CapabilityMetadata`。枚举全集不等于域启用集。 |
| IMPL-03-M02 | 建议新增 | `agent-service/agent_service/capabilities/query/models.py` | `FilterCandidate(field, operator, value)`、`SortCandidate(field, direction)`、`Page(number, size)`、`QueryPlanCandidate(filters, select, sorts, page)`、`ValidatedFilter`、`ValidatedQuery(domain, filters, select, sorts, page)`、`QueryResult(items, page, total?, observed_at, source_version?)`、`SafeQueryResult`；严格/frozen。 |
| IMPL-03-M03 | 建议新增 | `agent-service/agent_service/capabilities/aggregate/models.py` | `MetricCandidate(function, field?, alias?)`、`AggregateOrderCandidate(target, direction)`、`AggregatePlanCandidate(filters, group_by, metrics, order_by, limit)`、`ValidatedMetric`、`ValidatedAggregate`、`AggregateColumn`、`AggregateRow`、`AggregateResult(columns, rows, observed_at, source_version?)`、`SafeAggregateResult`。数值使用 `int|Decimal`，禁止 float。 |
| IMPL-03-M04 | 建议新增 | `agent-service/agent_service/clients/business_models.py` | Employee/Transaction 请求/响应 Pydantic DTO 逐字段镜像 IMPL-03-J05/J06，包含`request_id/deadline_at/authorization_projection`且不再包含自填`AgentSubjectScopeDto`；`extra='forbid'`。Java`Instant/LocalDate/BigDecimal`分别映射 Python aware`datetime/date/Decimal`，Business JWS 字符串使用严格长度上限。 |

### 14.5 测试落点

| 编号 | 状态 | 完整路径 | 覆盖 |
|---|---|---|---|
| IMPL-03-T01 | 建议新增 | `agent-service/tests/capabilities/test_business_validators.py`、`agent-service/tests/capabilities/test_aggregate_fidelity.py`、`agent-service/tests/contract/test_business_adapter_mapping.py`、`agent-service/tests/contract/test_business_http.py`、`agent-service/tests/security/test_business_scope.py` | 非法计划零上游、字段用途、0/1 基分页、日期/Decimal、服务+用户范围、低基数、宽端点调用为 0。 |
| IMPL-03-T02 | 建议新增 | `employee-service/src/test/java/com/dylan/employee/agent/query/AgentEmployeeQueryContractTest.java`、`AgentEmployeeQuerySecurityTest.java`、`AgentEmployeeSecurityConfigTest.java` | DTO/错误严格性、SQL 白名单、Servlet 双安全链、服务 token + Business JWS、请求 digest、单 tenant/资源模式、开关默认关闭、稳定排序和小桶抑制。 |
| IMPL-03-T03 | 建议新增 | `mq-procedure-service/src/test/java/com/dylan/mqprocedureserver/agent/AgentTransactionQueryContractTest.java`、`AgentTransactionQuerySecurityTest.java`、`AgentTransactionSecurityConfigTest.java`；`transaction-api/src/test/java/com/dylan/transaction/api/agent/AgentTransactionDtoTest.java` | Python/Java 固定夹具、专项模型模块无业务依赖、WebFlux 双安全链/WebSocket 回归、服务 token + Business JWS、单 tenant、UTC、宽 Map 拒绝、全局 COUNT 和能力默认关闭。 |

## 15. 测试与验证设计

| 编号 | 层级/建议路径 | 夹具与动作 | 关键断言 | 失败信号 |
|---|---|---|---|---|
| TEST-03-01 | 建议新增：Unit `tests/capabilities/test_business_validators.py` | 字段类型/操作符、IN 上限、日期/数值/枚举边界 | 仅元数据允许的组合生成 canonical DTO | 非法值到达 Adapter |
| TEST-03-02 | 建议新增：Security `tests/security/test_business_scope.py` | 分别移除 filter/select/sort/group/metric 权限，伪造 tenant/资源范围和小桶 | 用户范围严格绑定；违规桶整体通用拒绝 | 隐藏字段/桶/资源被返回或错误泄漏 |
| TEST-03-03 | 建议新增：Contract `tests/contract/test_query_aggregate_schema.py` | 混用 sorts/orderBy、额外字段、宽实体/Map 响应 | 严格 Schema 拒绝所有跨合同/额外内容 | 宽响应被静默接收 |
| TEST-03-04 | 建议新增：Contract `tests/contract/test_business_adapter_mapping.py` | 固定 canonical↔Java DTO、0/1 基分页、日期/Decimal/空值夹具 | 映射逐字段、时区/精度一致 | 字段丢失、偏移或浮点改写 |
| TEST-03-05 | 建议新增：Integration `tests/integration/test_business_failures.py` | 未知字段、tenant 错误、5xx、超时、迟到响应 | 整体失败关闭，无迟到 State/输出 | 部分/迟到/跨 tenant 结果出现 |
| TEST-03-06 | 建议新增：Unit `tests/capabilities/test_aggregate_fidelity.py` | Decimal/COUNT、缺单位的 SUM、模型 spy | 数值逐值等于上游；缺单位指标禁用；模型 0 调用 | 重算/舍入/默认币种或模型调用 |
| TEST-03-07 | 建议新增：Contract integration `tests/contract/test_business_http.py` | EMPLOYEE 专用 QUERY、TRANSACTION strict aggregate 与真实 Spring Security；篡改/交叉使用 Business JWS，错 tenant/resourceScopeMode | 服务 scope + 请求绑定 JWS 均验证，宽端点不在 Client 路由，DB spy 在拒绝时为 0 | 只凭服务身份、投影跨请求复用或调用`/employees`/`/txn/query` |
| TEST-03-08 | 建议新增：Java Contract/Security（IMPL-03-T02/T03） | Servlet/WebFlux 双安全链、用户/服务 keyset 交叉、默认开关、单 tenant、JWS active/previous、坏 digest、低基数、稳定排序、UTC、错误异常 canary | 内部端点只接受服务链，普通端点/WebSocket 不回归；安全失败在 Mapper 前拒绝且错误无泄漏 | 用户 token 命中内部链、服务 token 命中普通链、Mapper 被调用、时间偏移或隐藏正文返回 |

| 验证编号 | 工作目录与命令 | 预期结果 | 当前状态 |
|---|---|---|---|
| VAL-03-01 | 建议在`agent-service`创建后执行：python -m pytest tests/capabilities/test_business_validators.py tests/contract/test_query_aggregate_schema.py | 非法计划上游调用为 0，严格 Schema 通过 | 未执行：项目尚不存在 |
| VAL-03-02 | 建议在`agent-service`创建后执行：python -m pytest tests/contract/test_business_adapter_mapping.py tests/contract/test_business_http.py tests/integration/test_business_failures.py | 结果/错误/认证/时区/精度与上游合同一致 | 未执行：专用上游合同尚未冻结 |
| VAL-03-03 | 建议在`agent-service`创建后执行：python -m pytest tests/security/test_business_scope.py tests/capabilities/test_aggregate_fidelity.py | 推断/跨 tenant 泄漏为 0，聚合数值不改写 | 未执行：项目尚不存在 |
| VAL-03-04 | 实现后在仓库根执行：`mvn -pl employee-service,transaction-api,mq-procedure-service -am -Dsurefire.failIfNoSpecifiedTests=false -Dtest=AgentEmployeeQueryContractTest,AgentEmployeeQuerySecurityTest,AgentEmployeeSecurityConfigTest,AgentTransactionDtoTest,AgentTransactionQueryContractTest,AgentTransactionQuerySecurityTest,AgentTransactionSecurityConfigTest test` | Servlet/WebFlux 双安全链、严格 DTO、JWS/单 tenant、SQL/时间/低基数和专项模型边界通过 | 未执行：目标签名与测试尚未实现 |

最小纵切优先选择 EMPLOYEE QUERY，但进入 PoC 前必须先获得专用严格合同；若合同核验未通过，则该纵切受阻，不能直接复用过宽实体或无过滤 count。随后再补 TRANSACTION AGGREGATE；每步必须保持安全规则完整，不能以临时宽 DTO/Map 换速度。

## 16. 端到端追踪矩阵

| 需求/约束 | 设计规则 | 实现 | 测试 | 验证 |
|---|---|---|---|---|
| REQ-03-01/REQ-03-02、CON-03-01 | DR-03-01 严格计划到 canonical DTO | IMPL-03-P01/IMPL-03-P02/IMPL-03-P03/IMPL-03-M01/IMPL-03-M02/IMPL-03-M03 | TEST-03-01/TEST-03-03 | VAL-03-01 非法计划上游调用为零 |
| REQ-03-03/REQ-03-05、CON-03-02 | DR-03-02 每域 Adapter 和真实合同 | IMPL-03-J01/IMPL-03-J02/IMPL-03-J03/IMPL-03-J04/IMPL-03-J05/IMPL-03-J06/IMPL-03-J07/IMPL-03-J08/IMPL-03-J09/IMPL-03-J10/IMPL-03-J11/IMPL-03-J12/IMPL-03-J13/IMPL-03-J14/IMPL-03-J15/IMPL-03-J16/IMPL-03-P04/IMPL-03-P05/IMPL-03-P06/IMPL-03-P07/IMPL-03-M04/IMPL-03-T02/IMPL-03-T03 | TEST-03-04/TEST-03-07/TEST-03-08 | VAL-03-02/VAL-03-04 结果、认证与上游一致 |
| REQ-03-04、CON-03-03 | DR-03-03 字段分权、请求绑定投影和结果安全 | IMPL-03-J04/IMPL-03-J10/IMPL-03-J11/IMPL-03-J12/IMPL-03-J13/IMPL-03-J14/IMPL-03-J15/IMPL-03-J16/IMPL-03-P08/IMPL-03-T01/IMPL-03-T02/IMPL-03-T03 | TEST-03-02/TEST-03-05/TEST-03-06/TEST-03-07/TEST-03-08 | VAL-03-03/VAL-03-04 推断泄漏负向集通过 |
| 全量覆盖索引 | REQ-03-01、REQ-03-02、REQ-03-03、REQ-03-04、REQ-03-05；CON-03-01、CON-03-02、CON-03-03 | DR-03-01、DR-03-02、DR-03-03；IMPL-03-J01、IMPL-03-J02、IMPL-03-J03、IMPL-03-J04、IMPL-03-J05、IMPL-03-J06、IMPL-03-J07、IMPL-03-J08、IMPL-03-J09、IMPL-03-J10、IMPL-03-J11、IMPL-03-J12、IMPL-03-J13、IMPL-03-J14、IMPL-03-J15、IMPL-03-J16、IMPL-03-P01、IMPL-03-P02、IMPL-03-P03、IMPL-03-P04、IMPL-03-P05、IMPL-03-P06、IMPL-03-P07、IMPL-03-P08、IMPL-03-M01、IMPL-03-M02、IMPL-03-M03、IMPL-03-M04、IMPL-03-T01、IMPL-03-T02、IMPL-03-T03 | TEST-03-01、TEST-03-02、TEST-03-03、TEST-03-04、TEST-03-05、TEST-03-06、TEST-03-07、TEST-03-08 | VAL-03-01、VAL-03-02、VAL-03-03、VAL-03-04 |

## 17. 风险与待确认事项

风险是上游合同过宽/语义不稳和低基数聚合推断；前者在实施前冻结合同，后者由域阈值和负向测试控制。任何公共合同修改仍需上游授权。

## 18. 五轮逐文档评审

结果见 `../内部审查记录_v2.0.md`。结论仅表示本文可在条件约束下进入实现评审；专用严格上游合同、请求绑定授权投影及 Spring 安全链通过后，才可实施和启用 EMPLOYEE/TRANSACTION 能力。本文不表示现有宽端点已成为目标实现或生产批准完成。

### 18.1 内部自检记录

| 轮次 | 日期 | S0 | S1 | S2 | 本轮处理 | 结论 |
|---:|---|---:|---:|---:|---|---|
| 1 | 2026-07-22 | 0 | 2 | 3 | 核实上游现状、补约束并修正文档结构 | 已修复 |
| 2 | 2026-07-22 | 0 | 2 | 1 | 分离字段用途，收紧版本/金额单位合同 | 已修复 |
| 3 | 2026-07-22 | 0 | 2 | 0 | 冻结用户范围绑定和低基数抑制 | 已修复 |
| 4 | 2026-07-22 | 0 | 2 | 0 | 补齐约束/规则与可失败合同测试 | 已修复 |
| 5 | 2026-07-22 | 0 | 0 | 2 | 同步历史、五轮口径和实施条件 | 有条件通过 |

### 18.2 本次实施落点增补自检

| 轮次 | 检查重点 | 发现与处置 | 结论 |
|---:|---|---|---|
| A | 域项目/模型模块 | 明确 Transaction 业务实现位于 `mq-procedure-service`，分布式共享专项模型位于 `transaction-api` | 通过 |
| B | 字段与签名真实性 | 发现 Employee 原示例字段不存在，收敛为已核实的 `position/workBaseSi`；补齐 Java/Python 全签名 | 通过 |
| C | 宽合同隔离与追踪 | 宽 Employee/Transaction 端点仅作勘察输入；新 IMPL/测试追踪完整且严格校验 0 warning | 有条件通过：上游新合同仍待编码与合同测试 |

### 18.3 2026-07-23 串行五轮评审记录

| 轮次 | 评审重点 | 冻结发现 | 原子修订与复核 | 本轮结论 |
|---:|---|---|---|---|
| 1 | 权威、状态与实施准入 | `L2-03-R1-01`（S1）：内部结论允许先实施，但状态仍为`In Review`且上游合同条件未完成 | 改为`Ready for Implementation Review with Conditions`，禁止在实现评审前启动 Agent/上游代码 | 已修复 |
| 2 | 服务身份、用户范围与数据所有权 | `L2-03-R2-01`（S0）：请求中的`AgentSubjectScope`可由 Agent 自填，上游无法证明其来自有效授权；Employee/Transaction 又没有 tenant 行级字段 | 原子同步 02 新增`BUSINESS_AUTHORIZATION`请求绑定 JWS；两个域改为服务 token + JWS 双校验，并明确首版只支持配置锁定的`SINGLE_TENANT_ALL` | 已修复；多租户仍明确不支持 |
| 3 | DTO、错误、时间与能力开关 | `L2-03-R3-01`（S1）：Java 值对象签名不完整；`L2-03-R3-02`（S1）：缺少稳定错误封套；`L2-03-R3-03`（S1）：Transaction 时区及 QUERY/groupBy 启用边界不明确 | 展开 Employee/Transaction 全部 DTO/枚举/错误；冻结 UTC/databaseZone、默认关闭和全局 COUNT 首批边界；保持`transaction-api`仅为分布式共享模型 | 已修复 |
| 4 | Java/Python/测试追踪 | `L2-03-R4-01`（S1）：27 个 IMPL 与 Java 验证只靠范围缩写，新增安全合同无可执行 Maven 门禁 | 展开全部原子 ID，新增 Java DTO/JWS/SQL/时区/模块边界测试及 VAL-03-04；追踪脚本三类下游缺口均为 0 | 已修复 |
| 5 | Servlet/WebFlux 认证可行性 | `L2-03-R5-01`（S1）：两个业务服务仍使用默认用户 decoder，独立 Agent 服务 token 无法到达内部端点；且两服务技术栈不同 | 分别新增 Employee Servlet 双安全链和 Transaction WebFlux 双安全链，补普通端点/WebSocket 回归；严格校验 0 error/0 warning | 已修复后通过 |

本次停止原因：达到用户指定的 5 轮上限，且第 5 轮修复后无遗留 S0/S1/S2。当前文档状态保持`In Review`；实现就绪判断为`ready_with_conditions`。条件是：02 的 Business JWS、两个严格上游合同和独立服务安全链先获实现评审批准并通过 PoC；部署必须提供匹配现有单租户数据的`singleTenantRef`，任何多租户/资源集合需求须先补数据与合同设计。
