# 检索与索引基础设施 L2 实施详细设计 v2.0

## 1. 文档信息与范围

| 项目 | 内容 |
|---|---|
| 文档标识 | `AGENT-L2-05` |
| 文档层级 | L2 详细设计 |
| 文档状态 | In Review |
| 内部结论 | Ready for Implementation with Conditions（逐文档五轮评审通过） |
| 当前版本 | v2.0 |
| 适用基线 | `AGENT-L0-001`、`AGENT-RETRIEVAL-L1-001` v2.0；当前 `es-query-*` 仅为空壳基线 |
| 维护责任角色 | 检索与索引基础设施实现负责人（具体人员由项目治理指定） |
| 是否可作为实现依据 | 有条件：可实施合同、Registry、Search/Index/Rebuild 骨架和测试；真实启用须先通过源一致性合同、跨语言安全、真实 ES 和版本化质量门禁 |
| 上位约束 | AD-05、AD-06、AD-07、AD-08、AD-09、AD-10、AD-11、AD-12、AD-13、AD-14；ADR-04；RET-DEC-01～RET-DEC-08 |
| 目标落点 | 现有 `es-query-api`、`es-query-service` 边界；当前仅确认路径存在，不宣称功能完成 |
| 设计范围 | Search、upsert/delete/bulk、全量/增量重建、任务查询、Alias 回滚 |
| 直接上位 | `../检索与索引基础设施架构_L1_v2.0.md` |
| 直接消费者 | `04_DOCUMENT检索问答能力_L2实施详细设计_v2.0.md` 的 `DocumentSearchClient` |

## 2. 修改历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v2.0 | 2026-07-22 | 重建最小检索/索引合同，移除旧双平面、复杂发布围栏和默认多通道检索。 |
| v2.0-r1 | 2026-07-22 | 核实空壳代码基线，冻结跨语言 OpenAPI/JSON Schema 合同源并统一章节编号。 |
| v2.0-r2 | 2026-07-22 | 冻结 Search 单页合同、授权投影证明、严格错误封套和响应预算。 |
| v2.0-r3 | 2026-07-22 | 关闭重建并发变更丢失风险，固定源游标门禁、切换屏障和 ES 控制索引。 |
| v2.0-r4 | 2026-07-22 | 声明 CON/DR，补齐实施路径、可失败测试、验证命令和版本化质量门禁。 |
| v2.0-r5 | 2026-07-22 | 收敛版本/键/错误合同，完成逐文档五轮治理与条件式实施结论。 |
| v2.0-r6 | 2026-07-22 | 展开 es-query-api/service 的 Java Controller/Service/Port/模型/枚举/工具类签名及 Python 消费模型落点。 |

## 3. 设计目标与范围

目标是补齐 Agent 可依赖的稳定原子检索能力。范围外/不负责：Agent 规划和问答、原文所有权、默认向量/重排、多写者平台及独立管理服务。最小必要抽象是 Search/Index/Rebuild/Registry 四个模块。

## 4. 当前实现基线、关联资源与责任边界

现状是当前 `es-query-api`仅有 POM，`es-query-service`仅有启动类/配置/基础测试。关联资源为 ES、文档源和 04 DocumentSearchClient；检索服务负责派生索引，文档源负责原文，Agent 负责问答，不得互改所有权。

## 5. 模块职责、依赖方向与调用边界

`Controller -> Search/Index/Rebuild Service -> Registry/Source Port/ES Client`。禁止依赖 Agent 内部类型，禁止绕过 Registry 访问物理索引，禁止 Agent 依赖 ES Client。稳定 Search DTO 降低 Agent 与 ES 的耦合，各模块按查询/写入/任务保持内聚。

内聚与耦合判断：Contract 只因外部 HTTP 合同变化，Search 只因查询/过滤规则变化，IndexWrite 只因投影版本规则变化，Rebuild 只因源一致性/发布恢复变化，Registry 只因逻辑索引和 Schema 策略变化；它们保留在同一部署单元，通过严格 DTO 与端口单向连接，不共享 Agent 类型或业务实体。Source Adapter 只调用获准上游 API，不下沉为通用数据访问层，也不直连业务数据库。

## 6. 需求

| 编号 | 可验收要求 |
|---|---|
| REQ-05-01 | 调用方只能使用逻辑索引和封闭查询，不能注入物理索引或原生 ES DSL。 |
| REQ-05-02 | Search 同时验证服务身份与绑定本请求的用户授权投影，授权过滤在 ES 查询内生效；身份、绑定或安全字段缺失时失败关闭。 |
| REQ-05-03 | 文档写入按稳定 ID/sourceVersion 幂等，乱序写和删除后复活被阻止。 |
| REQ-05-04 | 重建在新索引完成验证后原子切换 Alias，失败不影响旧索引并可回滚。 |
| REQ-05-05 | 搜索质量、延迟、索引版本和任务状态可复现、可观察。 |

### 6.1 上位约束与设计规则

| 编号 | 来源/规则 | 责任主体 | 可观察结果 |
|---|---|---|---|
| CON-05-01 | AD-07/08/10/14、RET-DEC-04/05：跨语言严格合同、服务身份与请求绑定授权投影分离、过滤前置 | Contract/Search/Security | 物理索引/DSL/越权请求到 ES 为 0 |
| CON-05-02 | AD-09/10、RET-DEC-02/07：复合键、版本化幂等、删除不可被旧事件复活 | IndexWrite/Source Adapter | 重复结果相同，乱序/冲突明确拒绝，复活为 0 |
| CON-05-03 | AD-06/09、RET-DEC-02/07：只读稳定源 API、单重建所有者、无缺口才切 Alias | Rebuild/Control Index/Source Adapter | 并发任务最多 1；未追平时 Alias 切换为 0 |
| CON-05-04 | AD-11/12/13、ADR-04、RET-DEC-03/08：不夸大现状，正文不进观测，增强检索由证据触发 | Metrics/Evaluation/Release | 泄漏为 0；无版本化效果证据时只保留关键词基线 |
| DR-05-01 | OpenAPI 3.1 唯一规范源、严格单页 Search、JWS 请求绑定和检索前过滤 | Contract/Search/Security | Java/Python 夹具一致，坏投影 ES 零调用 |
| DR-05-02 | `corpusId+documentId+sourceVersion+digest`原子版本规则和 tombstone marker | IndexWrite | 幂等/冲突/旧写/删除结果可判定 |
| DR-05-03 | ES 控制索引 CAS/lease/checkpoint，快照追平或静默证明，单次 Alias action | Rebuild/Registry | 故障恢复不猜测，切换/回滚可对账 |
| DR-05-04 | 版本化语料、阈值和目标环境预算控制 Search 启用及扩展 | Evaluation/Release | Recall/排序/延迟/安全门禁可复现 |

## 7. 接口与契约设计

跨语言 HTTP 合同的唯一规范源固定为 IMPL-05-J01 指定的 OpenAPI 3.1 文件（复用其 JSON Schema）；Java 服务端类型和 Python `DocumentSearchClient` 类型均由该合同生成或逐字段合同测试约束，不能各自成为第二权威。首版不为“可能出现的 Java 消费者”引入代码生成插件或新生产依赖；是否生成 Java 类型属于后续实现选择，不能改变规范源。

### 7.1 Search

`POST /indexes/{logicalIndex}/search`

```json
{
  "requestId":"018f...",
  "deadlineAt":"2026-07-22T12:00:02Z",
  "queryText":"年假结转",
  "corpusId":"POLICY",
  "filters":[{"field":"documentType","operator":"IN","values":["POLICY"]}],
  "authorizationProjection":"<compact-JWS>",
  "page":{"size":20},
  "returnFields":["title","location","snippet","sourceVersion"]
}
```

约束：logicalIndex/corpus/filter/returnFields 均在服务端 Registry 白名单；queryText 1..4000；filters 最多 10；size 1..100；requestId、deadlineAt 必填；未知字段、额外 JSON、脚本、正则、任意 sort、cursor/from 深分页和原生 DSL 拒绝。首阶段仅返回一个受上限约束的结果页，不提供继续翻页；关键词相关度排序，稳定 tie-break 由服务端追加。

`authorizationProjection`是 02 的确定性 projector 生成的短时 compact JWS。受保护头必须含固定`typ=agent-search-authz+jwt`、算法白名单值和`kid`；payload 必需 claims 为`iss/aud/sub/subjectRef/tenantRef/logicalIndex/allowedCorpora/allowedSecurityTags/evidenceVersion/iat/exp/jti/requestDigest`。`aud`固定为本检索服务，HTTP 服务身份 subject 必须等于`sub`，requestId 必须等于`jti`；`requestDigest`固定为 base64url(SHA-256(RFC 8785 JCS(request fields)))，request fields 精确为`logicalIndex/queryText/corpusId/filters/returnFields/page/requestId/deadlineAt`且不含 authorizationProjection。HTTP Authorization 中的服务身份仍独立校验，JWS 不能代替服务认证；身份/请求绑定、签名、有效期或 kid 任一不一致时，在访问 ES 前失败关闭。首版不维护全局 nonce 库；在短有效期内，只有相同已认证服务才能精确重放同一只读请求，JWS 不得用于 Index/Rebuild。

响应每项：`documentId/corpusId/sourceVersion/indexedAt/title/location/snippet/score/classification`，封套只返回`logicalIndexVersion/tookMs/hits`。`indexedAt`由索引写入事实提供，不能由查询时钟伪造。响应的 hit 数、单 snippet 字符数和序列化总字节受 Registry 硬上限约束；不返回物理索引、内部安全表达式或完整原文。

所有接口使用同一严格错误封套`{requestId, code, retryable, message}`且`additionalProperties=false`。稳定错误码至少包括通用`INVALID_REQUEST|UNAUTHORIZED|FORBIDDEN|UPSTREAM_TIMEOUT|INTERNAL_ERROR`、查询`AUTHORIZATION_EXPIRED|INDEX_NOT_READY`、写入`VERSION_CONFLICT|STALE_VERSION|REBUILD_CUTOVER_IN_PROGRESS`和重建`REBUILD_ACTIVE|SOURCE_GAP|CONTROL_STATE_UNAVAILABLE|ALIAS_STATE_UNKNOWN`；HTTP 状态、code 和 retryable 映射在 OpenAPI 中唯一声明。外部 message 不暴露物理索引、规则或 ES 原文。仅明确未开始执行的只读连接失败可由唯一 Client 责任方在剩余 deadline 内重试一次，服务端不透明重试。

### 7.2 Index write

- `PUT /indexes/{logicalIndex}/corpora/{corpusId}/documents/{documentId}`：幂等 upsert；
- `POST /indexes/{logicalIndex}/corpora/{corpusId}/documents/{documentId}:delete`：严格 body 携带 requestId/sourceVersion 的版本化删除；
- `POST /indexes/{logicalIndex}/documents:bulk`：最多配置上限的独立 item，逐项结果，不以部分成功冒充整体成功。

文档 DTO：`requestId, documentId, corpusId, sourceVersion, title, content, location, classification, securityTags, updatedAt`。首版`sourceVersion`是 JSON 字符串承载的`0..9223372036854775807`十进制整数，按数值比较；updatedAt 只用于展示，不能参与新旧判断。路径 corpusId/documentId 必须与 body 一致。服务端根据 Registry 决定映射；调用方不能传 index mapping 或 ingest script。重复同版本且规范字段摘要相同返回幂等成功；同版本不同摘要、旧版本或缺安全标签拒绝。bulk 每项都携带 request item id，整体响应明确 total/succeeded/failed 和逐项 code，任一失败时不得返回整体成功语义。

### 7.3 Rebuild

- `POST /indexes/{logicalIndex}/rebuilds`，body 只允许 `mode=FULL|INCREMENTAL` 和 Registry 枚举的 `reasonCode`；source cursor 只能由 Source Adapter/控制索引产生，调用方不能指定或回退；
- `GET /rebuilds/{taskId}`；
- `POST /rebuilds/{taskId}/cancel`仅在切换前有效；
- 回滚由受限管理接口将 Alias 指回已验证上一版本，不能由 Agent 请求触发。

任务状态：`PENDING|RUNNING|CATCHING_UP|VALIDATING|SWITCHED|SUCCEEDED|FAILED|CANCELLED|ROLLED_BACK`。FULL 可经过 SWITCHED；INCREMENTAL 只追平当前 Alias，不创建/切换物理索引。状态是运维合同，不等同 Agent execution 状态；未知或非法迁移拒绝，不能靠覆盖状态“修复”。

## 8. Registry 与 Schema

`IndexRegistry`配置包含：`logicalIndex/activeAlias/schemaVersion/allowedCorpora/searchableFields/filterFields/returnFields/securityFields/sourceAdapter/sourceConsistencyMode/rebuildLimits`。`sourceConsistencyMode`只能为`SNAPSHOT_WITH_REPLAY|QUIESCED_WINDOW`：前者要求源提供一致快照游标、顺序可比较且可重放的变更游标、包含删除事实的 tombstone/version watermark 及明确保留期；后者要求经审计的源静默窗口。启动时检查所有字段在 mapping 模板存在、securityFields 非空、Alias 名合法、sourceAdapter 唯一；上述源合同未通过合同测试时，对应 logicalIndex 的 Rebuild 保持禁用。

物理索引命名由服务生成，例如 `<logical>-v<schema>-<taskId>`；外部不得依赖名称。每次成功发布生成不含物理名称的 opaque `logicalIndexVersion`（绑定 schemaVersion/publishId），在控制状态、Search 封套和审计中一致使用；回滚恢复 previous 对应版本，不能用查询时钟临时生成。Schema 不兼容变更必须走全量重建和 Alias 切换，不能原地更改导致旧查询漂移。

## 9. 详细功能与核心处理流程：查询实现

1. 鉴别 Agent 服务调用方，验证授权投影 JWS 的 typ/alg/kid、签名、iss/aud/sub、subject/tenant、jti/requestDigest、有效期及 logicalIndex/corpus 权限，并确认 HTTP 服务 subject 与 sub、requestId 与 jti 相等；禁止信任自填用户头、未签名对象或任意 security filter；
2. 严格解析请求并从 Registry 解析字段；
3. 将 queryText 编译为受控 multi-match/等价关键词查询；
4. 将 tenant/securityTags 和业务 filters 作为 bool filter 一并下推；
5. 应用 size、source includes、snippet 长度和绝对 timeout；
6. 严格解析 ES 响应，验证每个 hit 的 corpus/security/version 字段；
7. 返回稳定 DTO。任何安全绑定缺失时整体失败，不返回部分宽结果。

Agent 可在返回后继续裁剪，但不能依赖该裁剪弥补第 4 步缺失。

## 10. 数据设计与生命周期：写入一致性

ES `_id`固定为无 padding base64url(UTF-8 corpusId) + `.` + 无 padding base64url(UTF-8 documentId)，同时持久化原始两字段、数值可比较`sourceVersion`、规范字段 SHA-256 digest 和 deleted tombstone marker。禁止仅用`documentId`或分隔符未编码拼接导致碰撞。写脚本在同一 ES 文档上原子实现：

- incoming > current：写入或删除；
- incoming == current 且 digest 相同：幂等成功；
- incoming == current 且 digest 不同：冲突；
- incoming < current：拒绝旧事件。

单文档错误不重试未知写结果；调用方可用相同 id/version/digest 重发并得到幂等结果。bulk 返回每项状态，重试只选失败且幂等可判定的项。

## 11. 状态模型、事务边界、重建与迁移回滚

首版不引入关系数据库或 Redis。检索服务在同一 ES 集群拥有设置`index.hidden=true`的控制索引`es-query-control-v1`，仅保存 Registry 运行版本、任务、租约、checkpoint、source high-water、active/previous 物理版本和审计摘要，不保存正文或安全标签全集。以 ES `seq_no/primary_term` CAS 和带到期时间的 lease 保证每个 logicalIndex 单活动任务；lease 只协调任务，不跨网络调用持有事务。控制索引不可用时 Search 可继续读取已存在 Alias，Index/Rebuild/回滚/清理全部失败关闭；控制索引丢失时必须从 Alias 与权威源完成受审计对账/全量重建后才能恢复管理写入，不能猜测任务状态。

FULL 固定流程：

1. CAS 获得 logicalIndex lease，记录 active/previous、目标 schema 和 Source Adapter 合同版本；
2. 创建新物理索引；从当前 active 索引复制 tombstone/version marker，再从权威源取得一致快照及快照游标 C0，按版本规则导入并记录 checkpoint；
3. `SNAPSHOT_WITH_REPLAY`模式按游标重放 `(C0,C1]`，进入短切换屏障后拒绝该 logicalIndex 的新 Index 请求为可重试`REBUILD_CUTOVER_IN_PROGRESS`，再次取得 high-water C2 并重放至 C2；`QUIESCED_WINDOW`模式则验证已获授权的源静默证明，不能用应用内布尔开关冒充；
4. 只有在 source high-water、applied cursor、文档/删除数、必需/安全字段、重复键和固定样例查询全部一致时进入 VALIDATING；无法证明无游标缺口时 FAILED，Alias 保持不变；
5. 使用一次 ES Alias action 原子地移除旧目标并添加新目标，记录 previous/new index、C2/静默证明和操作者服务身份，再解除屏障；切换 API 结果未知时先读取 Alias 事实再迁移状态，禁止盲重试；
6. 观察窗口内比较错误率、空命中和样例查询；严重异常以同样 CAS/审计流程回切 previous；稳定且超过保留期后，清理任务再次证明目标既非 active 也非 previous 才删除。

INCREMENTAL 从控制索引保存的 lastAppliedCursor 读取连续变更并写当前 Alias；每一页确认后才推进 checkpoint，发现断档、过期游标或不兼容 schema 时 FAILED 并要求 FULL，不能跳过缺口。任务恢复从已确认 checkpoint 继续；接管者先核对 lease、Alias 和 source high-water。任何不一致均 FAILED/告警并等待显式重启或对账。

## 12. 权限与安全审计、运维

- Search 与 Index/Rebuild 使用不同服务权限；外部用户不得直达管理接口。
- Registry 必须声明`sourceService -> logicalIndex/corpusId/action`白名单；写入、删除、重建和回滚逐项校验，禁止跨语料库管理操作。
- ES 凭据只存在检索服务，Agent 和模型均不可见。
- 日志只记录 query digest、logicalIndex、corpus、过滤数量、耗时、命中数和错误码；不记录 queryText/snippet/content/securityTags 全值。
- 限制请求/响应/bulk/context 大小，并设置 ES timeout、连接池和并发上限。
- 指标：search latency/error/empty-hit、security reject、index conflict、task progress、Alias version、rollback count。

## 13. 当前仓库基线处理与最小变更

当前 `es-query-api`仅见 POM，`es-query-service`仅见启动类/配置/基础测试；完整历史实现位于 `_alpha` 的事实不能自动继承为目标代码。实施阶段应：

1. 对当前模块执行编译和启动基线；
2. 逐项按本 L2 新建最小实现或经审查迁移必要逻辑；
3. 不整体复制 `_alpha`，不恢复旧 Agent/document 依赖；
4. 如需改 employee-service 等现有消费者合同，先取得其范围授权并做兼容测试。

## 14. 实施落点

路径均相对仓库根目录 `D:/codex`。当前 `es-query-api`只有 POM，`es-query-service`只有启动类/配置/基础测试；除启动类标记`已存在`外，以下类型均为`建议新增`，不得据此宣称 Search/Index/Rebuild 已实现。

### 14.1 跨语言合同源与 Java API 模型

| 编号 | 状态 | 完整路径/类型 | 内容/签名 |
|---|---|---|---|
| IMPL-05-J01 | 建议新增 | `es-query-api/src/main/resources/openapi/es-query-v1.yaml` | OpenAPI 3.1 唯一规范源；声明 Search、Index、Bulk、Rebuild、Cancel、Rollback、统一错误的路径、状态码、枚举、`additionalProperties=false`与大小约束。Java/Python 类型不得成为第二权威。 |
| IMPL-05-J02 | 建议新增 | `es-query-api/src/main/java/com/dylan/esquery/api/v1/search/`；每个下列类型为该目录下同名 `.java` 文件 | `SearchRequest(String requestId, Instant deadlineAt, String queryText, String corpusId, List<SearchFilter> filters, String authorizationProjection, SearchPage page, Set<String> returnFields)`、`SearchFilter(String field, FilterOperator operator, List<String> values)`、`SearchPage(int size)`、`SearchHit(String documentId, String corpusId, String sourceVersion, Instant indexedAt, String title, String location, String snippet, double score, DataClassification classification)`、`SearchResponse(String logicalIndexVersion, long tookMs, List<SearchHit> hits)`；枚举 `FilterOperator{EQ,IN,GTE,LTE}`。 |
| IMPL-05-J03 | 建议新增 | `es-query-api/src/main/java/com/dylan/esquery/api/v1/index/`；每个下列类型为该目录下同名 `.java` 文件 | `IndexDocumentRequest(String requestId, String documentId, String corpusId, String sourceVersion, String title, String content, String location, DataClassification classification, Set<String> securityTags, Instant updatedAt)`；`DeleteDocumentRequest(String requestId, String sourceVersion)`；`BulkIndexRequest(String requestId, List<BulkIndexItem> items)`；`BulkIndexItem(String itemId, BulkOperation operation, String corpusId, String documentId, IndexDocumentRequest document, DeleteDocumentRequest delete)`；`IndexWriteResponse(String requestId, String documentId, String sourceVersion, IndexWriteStatus status)`；`BulkIndexResponse(String requestId, int total, int succeeded, int failed, List<BulkItemResult> items)`；`BulkItemResult(String itemId, IndexWriteStatus status, SearchErrorCode code)`；枚举 `BulkOperation{UPSERT,DELETE}`、`IndexWriteStatus{APPLIED,IDEMPOTENT,CONFLICT,STALE,FAILED}`。 |
| IMPL-05-J04 | 建议新增 | `es-query-api/src/main/java/com/dylan/esquery/api/v1/rebuild/`、`es-query-api/src/main/java/com/dylan/esquery/api/v1/error/`、`es-query-api/src/main/java/com/dylan/esquery/api/v1/common/`；每个下列类型为对应目录下同名 `.java` 文件 | `RebuildRequest(RebuildMode mode, String reasonCode)`、`RebuildTaskResponse(String taskId, String logicalIndex, RebuildMode mode, RebuildStatus status, String logicalIndexVersion, Instant startedAt, Instant updatedAt, String failureCode)`、`CancelRebuildResponse(String taskId, RebuildStatus status)`、`RollbackRequest(String requestId, String reasonCode)`、`RollbackResponse(String logicalIndex, String logicalIndexVersion, RebuildStatus status)`、`ErrorResponse(String requestId, SearchErrorCode code, boolean retryable, String message)`；枚举 `RebuildMode{FULL,INCREMENTAL}`、`RebuildStatus{PENDING,RUNNING,CATCHING_UP,VALIDATING,SWITCHED,SUCCEEDED,FAILED,CANCELLED,ROLLED_BACK}`、`SearchErrorCode`（7.1 稳定码全集）、`DataClassification{PUBLIC,INTERNAL,RESTRICTED,UNKNOWN}`。 |

Java 类型可由 OpenAPI 生成或手写，但必须通过同一固定夹具；首版不因代码生成引入未批准生产插件。生成选择为`待确认`，不影响文件与签名权威。

### 14.2 Java Search 实施落点

| 编号 | 状态 | 完整路径/类 | 入口与主要方法签名 | 责任 |
|---|---|---|---|---|
| IMPL-05-J05 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/search/SearchController.java` | `public ResponseEntity<SearchResponse> search(String logicalIndex, SearchRequest request, Authentication authentication)` | `POST /indexes/{logicalIndex}/search`入口；严格解析后交给安全验证和 SearchService。 |
| IMPL-05-J06 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/search/SearchService.java` | `public SearchResponse search(AuthenticatedSearchCommand command)`；内部 `void validateResponse(SearchResponse response, AuthorizedSearchContext context, IndexDefinition definition)` | Registry 解析、受控编译、绝对超时、Repository 调用和响应安全重验；不透明重试为 0。 |
| IMPL-05-J07 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/search/ControlledQueryCompiler.java` | `public CompiledSearchQuery compile(AuthenticatedSearchCommand command, IndexDefinition definition, Duration remaining)` | 只从 Registry 映射 query/filter/source includes/tie-break/size/timeout；不接受物理索引或原生 DSL。 |
| IMPL-05-J08 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/search/ElasticsearchSearchRepository.java` | `public SearchRepositoryResult search(CompiledSearchQuery query)`；`public AliasSnapshot readAlias(String activeAlias)` | 唯一 ES Search/Alias 读取 Adapter；ES Client 具体类型封装在此包，不能出现在 Controller/API。 |
| IMPL-05-J09 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/security/SearchAuthorizationVerifier.java` | `public AuthorizedSearchContext verify(Authentication authentication, String logicalIndex, SearchRequest request, Instant now)`；内部 `void verifyRequestDigest(String logicalIndex, SearchRequest request, SearchAuthorizationClaims claims)`、`void verifyClaims(Authentication authentication, String logicalIndex, SearchRequest request, SearchAuthorizationClaims claims, Instant now)` | 独立校验 HTTP 服务身份与 JWS typ/alg/kid/签名/iss/aud/sub/jti/digest/exp，并在 ES 调用前失败。 |
| IMPL-05-J10 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/security/JcsRequestDigest.java` | `public String digest(String logicalIndex, SearchRequest request)`；`public byte[] canonicalize(SearchDigestInput input)` | 精确实现 RFC 8785 JCS 输入集合和 SHA-256 base64url；不得包含 authorizationProjection。加密/JCS 库版本需依赖评审。 |

### 14.3 Java Index 实施落点

| 编号 | 状态 | 完整路径/类 | 入口与主要方法签名 | 责任 |
|---|---|---|---|---|
| IMPL-05-J11 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/index/IndexWriteController.java` | `public ResponseEntity<IndexWriteResponse> upsert(String logicalIndex, String corpusId, String documentId, IndexDocumentRequest request, Authentication authentication)`；`public ResponseEntity<IndexWriteResponse> delete(String logicalIndex, String corpusId, String documentId, DeleteDocumentRequest request, Authentication authentication)`；`public ResponseEntity<BulkIndexResponse> bulk(String logicalIndex, BulkIndexRequest request, Authentication authentication)` | 校验管理服务身份、路径/body 一致和 Registry action 白名单。 |
| IMPL-05-J12 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/index/IndexWriteService.java` | `public IndexWriteResponse upsert(AuthorizedIndexCommand command)`；`public IndexWriteResponse delete(AuthorizedDeleteCommand command)`；`public BulkIndexResponse bulk(AuthorizedBulkCommand command)` | 执行 sourceVersion/digest/tombstone 原子规则；bulk 保留逐项结果，不将部分成功包装为整体成功。 |
| IMPL-05-J13 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/index/ElasticsearchIndexRepository.java` | `public IndexWriteResult applyVersionedWrite(VersionedIndexMutation mutation)`；`public List<IndexWriteResult> applyBulk(List<VersionedIndexMutation> mutations)`；`public void setCutoverBarrier(String logicalIndex, boolean enabled)` | 封装 ES 脚本/批量写与切换屏障；未知写结果由同 id/version/digest 重放判定。 |
| IMPL-05-J14 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/index/DocumentKeyCodec.java`、`es-query-service/src/main/java/com/dylan/baseline/esquery/index/CanonicalDocumentDigest.java`、`es-query-service/src/main/java/com/dylan/baseline/esquery/index/SourceVersion.java` | `String encode(String corpusId, String documentId)`；`String digest(IndexDocumentRequest request)`；`static SourceVersion parse(String value)`、`int compareTo(SourceVersion other)` | 复合键 base64url、规范字段 SHA-256 和无符号范围内十进制版本比较；禁止分隔符直拼与更新时间排序。 |

### 14.4 Java Rebuild、Registry 与运维落点

| 编号 | 状态 | 完整路径/类 | 入口与主要方法签名 | 责任 |
|---|---|---|---|---|
| IMPL-05-J15 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/rebuild/RebuildController.java` | `public ResponseEntity<RebuildTaskResponse> start(String logicalIndex, RebuildRequest request, Authentication authentication)`；`public ResponseEntity<RebuildTaskResponse> get(String taskId, Authentication authentication)`；`public ResponseEntity<CancelRebuildResponse> cancel(String taskId, Authentication authentication)`；`public ResponseEntity<RollbackResponse> rollback(String logicalIndex, RollbackRequest request, Authentication authentication)` | 受限管理入口；Agent 服务身份不得调用。 |
| IMPL-05-J16 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/rebuild/RebuildService.java` | `public RebuildTask start(AuthorizedRebuildCommand command)`；`public RebuildTask get(String taskId)`；`public RebuildTask cancel(AuthorizedCancelCommand command)`；`public RebuildTask rollback(AuthorizedRollbackCommand command)`；`public RebuildTask resume(String taskId)` | 单任务 lease、状态机、快照/重放、VALIDATING、单次 Alias action、未知结果对账和回滚。 |
| IMPL-05-J17 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/rebuild/SourcePort.java` | `SourceSnapshot openSnapshot(String logicalIndex, int batchSize)`；`SourcePage readSnapshot(SourceSnapshot snapshot, String cursor, int batchSize)`；`ChangePage readChanges(String logicalIndex, String afterCursor, int batchSize)`；`SourceHighWater highWater(String logicalIndex)`；`QuiescenceProof verifyQuiescence(String logicalIndex)` | 权威源一致性 Port；具体 Adapter 必须逐源通过合同测试，不直连业务数据库。 |
| IMPL-05-J18 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/rebuild/ControlStateRepository.java`、`es-query-service/src/main/java/com/dylan/baseline/esquery/rebuild/ElasticsearchControlStateRepository.java` | `Optional<RebuildLease> acquireLease(String logicalIndex, String owner, Instant expiresAt)`；`RebuildTask compareAndSetTask(RebuildTask expected, RebuildTask updated)`；`void saveCheckpoint(RebuildCheckpoint checkpoint)`；`Optional<ControlIndexState> readIndexState(String logicalIndex)` | 使用 control index 的 seq_no/primary_term CAS；控制状态不可用时管理写失败关闭。 |
| IMPL-05-J19 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/rebuild/RebuildStateMachine.java` | `public RebuildTask transition(RebuildTask current, RebuildEvent event)`；`public boolean canCancel(RebuildStatus status)` | 显式合法迁移；禁止覆盖状态修复和切换后取消。 |
| IMPL-05-J20 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/registry/IndexRegistryProperties.java`、`es-query-service/src/main/java/com/dylan/baseline/esquery/registry/IndexRegistry.java`、`es-query-service/src/main/java/com/dylan/baseline/esquery/registry/IndexRegistryValidator.java` | `IndexDefinition require(String logicalIndex)`；`void validate(IndexRegistryProperties properties)`；`void afterPropertiesSet()` | 加载逻辑索引、Alias、Schema、字段、预算、Source Adapter 和一致性模式；未知/冲突/安全字段空时启动失败。 |
| IMPL-05-J21 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/registry/RegistryModels.java`（或同包独立 records） | `IndexDefinition`、`FieldDefinition`、`RebuildLimits`、`SourceBinding`；枚举 `SourceConsistencyMode{SNAPSHOT_WITH_REPLAY,QUIESCED_WINDOW}`、`RegistryAction{SEARCH,UPSERT,DELETE,BULK,REBUILD,ROLLBACK}` | 版本化不可变 Registry 模型；不暴露物理索引给外部合同。 |
| IMPL-05-J22 | 建议新增 | `es-query-service/src/main/java/com/dylan/baseline/esquery/config/EsQuerySecurityConfig.java`、`es-query-service/src/main/java/com/dylan/baseline/esquery/config/EsClientConfig.java`；`es-query-service/src/main/java/com/dylan/baseline/esquery/observability/SearchMetrics.java`、`es-query-service/src/main/java/com/dylan/baseline/esquery/observability/SecurityAudit.java` | `SecurityFilterChain filterChain(HttpSecurity http) throws Exception`；`void record(SearchMetric metric)`；`void record(SecurityAuditEvent event)` | 分离 Search 与管理 scope、限制连接池/超时/大小并执行脱敏观测。ES/JWS/JCS/Testcontainers 版本须与目标 BOM 核验后另行批准生产/测试依赖。 |
| IMPL-05-J23 | 已存在 | `es-query-service/src/main/java/com/dylan/baseline/esquery/EsQueryServiceApplication.java` | `public static void main(String[] args)` | 保留启动入口；新增组件必须位于其 component scan 包下。 |

Rebuild 内部模型至少包括`RebuildTask`、`RebuildLease`、`RebuildCheckpoint`、`ControlIndexState`、`SourceSnapshot`、`SourcePage`、`ChangePage`、`SourceHighWater`、`QuiescenceProof`、`AliasSnapshot`、`RebuildEvent`；均为不可变 record，字段覆盖 task/logicalIndex/schema/active/previous/target/cursor/high-water/lease/version/timestamps/failureCode，正文不得进入控制索引。

### 14.5 Python 消费端落点

| 编号 | 状态 | 完整路径 | 函数/模型 | 权威边界 |
|---|---|---|---|---|
| IMPL-05-P01 | 建议新增 | `agent-service/agent_service/clients/retrieval.py`、`agent-service/agent_service/clients/retrieval_models.py` | `async def search(self, request: SearchRequest, deadline: Deadline) -> SearchResponse`；模型 `SearchRequest/SearchFilter/SearchPage/SearchHit/SearchResponse/SearchErrorResponse` | 由 L2-04 实施；Python 只消费 IMPL-05-J01 OpenAPI，不实现 Index/Rebuild，不访问 ES。完整字段/函数见 L2-04 IMPL-04-P03/M03。 |
| IMPL-05-P02 | 建议新增 | `agent-service/tests/contract/test_retrieval_openapi.py` | `def test_python_models_match_openapi_fixtures() -> None` | 合法、extra-field、枚举、错误和日期夹具与 Java/OpenAPI 逐字段一致。 |

### 14.6 测试类落点

| 编号 | 状态 | 完整路径 | 覆盖 |
|---|---|---|---|
| IMPL-05-T01 | 建议新增 | `es-query-service/src/test/java/com/dylan/baseline/esquery/contract/OpenApiContractTest.java`、`es-query-service/src/test/java/com/dylan/baseline/esquery/security/SearchAuthorizationTest.java`、`es-query-service/src/test/java/com/dylan/baseline/esquery/search/SearchIntegrationIT.java` | OpenAPI/Java/Python 合同、JWS 双身份、ES 前拒绝、真实 mapping/query 与响应预算。 |
| IMPL-05-T02 | 建议新增 | `es-query-service/src/test/java/com/dylan/baseline/esquery/index/IndexVersioningTest.java`、`es-query-service/src/test/java/com/dylan/baseline/esquery/rebuild/SourceAdapterContractTest.java`、`es-query-service/src/test/java/com/dylan/baseline/esquery/rebuild/RebuildConsistencyIT.java`、`es-query-service/src/test/java/com/dylan/baseline/esquery/rebuild/RebuildRecoveryIT.java` | 复合键/版本/tombstone、源缺口、单 lease、追平切换、未知 Alias 结果、回滚和活动索引不清理。 |
| IMPL-05-T03 | 建议新增 | `es-query-service/src/test/java/com/dylan/baseline/esquery/architecture/DependencyBoundaryTest.java`、`es-query-service/src/test/java/com/dylan/baseline/esquery/registry/IndexRegistryValidatorTest.java` | 无 Agent 类型/ES Client 外泄/正文日志，Registry 启动失败关闭。 |

## 15. 测试与验证设计

| 编号 | 层级/建议路径 | 夹具与动作 | 关键断言 | 失败信号 |
|---|---|---|---|---|
| TEST-05-01 | 建议新增：Contract `.../contract/OpenApiContractTest.java` 与 Python consumer fixture | 固定合法/extra-field/DSL/物理索引/超限请求及错误封套 | OpenAPI、Java、Python 逐字段/枚举/额外属性一致 | 任一语言接受合同外字段或错误漂移 |
| TEST-05-02 | 建议新增：Security `.../security/SearchAuthorizationTest.java` | 服务身份和 JWS 独立夹具；坏签名、错 typ/aud/sub/jti/requestDigest、过期/缺字段 | 全部在 ES 前拒绝且返回稳定安全错误 | ES spy 有调用或外部 message 泄露规则 |
| TEST-05-03 | 建议新增：Unit/Property `.../index/IndexVersioningTest.java` | 随机复合键、重复/乱序/同版异 digest、删除后旧写、bulk 部分失败 | 版本状态机确定且跨 corpus 无碰撞 | 旧内容覆盖、新删除复活或整批假成功 |
| TEST-05-04 | 建议新增：Contract `.../rebuild/SourceAdapterContractTest.java` | 快照 C0、连续/断档/过期游标、删除 watermark、静默证明 | 仅已声明 consistency mode 可启用；缺口不可跳过 | 无可重放源仍进入 VALIDATING |
| TEST-05-05 | 建议新增：Integration `.../rebuild/RebuildConsistencyIT.java` | 并发 lease，快照期间新增/更新/删除，切换屏障和未知切换结果 | 活动任务最多 1，追平 C2 后单次切换，零变更丢失 | 未追平仍切换或屏障写被静默吞掉 |
| TEST-05-06 | 建议新增：Recovery `.../rebuild/RebuildRecoveryIT.java` | lease 到期接管、checkpoint 重放、控制索引故障、回滚/清理 | 不猜测切换；active/previous 永不清理 | 双任务、错误回切或活动索引被删除 |
| TEST-05-07 | 建议新增：真实 ES/Testcontainers `.../search/SearchIntegrationIT.java` | 固定语料、securityTags/tenant 负向集、响应字节/timeout | 过滤下推，越权命中 0，响应预算生效 | mock 通过但真实 mapping/query 失败 |
| TEST-05-08 | 建议新增：Architecture `.../architecture/DependencyBoundaryTest.java` | 扫描依赖、Controller 参数和日志 canary | 无 Agent 类型/ES Client 外泄/正文日志，管理权限独立 | 反向依赖、物理名或 canary 暴露 |

| 验证编号 | 工作目录与建议命令/评测 | 预期结果 | 当前状态 |
|---|---|---|---|
| VAL-05-01 | 仓库根目录：mvn -f es-query-service/pom.xml test；mvn -f es-query-api/pom.xml test | 当前空壳基线测试通过；不能据此宣称目标 Search/Index/Rebuild 已实现 | 已执行：service 1/1 通过；api 构建成功、无源码/测试 |
| VAL-05-02 | 实现后：mvn -f es-query-service/pom.xml test，并执行 TEST-05-01～08 | 合同/安全/一致性/恢复测试全通过，越权命中和复活均为 0 | 未执行：目标实现不存在 |
| VAL-05-03 | 版本化固定语料与负向集：Recall@50、NDCG@10、P95/P99、空命中、越权泄漏 | dataset/schema/index/analyzer/threshold/环境版本固定；Recall@50、NDCG@10 和延迟达到批准阈值，越权泄漏为 0 | 未执行：语料、阈值和目标环境未冻结 |

只有实施并在目标环境运行后才能声明能力或效果通过；当前评审不把空壳启动测试等同于目标能力验证。

## 16. 端到端追踪矩阵

| 需求/约束 | 设计规则 | 实现 | 测试 | 验证 |
|---|---|---|---|---|
| REQ-05-01/02、CON-05-01 | DR-05-01 唯一合同、白名单和检索前过滤 | IMPL-05-J01～J10、J20～J22、P01/P02、T01/T03 | TEST-05-01/02/07/08 | VAL-05-02/03 |
| REQ-05-03、CON-05-02 | DR-05-02 sourceVersion/digest/tombstone 幂等 | IMPL-05-J03/J04、J11～J14、J20/J21、T02 | TEST-05-03/07 | VAL-05-02 |
| REQ-05-04、CON-05-03 | DR-05-03 CAS lease、追平后 Alias 切换 | IMPL-05-J04、J08、J13、J15～J21、T02 | TEST-05-04/05/06 | VAL-05-02 |
| REQ-05-05、CON-05-04 | DR-05-04 版本化质量与运维门禁 | IMPL-05-J06/J08、J16～J23、T01～T03 | TEST-05-07/08 | VAL-05-03 |
| 全量覆盖索引 | REQ-05-01、REQ-05-02、REQ-05-03、REQ-05-04、REQ-05-05；CON-05-01、CON-05-02、CON-05-03、CON-05-04 | DR-05-01、DR-05-02、DR-05-03、DR-05-04；IMPL-05-J01～J23、P01/P02、T01～T03 | TEST-05-01、TEST-05-02、TEST-05-03、TEST-05-04、TEST-05-05、TEST-05-06、TEST-05-07、TEST-05-08 | VAL-05-01、VAL-05-02、VAL-05-03 |

## 17. 风险与待确认事项

风险是当前模块为空壳、上游消费者合同兼容、源服务快照/重放能力和真实 ES 运维参数未确认；实施先做基线测试。若源不能提供`SNAPSHOT_WITH_REPLAY`合同，只能在获授权且可验证的`QUIESCED_WINDOW`重建，不能以“最终一致”接受切换数据倒退。公共合同变化需授权，容量、lease/游标/tombstone/旧索引保留期必须由目标环境和源最大重放窗口验证决定。

## 18. 五轮逐文档评审

结果同步至 `../内部审查记录_v2.0.md`。结论允许实施合同和基础设施骨架；在源快照/重放或静默合同、JWS 互操作、真实 ES 一致性、版本化效果与目标环境门禁通过前，Search/Index/Rebuild 不得声明可用或生产批准。本文不表示代码、数据、质量效果或生产验证完成。

### 18.1 内部自检记录

| 轮次 | 日期 | S0 | S1 | S2 | 处置摘要 | 状态 |
|---:|---|---:|---:|---:|---|---|
| 1 | 2026-07-22 | 0 | 1 | 3 | 核实空壳基线，冻结唯一合同源、治理身份和章节结构 | 已修复 |
| 2 | 2026-07-22 | 0 | 2 | 1 | 冻结单页 Search、请求绑定 JWS、错误和响应预算 | 已修复 |
| 3 | 2026-07-22 | 1 | 2 | 0 | 修复重建期间变更丢失风险，固定源一致性和控制状态 | 已修复 |
| 4 | 2026-07-22 | 0 | 3 | 1 | 补齐约束/规则、可失败测试、验证与质量门禁 | 已修复 |
| 5 | 2026-07-22 | 0 | 1 | 1 | 修复删除复合键缺 corpus、JWS 规范化和发布版本歧义后回归 | 有条件通过 |

### 18.2 本次实施落点增补自检

| 轮次 | 检查重点 | 发现与处置 | 结论 |
|---:|---|---|---|
| A | Java 包/类完整性 | 补齐 API records/枚举及 Search/Index/Rebuild/Registry/Security/工具类完整路径 | 通过 |
| B | 方法与状态模型 | 补齐 Controller、Service、Port、Repository、CAS/版本工具的输入输出签名及 Python 消费模型 | 通过 |
| C | 权威与可用性声明 | OpenAPI 仍是唯一合同源；空壳现状、生产依赖待批和效果门禁未被改写；严格校验 0 warning | 有条件通过 |
