# 入口、规划与可信执行 L2 实施详细设计 v2.0

## 1. 文档信息与范围

| 项目 | 内容 |
|---|---|
| 文档标识 | `AGENT-L2-01` |
| 文档层级 | L2 详细设计 |
| 文档状态 | In Review |
| 内部结论 | Ready for Implementation（逐文档五轮评审通过） |
| 当前版本 | v2.0 |
| 适用基线 | `AGENT-L0-001`、`AGENT-APP-L1-001` v2.0；新 Python Agent 代码尚不存在 |
| 维护责任角色 | Agent 入口与图编排实现负责人（具体人员由项目治理指定） |
| 是否可作为实现依据 | 是，但仅限首个 01/02/03 最小纵切 PoC 与分阶段实现；上游合同/PoC/生产门禁仍须单独通过 |
| 上位约束 | AD-01、AD-02、AD-03、AD-09、AD-10、AD-13、AD-14；ADR-01、ADR-02、ADR-03、ADR-05、ADR-06；APP-DEC-01、APP-DEC-02、APP-DEC-03、APP-DEC-08、APP-DEC-09 |
| 范围 | Python ASGI API、LangGraph StateGraph/State、规划 Schema、确定性准入、错误和审计 |
| 范围外 | 权限字段算法（02）、能力字段（03/04）、检索实现（05） |

## 2. 修改历史

| 版本 | 日期 | 变更 |
|---|---|---|
| v2.0 | 2026-07-22 | 重建为官方 Python LangGraph 单运行时、受限同步 StateGraph 和确定性计划准入。 |
| v2.0-r1 | 2026-07-22 | 补齐治理身份，修正 State 字段所有权、移除 planning_context 持久字段并统一章节编号。 |
| v2.0-r2 | 2026-07-22 | 将历史降为不可信数据，并封闭 warning/error/CLARIFY 输出。 |
| v2.0-r3 | 2026-07-22 | 禁止多目标/动态路由，冻结 Client 重试与安全审计失败关闭。 |
| v2.0-r4 | 2026-07-22 | 声明 CON/DR，补齐测试夹具、关键断言、失败信号和验证命令。 |
| v2.0-r5 | 2026-07-22 | 同步五轮治理口径、条件式实施依据和自检记录。 |

## 3. 设计目标与范围

目标是用官方 Python LangGraph 明确首版控制流，同时严格限制图能力。最小变更/新增抽象仅限 FastAPI/Pydantic 边界、`AgentState`、Graph Builder、固定节点、Validator 和窄 Client；不建设 Java 主编排、动态工作流或通用 Agent 平台。

## 4. 当前实现基线、关联资源与责任边界

新 Python `agent-service` 尚不存在，所有代码路径均为建议新增。网关/Auth/Java 业务服务是关联资源；FastAPI 负责 HTTP，已编译 StateGraph 是唯一流程权威，规划节点只产候选，确定性能力节点只收已校验计划。本文不负责上游公共合同变更，也不宣称 LangGraph PoC 已完成。

## 5. 模块职责、依赖方向与调用边界

依赖方向为 `api -> compiled graph -> nodes -> planning/capabilities/security -> clients`。禁止 Client 反向依赖图，禁止能力节点绕过 Validator，禁止模型节点直接调用 Auth/业务/检索 Client，禁止新增 Java 主编排；该稳定合同保持节点内聚、降低图与上游耦合。

## 6. 需求

| 编号 | 可验收要求 |
|---|---|
| REQ-01-01 | 已认证调用方可用一个同步接口提交问题并得到结果、澄清或类型化错误。 |
| REQ-01-02 | 模型只能产生封闭计划，任何非法/未知内容在上游调用前被拒绝。 |
| REQ-01-03 | 所有阶段共享一个绝对 deadline，超时后不提交迟到结果。 |
| REQ-01-04 | 每个请求可通过 requestId 关联审计和指标，但不泄露正文。 |
| REQ-01-05 | 组件依赖保持模块化单体边界，不出现旧模块或跨运行时合同。 |
| REQ-01-06 | Graph State 显式类型化且只在请求内存在；首版顶层及独立嵌套图均使用`checkpointer=False`。 |
| REQ-01-07 | 图拓扑固定，一个请求只执行一个能力，无动态节点、循环 Tool 或多 Agent。 |

### 6.1 上位约束与设计规则

| 编号 | 来源/规则 | 责任主体 | 可观察结果 |
|---|---|---|---|
| CON-01-01 | AD-03/10：模型只提议，严格合同与确定性准入 | api/plan/validate | 非法计划在任何上游调用前终止 |
| CON-01-02 | AD-09：绝对 deadline 与 Client 唯一重试所有者 | deadline/各 Client | 超时后无 State 更新、输出或新增调用 |
| CON-01-03 | AD-01/13/14：单运行时、封闭输出、服务身份/出站安全 | graph/clients/audit | 无 Java 编排、无敏感 State/trace、无未批准出站 |
| CON-01-04 | AD-02：请求级 State、固定图、显式禁用 checkpointer | state/builder | 无 thread、持久记忆、动态路由或恢复能力 |
| DR-01-01 | API 与计划使用封闭 Schema；validate 产生唯一不可变计划 | api/plan/validate | 只有合法单能力计划可执行 |
| DR-01-02 | 全链路只使用一个绝对 deadline | deadline/clients | 远调、重试、序列化均不越过 deadline |
| DR-01-03 | 仅输出安全摘要并在返回前确认安全审计接收 | audit/result_security | 正文与秘密不落日志，审计失败不返回结果 |
| DR-01-04 | State 字段单写者、固定单目标条件边、`checkpointer=False` | state/builder | 拓扑和状态合同可快照验证 |

## 7. 接口与契约设计

### 7.1 请求

`POST /api/agent/v1/execute`

```json
{
  "message": "查询研发部在职员工数量",
  "conversation": [{"role":"USER","content":"..."}],
  "timeoutMs": 10000
}
```

| 字段 | 规则 |
|---|---|
| `message` | required，去首尾后 1..4000 字符 |
| `conversation` | optional，最多 10 轮/总计 12000 字符，只允许 USER/ASSISTANT；两种角色都只是调用方提供的不可信转录标签，当前请求不得重复 |
| `timeoutMs` | optional，1000..30000；缺省由服务配置给定 |

未知字段、显式非法 null、非法 role、超长或空白 message 返回 `INVALID_REQUEST`。主体、tenant、权限和能力不得由请求体提供。`conversation`不得直接映射为模型的 system/developer/tool 控制消息；即使标签为 ASSISTANT，也必须在规划输入中作为有边界的不可信历史数据编码，并先经过 02 的最小投影。

### 7.2 响应

成功 HTTP 200：

```json
{
  "requestId":"...",
  "type":"QUERY_RESULT",
  "capability":"QUERY",
  "data":{},
  "citations":[],
  "warnings":[]
}
```

`type`只允许 `QUERY_RESULT|AGGREGATE_RESULT|DOCUMENT_ANSWER|DOCUMENT_SUMMARY|CLARIFICATION`。非 DOCUMENT 的 `citations` 必须为空。`warnings`只允许白名单`code`和受限标量参数，展示文本由服务端模板生成，禁止复制模型/上游正文。错误封套：

```json
{"requestId":"...","code":"FORBIDDEN","message":"请求未获授权","retryable":false}
```

HTTP 映射：400 invalid/model-output-invalid，401 unauthenticated，403 forbidden，408 timeout，422 clarification/unsupported/evidence-insufficient，502 upstream-unavailable，500 internal。`retryable=true`仅表示调用方可重新发起新请求，不授权服务内部重放。

错误`message`也只能由`code`和受限参数映射的服务端模板生成；异常正文、上游响应、模型文本、Prompt 和策略细节不能成为 message 或 warning。

## 8. 规划合同

```json
{
  "schemaVersion":"1",
  "outcome":"EXECUTE",
  "capability":"QUERY",
  "domain":"EMPLOYEE",
  "intent":"按条件查询员工",
  "payload":{}
}
```

`outcome`为 `EXECUTE|CLARIFY|REJECT`。EXECUTE 必须有 capability/domain/payload；CLARIFY 只能有白名单`reasonCode/missingFields/optionKeys`，不得包含自由文本问题或答案选项；REJECT 只能有 `reasonCode`。`safe_response`把这些键映射为服务端模板。所有对象 `additionalProperties=false`。

`intent`是 1..128 字符的诊断标签，只用于内存内校验/指标分类，不参与授权、路由或上游参数构造，也不得按原文写日志；真正执行只读取已校验的 capability/domain/payload。

规划输入由`plan`节点根据当前问题和`effective_authorization`即时构造，只包含：经 02 投影的有限历史、获准 capability/domain 列表、各能力 Schema 摘要和当前日期/时区。该投影是节点内临时值，不写入 Graph State。禁止发送 JWT、权限码全集、隐藏字段、真实数据样本、服务地址、密钥或原生上游合同。

## 9. Graph State 数据设计与生命周期

API 在调用图之前完成 JWT 签名、`kid`、时效和 issuer/audience 校验，只把可信主体写入初始 State；不得信任网关注入的用户头。`AgentState`使用 `TypedDict`或等价显式 schema；HTTP/上游边界使用 Pydantic model。首版字段如下：

| 字段 | 写入节点 | 生命周期/约束 |
|---|---|---|
| `request_id/deadline_at` | api/start | required；全图只读 |
| `trusted_identity` | api/start | JWT 验证后的最小主体；写入后全图只读，不含原始 JWT |
| `effective_authorization` | auth_context | Auth/策略/资源事实交集；不含完整权限响应 |
| `plan_candidate` | plan | 模型候选不可信；规划输入投影只在节点内存在，不进入 State |
| `validated_plan/selected_capability` | validate | 只有确定性 validate 可写 |
| `query_result/aggregate_result` | query/aggregate | 互斥严格 DTO；仅所属能力节点可写 |
| `search_hits/evidence_package/model_output` | DOCUMENT 子图 | 仅 DOCUMENT 分支；逐节点收窄且有数量/大小上限 |
| `secured_result/error` | result_security/safe_response | Graph 输出唯一来源；error 仅安全码/参数，不含异常正文 |

JWT、服务密钥、HTTP Client、Prompt 模板、完整原文和模型原始响应不得进入 State。节点只返回自己的局部更新；同一字段不得由两个非终止节点竞争写入。图以封闭 input/output schema 调用`builder.compile(checkpointer=False)`；任何独立编译的嵌套图也必须使用`checkpointer=False`，不得继承父图持久化。output 仅含`request_id/secured_result/safe_error`。不使用 `thread_id`，请求结束后 State 释放，不支持恢复、持久记忆、time travel 或人工中断。LangSmith/第三方 tracing 默认关闭，避免 State/Prompt 外发。

## 10. 详细功能与核心处理流程

### 10.1 固定图拓扑

```text
START -> auth_context -> plan -> validate
validate -> query | aggregate | document | clarify | reject
document -> retrieve -> evidence_gate -> generate_or_summary -> citation_gate
query/aggregate/citation_gate -> result_security -> END
clarify/reject/任一安全失败 -> safe_response -> result_security -> END
```

`auth_context/validate/query/aggregate/retrieve/evidence_gate/citation_gate/safe_response/result_security`必须是确定性节点；`plan/generate_or_summary`是仅有的模型节点。`safe_response`只能把白名单原因码映射为模板标识和受限参数，禁止复制模型自由文本、上游正文或异常信息；`result_security`是所有 Graph 输出的唯一出口。能力路由条件边只读取封闭枚举并恰好返回一个静态目标，不执行网络调用，不返回目标列表，不使用`Send`或`Command.goto`绕过已声明条件边。图没有返回上游节点的边，没有模型自主 Tool loop，也不在运行时注册节点。

### 10.2 确定性校验流水线

1. 严格 JSON 解析和顶层 outcome 校验；
2. capability/domain 必须在当前授权候选集中；
3. 选择唯一能力 Validator；
4. 能力 Validator 校验字段、操作符、函数、值类型、排序、分页和资源预算；
5. 生成不可变 `ValidatedPlan`，只含 canonical 值；
6. capability 执行器只接收 `ValidatedPlan`，禁止接收模型原始 JSON。

Validator 不做网络调用，不依赖模型，不默默删除非法过滤条件。信息不足返回 `CLARIFICATION_REQUIRED`；违反白名单返回 `MODEL_OUTPUT_INVALID` 或 `FORBIDDEN`，两者不得混淆。

### 10.3 请求执行算法

```text
FastAPI JWT + Pydantic request validation
 -> build initial AgentState(requestId/deadline)
 -> await compiled_graph.ainvoke(state) with checkpointer disabled and without thread_id
 -> graph executes auth/plan/validate/one branch/result security
 -> Pydantic response serialization + audit
```

- 模型规划失败不切换到字符串规则猜测；只有`ModelClient`可对明确的瞬时连接失败重试一次，必须生成新调用并受剩余 deadline 限制，plan 节点和 Graph 不再重试。
- 一个请求只执行一个能力，不跨域 join，不形成模型自主循环。
- `CLARIFY`不调用业务/检索上游；用户回答通过新的 HTTP 请求进入。
- FastAPI 客户端断开或 deadline 到期后取消图调用；每个节点在远调前后检查 deadline，迟到响应不得更新 State。

## 11. 超时预算

`deadline = min(receivedAt + requestedTimeout, receivedAt + configuredMax)`。每次远调超时为 `min(componentMax, deadline-now-safetyMargin)`；不足最小预算时直接 `TIMEOUT`。

首版建议预算比例仅作为配置默认值：Auth 10%、规划 35%、能力执行 35%、结果安全/序列化 20%。代码使用绝对剩余时间，不串联相对 timeout。业务读取最多一次幂等重试；写入、未知发送结果和整个请求不得自动重试。

## 12. 并发、幂等与事务边界

- 接口是只读编排，不提供业务写能力；相同问题的重复请求是独立请求，不承诺去重。
- `requestId`由服务生成，不把调用方 id 当授权或幂等证明。
- Agent 不开启跨上游事务；本地无业务数据库事务。
- httpx 客户端不得持锁等待；`AgentState`不得跨请求共享或写入进程级全局变量。
- LangGraph 节点级 retry policy 首版不设全局默认；需要重试的只读 Client 在自身边界按 AD-09 显式实现。

## 13. 错误分类与安全审计

审计字段：`requestId`、主体不可逆摘要、tenant 引用、capability/domain、策略版本、模型目标 ID、上游服务码、阶段、耗时、结果码、返回条数/引用数。禁止 message/history/plan 原文/结果正文/JWT/secret/完整资源 ID 集合。

安全审计是返回结果的前置门禁：若审计事件无法在剩余 deadline 内被已批准的审计接收端确认接收，则结果失败关闭并返回安全错误；不得把敏感结果先返回后补记。普通运行指标/非安全调试日志失败可以降级，但必须产生独立健康指标和告警，且不能影响安全审计字段或正文禁记规则。具体审计字段、接收端和保留策略由 02 冻结。

## 14. 实施落点

| 编号 | 建议路径/类型 |
|---|---|
| IMPL-01-01 | 建议新增：`agent_service/api/routes.py`、`schemas.py`（FastAPI/Pydantic） |
| IMPL-01-02 | 建议新增：`agent_service/graph/state.py`、`builder.py`、`deadline.py` |
| IMPL-01-03 | 建议新增：`agent_service/graph/nodes/plan.py`、`planning/models.py` |
| IMPL-01-04 | 建议新增：`agent_service/graph/nodes/validate.py`、`planning/validators.py` |
| IMPL-01-05 | 建议新增：`agent_service/observability/audit.py` |
| IMPL-01-06 | 建议新增：图拓扑/包依赖测试，禁止 Java 编排、动态节点、checkpointer 和数据库依赖 |

## 15. 测试与验证设计

| 编号 | 层级/建议路径 | 夹具与动作 | 关键断言 | 失败信号 |
|---|---|---|---|---|
| TEST-01-01 | 建议新增：Contract `tests/contract/test_api_schema.py` | 合法/边界/非法 null/未知字段及伪 ASSISTANT 历史请求 | 严格 JSON；历史只进入不可信数据区 | 非法请求被接受或历史成为控制消息 |
| TEST-01-02 | 建议新增：Unit `tests/unit/test_plan_schema.py` | 五种路由结果、缺失/额外/未知枚举计划 | 仅封闭组合解析；CLARIFY 无自由文本 | 非法候选进入 validate 后续 |
| TEST-01-03 | 建议新增：Unit `tests/unit/test_plan_validator.py` | 注入非法字段/域/函数并使用 spy Clients | 所有 Client 调用计数为 0 | 任一非法计划触发远调 |
| TEST-01-04 | 建议新增：Async integration `tests/integration/test_deadline.py` | 在 Auth/模型/能力/结果阶段阻塞并越过 deadline | 取消完成；无迟到 State/输出/新调用 | deadline 后仍写 State 或返回结果 |
| TEST-01-05 | 建议新增：Graph `tests/graph/test_clarify.py` | CLARIFY 后提交新 HTTP 请求 | 首次零上游；新请求无旧计划/State | 跨请求复用计划或 thread |
| TEST-01-06 | 建议新增：Unit `tests/unit/test_audit.py` | 注入正文/令牌/Prompt并模拟审计接收失败 | 仅安全摘要；失败关闭 | 泄漏字段或审计失败仍返回结果 |
| TEST-01-07 | 建议新增：Architecture `tests/architecture/test_graph_topology.py` | 导出编译图节点/边和包依赖 | 单静态目标、无 Send/Command 旁路/循环/Java 编排 | 快照新增未批准边或依赖 |
| TEST-01-08 | 建议新增：Graph `tests/graph/test_no_persistence.py` | 编译顶层/独立嵌套图并尝试 thread 配置 | 均`checkpointer=False`；无跨请求状态 | 可读取 checkpoint/thread 历史 |
| TEST-01-09 | 建议新增：Contract integration `tests/contract/test_spring_clients.py` | 真实 Spring 安全链夹具覆盖认证、错误和超时 | Python/Java 合同一致且失败关闭 | 认证绕过或错误/超时误映射 |
| TEST-01-10 | 建议新增：Security `tests/security/test_output_and_tracing.py` | 向 State、warning、error、callbacks 注入 canary | 封闭 output 且 canary 不出现在输出/日志/trace | 任一 canary 外泄 |

| 验证编号 | 工作目录与命令 | 预期结果 | 当前状态 |
|---|---|---|---|
| VAL-01-01 | 建议在`agent-service`创建后执行：python -m pytest tests/contract/test_api_schema.py tests/unit/test_plan_schema.py tests/unit/test_plan_validator.py | 严格合同通过，非法计划上游调用为 0 | 未执行：项目尚不存在 |
| VAL-01-02 | 建议在`agent-service`创建后执行：python -m pytest tests/integration/test_deadline.py tests/graph/test_clarify.py | deadline 后迟到更新为 0，无跨请求 State | 未执行：项目尚不存在 |
| VAL-01-03 | 建议在`agent-service`创建后执行：python -m pytest tests/unit/test_audit.py tests/architecture/test_graph_topology.py | 审计/依赖/拓扑门禁通过 | 未执行：项目尚不存在 |
| VAL-01-04 | 建议在`agent-service`创建后执行：python -m pytest tests/graph/test_no_persistence.py tests/contract/test_spring_clients.py tests/security/test_output_and_tracing.py | 无持久化、认证互通且泄漏为 0 | 未执行：项目尚不存在 |

验收：REQ-01-01～07 均有自动测试；PoC 覆盖 `AUTH -> PLAN -> QUERY/CLARIFY -> RESULT_SECURITY`，并真实验证 Spring 安全链和 Python HTTP Client。本设计不授权创建代码，实施需另行执行。

## 16. 端到端追踪矩阵

| 需求/约束 | 设计规则 | 实现 | 测试 | 验证 |
|---|---|---|---|---|
| REQ-01-01/02、CON-01-01 | DR-01-01 严格入口与计划准入 | IMPL-01-01/03/04 | TEST-01-01/02/03 | VAL-01-01 无非法上游调用 |
| REQ-01-03、CON-01-02 | DR-01-02 绝对 deadline | IMPL-01-02 | TEST-01-04 | VAL-01-02 迟到结果为零 |
| REQ-01-04/05、CON-01-03 | DR-01-03 脱敏审计与单运行时依赖 | IMPL-01-05/06 | TEST-01-06/07 | VAL-01-03 图拓扑与泄漏扫描通过 |
| REQ-01-06/07、CON-01-04 | DR-01-04 类型化请求 State、封闭输出、无 checkpointer、固定无环图 | IMPL-01-02/06 | TEST-01-07/08/09/10 | VAL-01-04 无持久状态/旁路/泄漏，PoC 合同通过 |
| 全量覆盖索引 | REQ-01-01、REQ-01-02、REQ-01-03、REQ-01-04、REQ-01-05、REQ-01-06、REQ-01-07；CON-01-01、CON-01-02、CON-01-03、CON-01-04 | DR-01-01、DR-01-02、DR-01-03、DR-01-04；IMPL-01-01、IMPL-01-02、IMPL-01-03、IMPL-01-04、IMPL-01-05、IMPL-01-06 | TEST-01-01、TEST-01-02、TEST-01-03、TEST-01-04、TEST-01-05、TEST-01-06、TEST-01-07、TEST-01-08、TEST-01-09、TEST-01-10 | VAL-01-01、VAL-01-02、VAL-01-03、VAL-01-04 |

## 17. 风险与待确认事项

风险是 LangGraph 版本/API 演进、模型计划稳定性、同步预算和 Python/Java 安全合同。通过锁定兼容版本、图/状态回归、严格 Validator、deadline 指标和实现前 PoC 控制；若硬性 Java-only 约束被证实，只能重新评审 LangGraph4j 备选，不能增加 Java 主编排旁路。

官方依据：`StateGraph`节点以共享 state 传递局部更新，图需 compile 后 invoke；checkpointer 才提供 checkpoint/thread 持久化与恢复。本设计以`checkpointer=False`显式禁止使用或继承：<https://reference.langchain.com/python/langgraph/graph/state/StateGraph/compile>、<https://docs.langchain.com/oss/python/langgraph/persistence>。

## 18. 五轮逐文档评审

结果见 `../内部审查记录_v2.0.md`。结论仅表示本文可进入首个 01/02/03 最小纵切 PoC 与分阶段实现，不代表 LangGraph 代码、Spring 合同、目标系统质量或生产批准完成。

### 18.1 内部自检记录

| 轮次 | 日期 | S0 | S1 | S2 | 本轮处理 | 结论 |
|---:|---|---:|---:|---:|---|---|
| 1 | 2026-07-22 | 0 | 2 | 3 | 修正 State 所有权/最小化与文档结构 | 已修复 |
| 2 | 2026-07-22 | 0 | 2 | 0 | 收紧历史输入和所有对外文本输出 | 已修复 |
| 3 | 2026-07-22 | 0 | 2 | 1 | 冻结静态单目标路由、重试和审计终态 | 已修复 |
| 4 | 2026-07-22 | 0 | 2 | 0 | 补齐约束/规则声明与可失败测试合同 | 已修复 |
| 5 | 2026-07-22 | 0 | 0 | 2 | 同步历史、实施依据和五轮治理口径 | 通过 |
