# 单体 Agent 智能体 L0 总体架构设计评审报告

## 1. 报告治理信息

| 项目 | 内容 |
|---|---|
| 报告标识 | `AGENT-L0-001-REVIEW-001` |
| 报告状态 | 已完成 |
| 评审日期 | 2026-07-16 |
| 评审对象 | [单体Agent智能体总体架构_L0_v0.1.md](单体Agent智能体总体架构_L0_v0.1.md) |
| 评审类型 | `architecture_l0` |
| 评审方式 | 独立评审、授权修复、重新评审，最多 5 轮 |
| 实际轮次 | 3 轮 |
| 报告位置 | `docs/design/agent/单体Agent智能体总体架构_L0_v0.1_评审报告.md` |
| 报告作用 | 记录本次 L0 评审门禁、发现、修复和最终结论；不替代 L0 本身的架构权威 |

## 2. 评审结论

| 项目 | 内容 |
|---|---|
| 结论 | 通过 |
| 评审门禁 | `L0→L1` |
| 就绪状态 | `ready` |
| 目标文档状态 | 已评审 / 已通过 / 未实施 / 未生效 |
| 执行阻塞数 | 0 |
| S0 / S1 / S2 / S3 | 0 / 0 / 0 / 0 |
| 评审阶段修改被评审文档 | 否；独立发现集冻结后，由 `architecture-design-doc` 在用户授权下完成修复和状态同步 |
| 主要结论 | 全局范围、模块分解、所有权、跨运行时契约、状态一致性、安全、检索边界、迁移回滚和 L1 治理已经形成一致且可追踪的 L0 约束 |

最终 L0 已满足治理两份 L1 架构文档的条件，但不具备跳过 L1/L2 直接进入编码或生产发布的效力。

## 3. 评审范围与基线

| 资源 | 角色 | 层级 | 状态/版本 | 是否读取 | 权威性/作用 | 置信度 |
|---|---|---|---|---|---|---|
| `docs/design/agent/单体Agent智能体总体架构_L0_v0.1.md` | target | L0 | v0.1；最终已评审/已通过 | 是，初评及每轮修复后重新读取 | 本次唯一被评审架构文档 | 高 |
| 2026-07-16 用户确认的 Agent 需求、技术选择和文档层级 | requirement | 外部需求 | 当前会话确认 | 是 | 高于仓库内实验文档的需求基线 | 高 |
| `auth-service`、employee、transaction、文档来源及 AI 服务语义 | external_contract | 外部契约 | L0 仅定义消费边界 | 读取 L0 中的契约声明，未评审完整外部契约文件 | 验证所有权和依赖方向，不验证字段级兼容性 | 中 |
| 规划中的两份 L1 | governed_child | L1 | 尚未创建 | 否，不存在可读文件 | 验证 L0 分解计划，不评价 L1 落实情况 | 高 |
| 既有 `agent-*`、`es-*` 项目和旧设计/评审文档 | excluded | 混合 | 实验性 | 未作为架构基线读取 | 仅确认其被明确排除；不参与新架构合规判断 | 高 |

### 3.1 范围内

- 文档身份、层级、权威范围、目标和非目标；
- 模块/能力分解、责任与非责任、数据/状态/契约所有权；
- Java/Python 双运行时、业务适配器、检索与索引、外部权限和业务域边界；
- QUERY、AGGREGATE、DOCUMENT 关键流程和失败路径；
- 安全、授权、事务、幂等、取消、超时、迟到结果、兼容性、可观测和回滚；
- 20 个全局约束、13 个核心决策及两份 L1 的继承计划；
- L0 是否足以治理 L1。

### 3.2 范围外

- L1/L2 文档生成或评审；
- API 字段、DTO、数据库表、索引 Mapping、SQL 和代码实现；
- 现有实验代码是否符合新 L0；
- 外部业务、权限和模型服务契约的字段级验证；
- 生产 SLO、容量和高可用方案批准。

### 3.3 基线优先级与冲突处理

采用以下权威顺序：当前用户明确需求 → 当前 L0 → 项目规则 → 本机实现/环境证据 → 一般工程原则。旧 Agent/ES 文档即使带有“已评审”或“权威”字样，也因用户明确排除而不参与本轮基线选择。未发现需要在多个有效权威基线之间裁决的冲突。

## 4. 执行阻塞

未发现影响本次核心结论的执行阻塞。目标文件可读、层级明确、修改与报告输出均已获得用户授权，所需发现、追踪和校验脚本均可执行。

规划中的 L1 尚未创建属于 L0 下位交付的预期状态，不构成本次 L0 评审阻塞。

## 5. 发现汇总

### 5.1 最终未关闭发现

| 严重级别 | 数量 | 对评审门禁的影响 |
|---|---:|---|
| S0 | 0 | 无 |
| S1 | 0 | 无 |
| S2 | 0 | 无 |
| S3 | 0 | 无 |

### 5.2 评审循环历史

| 阶段 | 发现 | 修复结果 |
|---|---:|---|
| 第 1 轮初评 | 5 个 S1 | 全部修复并复审确认关闭 |
| 第 2 轮复评 | 2 个 S2 | 全部修复并复审确认关闭 |
| 第 3 轮复评 | 无新增问题 | 满足停止条件 |

## 6. 评审发现及关闭证据

### 6.1 第 1 轮 S1

| ID | 层级与原位置 | 原始证据及期望/实际差异 | 触发场景与影响 | 修复建议、责任方和授权 | 当前关闭证据 | 门禁影响/状态 |
|---|---|---|---|---|---|---|
| REV-L0-001 | L0；原第 8.1、9.4、10.1 节 | 原文分别声明 `auth-service` 拥有权限、来源方拥有权限关联、Java 生成过滤，但未明确主体权限、资源属性、最终决策和执行的唯一权威。L0 应消除重复授权事实源。 | 文档资源属性和主体权限不一致时，各组件可能得出不同结论，造成越权检索或错误拒绝。 | 仅修改 L0；由 Agent 架构、安全、文档和检索责任方建立权威组合模型；已获本次修复授权。 | 新增 `AD-17`、`DEC-10` 和第 10.4 节，明确 Auth 决策、来源资源属性、Java 不可扩大约束和检索执行。 | 原阻塞 `L0→L1`；已关闭 |
| REV-L0-002 | L0；原第 10.2、11.4 节 | 原文记录取消/超时终态，但没有取消传播、迟到回调拒绝和终态竞争规则。L0 应定义跨运行时一致性权威。 | Java 已超时或取消后，Python/Tool 迟到完成可能把终态覆盖为成功或追加引用。 | 仅修改 L0；由 Java/Python 执行责任方定义单调终态与执行栅栏；已获授权。 | 新增 `AD-18`、`DEC-11`，并在状态与故障表中规定栅栏、deadline、幂等取消和迟到结果拒绝。 | 原阻塞 `L0→L1`；已关闭 |
| REV-L0-003 | L0；原第 10.3、11.4、14 节 | 原文只有 CI 契约 diff，没有 Java/Python/检索服务独立部署时的版本偏差门禁。L0 应约束滚动升级和回滚兼容性。 | 不兼容实例在滚动发布中接收流量，导致字段误解、运行时失败或错误降级。 | 仅修改 L0；由契约和运维责任方定义制品版本/指纹与 readiness；已获授权。 | 新增 `AD-19`、`DEC-12`、兼容矩阵、readiness、兼容重叠和协调切换规则。 | 原阻塞 `L0→L1`；已关闭 |
| REV-L0-004 | L0；原第 8、9.3、11.2 节 | 原文要求索引 CRUD，但未定义管理入口与查询入口的权限隔离、幂等、乱序和审计。L0 应保护索引写入边界。 | 查询凭据或 Python Tool 获得索引写权限时，可能发生索引投毒、误删或业务版本回退。 | 仅修改 L0；由文档与检索责任方隔离查询面和管理面；已获授权。 | 新增 `AD-20`、`DEC-13`，补充管理契约、服务身份、来源范围、幂等、单调版本和审计规则。 | 原阻塞 `L0→L1`；已关闭 |
| REV-L0-005 | L0；原第 14 节 | 原迁移表把“回到/回切 alpha”作为默认回滚路径，与 `AD-01` 的实验资产非基线约束冲突。L0 回滚不得依赖未经验证资产。 | 新架构失败时回切到不可确认可用或安全的实验系统，会重新引入旧风险。 | 仅修改 L0；由 Agent/运维责任方取消 alpha 默认回滚资格；已获授权。 | 第 14 节现规定 alpha 仅作隔离归档和现状证据；回滚到已验证新版本或关闭新入口，alpha 需独立验证和批准。 | 原阻塞 `L0→L1`；已关闭 |

### 6.2 第 2 轮 S2

| ID | 层级与原位置 | 原始证据及期望/实际差异 | 触发场景与影响 | 修复建议、责任方和授权 | 当前关闭证据 | 门禁影响/状态 |
|---|---|---|---|---|---|---|
| REV-L0-006 | L0；第 7、10.3、12 节 | 第 1 轮修复曾使用“所有跨进程调用”，可能把内部制品指纹机制错误施加给外部业务或模型提供方。约束应限定在本系统可治理范围。 | 外部服务不支持内部握手机制时，L1 可能设计出不可实施依赖。 | 收窄到系统自主管理的 Java Agent、Python Runtime 和检索服务；由契约责任方修复；已获授权。 | `AD-19`、`DEC-12` 和第 10.3 节已明确内部部署单元使用指纹，外部契约由 Adapter/Port 隔离。 | 非阻塞；已关闭 |
| REV-L0-007 | L0；第 5.3、14、16 节 | 第 1 轮新增的终态栅栏、契约 readiness 和索引管理面未直接进入成功标准，阶段 0 还混用独立评审与作者自审严重度。应建立清晰验证追踪。 | L1 只继承文字约束而遗漏验收门禁，导致修复无法验证。 | 把新增不变量加入成功标准、迁移门禁和追踪矩阵，并区分 S/P 术语；由 L0 维护者修复；已获授权。 | 第 5.3 节增加三项可验证成功标准；第 14 节区分独立评审 S0/S1 与作者自审 P0/P1；第 16 节映射 `AD-17～AD-20`。 | 非阻塞；已关闭 |

### 6.3 最终未关闭问题

最终复评未发现未关闭的 S0、S1、S2 或有明确价值的 S3。

## 7. L0 分层评审结果

| 检查域 | 结论 | 证据 |
|---|---|---|
| 权威范围与目标 | 通过 | 第 1、3～5 节定义 L0 权威、非目标、质量目标和成功标准 |
| 当前/目标状态与迁移 | 通过 | 第 6、14 节区分环境复用、应用绿地重建、分阶段门禁和非 alpha 默认回滚 |
| 分域与所有权 | 通过 | 第 8、10 节为 Agent、Python、Adapter、检索、Auth、业务域和文档来源方建立唯一责任与事实来源 |
| 全局决策与不变量 | 通过 | 第 7 节 `AD-01～AD-20`，第 12 节 `DEC-01～DEC-13` 均有理由、适用范围、风险和验证方向 |
| 跨域依赖和数据/状态 | 通过 | 第 8～10 节明确允许方向、受控回调、单一持久状态、执行栅栏、授权组合和索引生命周期 |
| 安全、可靠性与运维 | 通过 | 第 11 节覆盖权限、隐私、事务、幂等、超时、取消、降级、兼容、日志、部署和 SQL 治理 |
| L1 分解完整性 | 通过 | 第 13、16 节只规划两份 L1，并为所有内部模块和约束给出继承与验证证据 |
| 架构层级边界 | 通过 | 文档保留语义契约和全局约束，没有进入完整 DTO、表字段、类方法或 SQL 详细设计 |

## 8. 约束与下位追踪结果

| 上游范围 | 下位位置 | 映射状态 | 说明 |
|---|---|---|---|
| `AD-01～AD-14`、`AD-16～AD-20`、`DEC-01～DEC-13` | Agent 应用与能力 L1 规划行 | 已覆盖 | 负责 Java/Python、能力、Adapter、执行、授权、契约和 DOCUMENT 编排 |
| `AD-01`、`AD-06～AD-09`、`AD-13`、`AD-15～AD-20` 及适用 DEC | 检索与索引 L1 规划行 | 已覆盖 | 负责查询/管理契约、检索、索引、权限执行、兼容和取消边界 |
| DOCUMENT 业务编排 | Agent L1 下的 DOCUMENT L2 规划 | 已覆盖 | 当前不单独建立 DOCUMENT L1 |
| 原子召回、索引与向量 | 检索 L1 下的 L2 规划 | 已覆盖 | 不把 DOCUMENT 整体流程下沉到检索服务 |

两份 L1 尚未创建，因此本表确认的是 L0 分解与计划覆盖，不代表下位实现已经完成继承。

## 9. 实现就绪判断

- 当前门禁：`L0→L1`。
- 就绪判断：`ready`。
- 允许的下一步：生成并评审“单体 Agent 应用与能力架构”L1 和“检索与索引基础设施架构”L1。
- 暂不允许的动作：跳过 L1/L2 直接实施、把本结论解释为生产批准、把 alpha 资产当作实现基线。
- 进入 L1 正式评审前必须完成：DeepSeek `deepseek-v4-flash` 的数据处理边界、API 兼容性与效果 PoC，DOCUMENT 评测集和阈值，文档资源属性/版本/撤权契约，以及内部服务身份方案。LLM 供应商/模型/API 和维护人 Dylan 已在评审后确认。
- 生产架构批准前必须完成：生产 SLO、容量、网络隔离、高可用、备份恢复和正式中间件版本基线。
- 后续是否需要代码对照评审：需要；应在 L2 完成并形成实现后，使用代码对照设计评审验证实际依赖、契约和安全边界。

## 10. 证据限制与未验证项

| 项目 | 原因 | 对结论的影响 | 建议验证方式 |
|---|---|---|---|
| 两份 L1 的实际继承 | 文档尚未创建 | 不影响 L0→L1 门禁；不能声明跨层落实完成 | 生成 L1 后逐项映射 `AD`/`DEC` 并独立评审 |
| 外部 API 字段级兼容性 | 本次是 L0 评审，外部契约文件不在范围 | 不影响总体所有权；可能影响 L1 具体契约 | L1/L2 阶段执行 OpenAPI 和业务契约评审 |
| 现有实验代码符合性 | 用户明确禁止将其作为新基线 | 不影响绿地 L0；不能声明实现已满足架构 | 新实现完成后进行代码对照文档评审 |
| DOCUMENT 量化质量 | 评测集和阈值尚未冻结 | 不影响 L0 分解；阻塞 L1 正式评审和能力验收 | 建立固定语料、查询、标注及 Recall/NDCG/证据指标 |
| 生产可用性 | 当前只有本机测试环境，生产 SLO/拓扑未定义 | 不影响 L0→L1；阻塞生产批准 | 完成容量、故障、恢复和安全部署设计 |

## 11. 推荐处理顺序

| 顺序 | 目标文件/责任方 | 所需动作 | 前置条件 | 建议技能 |
|---:|---|---|---|---|
| 1 | 规划路径：`docs/design/agent/单体Agent应用与能力架构_L1_v0.1.md`；Agent 架构责任方 | 生成 Agent 应用与能力 L1，并映射适用 `AD`/`DEC` | 本 L0 已评审/已通过 | `architecture-design-doc` |
| 2 | 规划路径：`docs/design/agent/检索与索引基础设施架构_L1_v0.1.md`；检索架构责任方 | 生成检索与索引 L1，并与 Agent L1 联合定义授权、契约、deadline 和查询/管理面 | 本 L0 已评审/已通过 | `architecture-design-doc` |
| 3 | 两份 L1；独立评审责任方 | 执行 L1 逐份评审及跨 L1 边界检查 | 两份 L1 初稿完成 | `design-doc-review` |

## 12. 复审记录

| 轮次 | 基线与日期 | 已修复 | 部分修复 | 未修复 | 新增问题 | 结论 |
|---:|---|---:|---:|---:|---:|---|
| 1 | L0 v0.1 初稿，2026-07-16 | 0 | 0 | 5 个 S1 | 0 | 有条件通过；修复前不得进入 L1 |
| 2 | 第 1 次修复后 L0 v0.1 | 5 个 S1 | 0 | 0 | 2 个 S2 | 有条件通过；完成局部追踪修复后复评 |
| 3 | 第 2 次修复后 L0 v0.1 | 2 个 S2 | 0 | 0 | 0 | 通过；`L0→L1` ready |

## 13. 最终结论

本次评审结论为“通过”，评审门禁为 `L0→L1`，就绪状态为 `ready`。无执行阻塞，最终 S0/S1/S2/S3 均为 0。L0 的权威范围、模块与数据所有权、Java/Python 双运行时边界、DOCUMENT/检索职责、安全与一致性不变量、迁移回滚和两份 L1 治理计划已经足以约束下一阶段。下一步应生成两份 L1 并继续执行独立评审，不得据此跳过详细设计或直接认定实现、生产环境已就绪。

## 14. 评审后信息同步

| 日期 | 已确认事项 | 仍待确认事项 | 对原评审结论的影响 |
|---|---|---|---|
| 2026-07-16 | LLM 供应商 DeepSeek、模型 `deepseek-v4-flash`、API Base URL `https://api.deepseek.com`；文档及模块维护人 Dylan | DeepSeek 数据处理边界、API 兼容性与效果 PoC；内部服务身份与短期能力凭据；DOCUMENT 评价集与阈值；检索 L1 所需文档源契约 | 属于 L0 评审后的事实同步，不构成新一轮 L0 复评；L0→L1 的“通过/ready”不变，但 Agent L1 仍需解除自身门禁后复评 |

本节只同步用户明确确认的事实。未对 DeepSeek 服务条款、模型实际能力、数据留存/训练政策或接口兼容性作外部验证，也未把示例性安全与评价阈值视为已批准决策。
