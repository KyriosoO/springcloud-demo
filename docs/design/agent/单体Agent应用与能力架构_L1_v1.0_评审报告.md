# 单体 Agent 应用与能力 L1 v1.0 架构设计评审报告

## 1. 报告治理信息

| 项目 | 内容 |
|---|---|
| 报告标识 | `AGENT-APP-L1-V1.0-REVIEW-001` |
| 报告状态 | 已完成 |
| 评审日期 | 2026-07-17 |
| 评审对象 | [单体Agent应用与能力架构_L1_v1.0.md](./单体Agent应用与能力架构_L1_v1.0.md) |
| 评审类型 | `architecture_l1 + cross_layer` |
| 评审方式 | 逐文档独立评审、与 L0/检索 L1 交叉评审、授权修复与复评 |
| 最大轮次 / 实际轮次 | 5 / 3 |
| 基线提交 | `a60675ba1c12a070acfb16f5afab132aa9a9fbf4`，2026-07-17T18:21:56+08:00 |
| 报告作用 | 记录 Agent 应用与能力域的评审发现、修复证据和 `L1→L2` 门禁；不替代目标文档的架构权威 |

## 2. 评审结论

| 项目 | 内容 |
|---|---|
| 结论 | 通过 |
| 评审门禁 | `L1→L2`，并核对与 L0 及检索 L1 的交接一致性 |
| 就绪状态 | `ready` |
| 目标文档状态 | 已评审 / 已通过 / 未实施 / 未生效 |
| 执行阻塞数 | 0 |
| 最终 S0 / S1 / S2 / S3 | 0 / 0 / 0 / 0 |
| 评审阶段文档改动 | 是；独立发现集冻结后，由 `architecture-design-doc` 在用户授权下完成最小修订，再由 `design-doc-review` 复评 |
| 主要判断 | Agent 域职责、跨运行时契约、外部依赖、模型安全权威、失败闭环和 L2 门禁已清晰；架构发现和状态同步后的治理表述问题均已关闭 |

本结论允许在文档规定的前置条件下开展 Agent 域 L2 设计，不表示代码、配置、评价或生产发布已经通过。

## 3. 评审范围与基线

| 资源 | 角色 | 版本/指纹 | 评审用途 | 置信度 |
|---|---|---|---|---|
| `单体Agent应用与能力架构_L1_v1.0.md` | target | v1.0；当前 SHA-256 `152466BABD621DA498188D4C1BD5AE19E6E07EF0D8CD8CB5B23DBF9751B6AAB4` | Agent L1 逐文档评审对象 | 高 |
| `单体Agent智能体总体架构_L0_v1.0.md` | parent | v1.0；同轮评审 | 验证上位原则、决策和门禁承接 | 高 |
| `检索与索引基础设施架构_L1_v1.0.md` | peer | v1.0；同轮评审 | 验证 DOCUMENT 查询交接、重试和评价边界 | 高 |
| `单体Agent应用与能力架构_L1_v0.1.md` | historical_source | SHA-256 `3BD87D366E456407D596EDE52A9062158E46751013AB873ABF8985A932124AFC` | 检查 v1.0 压缩表达是否丢失有效外部依赖和韧性约束 | 高 |
| `evidence/eb-l1-003/document-eval-v3.0.0/README.md` 及同目录评价包 | evidence | v3.0.0；README SHA-256 `B8131EBA9768CC2AC8DBE2B2A449EB9D4BC1E4ED153C7A024FDCEBC1543D6B2A` | 核对 DOCUMENT 评价事实和证据边界 | 高 |

### 3.1 范围内

- Agent 应用与能力域的范围、组件、职责、非职责和依赖方向；
- Java/Python 职责分配、模型准入、安全投影、Runtime→Tool 约束和状态所有权；
- QUERY、AGGREGATE、DOCUMENT 的关键流程、错误、取消、重试和恢复；
- 与权限、员工、交易、检索和模型/重排服务的架构级契约；
- L0 约束承接、检索 L1 交接及 Agent 域 L2 就绪性。

### 3.2 范围外

- DTO、OpenAPI、类、方法、数据库表、索引字段和提示词的实现级设计；
- 现有代码与本 L1 的符合性、测试覆盖和运行行为；
- 外部服务真实字段、配额、SLA 和生产部署可用性；
- 生产业务数据评价和发布批准。

### 3.3 权威顺序

本轮采用：用户明确要求 → L0 v1.0 → 本 L1 v1.0 → 检索 L1 v1.0 的对等交接 → v0.1 冻结来源 → 版本化证据中的客观事实 → 一般工程原则。未发现需要修改范围外上位或外部契约文档的冲突。

## 4. 执行阻塞

未发现执行阻塞。目标、上位、对等文档及评价包均可读取，修订范围已明确授权，逐文档评审和交叉评审所需证据充足。

## 5. 发现汇总

### 5.1 最终未关闭发现

| 严重级别 | 数量 | 门禁影响 |
|---|---:|---|
| S0 | 0 | 无 |
| S1 | 0 | 无 |
| S2 | 0 | 无 |
| S3 | 0 | 无 |

### 5.2 评审循环历史

| 阶段 | 严重级别 | 数量 | 结果 |
|---|---|---:|---|
| 第 1 轮初评 | S1 | 4 | 冻结后授权修复 |
| 第 2 轮复评与状态同步 | S2 | 1 | 架构发现已关闭；状态回写后发现内部自评历史表述与当前状态冲突 |
| 第 3 轮复评 | S0～S3 | 0 | 全部发现关闭，无新增问题，达到停止条件 |

## 6. 第 1 轮冻结发现与关闭证据

| ID | 位置与证据 | 期望 / 实际差异 | 触发场景与影响 | 建议、责任方与授权 | 关闭证据及状态 |
|---|---|---|---|---|---|
| `REV-APP-101` | 初稿第 6～7 节的组件图把安全策略直接连接到模型端口，正文又写成 Java/Runtime 共同确保上下文；L0 则规定 Java 独占模型访问决策和安全投影，Python 只调用获准端口 | 期望模型准入与安全投影只有一个权威；实际图文允许 Python 被理解为共同决策者或直接绕过 Java | L2 按图实现时可能在双运行时重复授权、产生不同过滤结果，或让 Runtime 直连模型，从而绕过审计与策略 | 调整图的依赖方向，明确 Java 生成准入决策和安全投影，Python 仅在授权候选与预算内编排；Agent 架构维护者负责；修改已获授权 | 第 6 节图改为安全决策进入执行层、Runtime 只连接“获准模型端口”；第 7.3、9.2 节明确 Java 单一责任；第 2 轮复评关闭 |
| `REV-APP-102` | 初稿压缩掉 `auth-service`、`employee-service`、`mq-procedure-service`、检索查询面、LLM/Reranker 的提供/消费、失败、重试和降级语义；L0 和 v0.1 均明确这些依赖 | 期望 L1 至少给出架构级提供方、责任方和失败边界；实际 L2 无法判断 Adapter、Port 和重试责任归属 | 下位设计可能把外部业务调用放入 Runtime、对不可重试写操作透明重试，或让调用方自行解释降级结果，造成职责泄漏和契约不一致 | 新增出站依赖边界表，逐项定义消费方、提供方、失败语义、重试/降级和所有权；Agent 架构及各 Adapter 责任方负责；修改已获授权 | 第 7.2 节已覆盖五类外部依赖及其失败和重试边界，第 9.3 节补充总预算约束；第 2 轮复评关闭 |
| `REV-XDOC-101` | L0 保留“纯合成 v3 实跑不能替代生产业务代表性扩展”的门禁，Agent L1 初稿只保留目标系统实跑要求 | 期望 DOCUMENT 生产质量前置条件在 L1/L2 清晰可见；实际存在下传遗漏 | 合成数据通过时可能被错误解释为生产质量放行，导致未覆盖真实分布、权限和长尾文档就进入发布 | 在质量门禁、L2 前置条件和风险章节恢复经授权、脱敏、分层且具业务代表性的扩展要求；Agent 与评价责任方负责；修改已获授权 | 第 10.4、12、14 节已明确合成评价的限制和生产业务代表性扩展门禁；第 2 轮复评关闭 |
| `REV-XDOC-102` | v0.1 定义唯一重试责任和共享 deadline/attempt 预算，Agent L1 初稿仅一般性描述取消与重试 | 期望入口、检索服务和内部客户端共享总预算且只有唯一责任方；实际可能出现跨层各自重试 | 查询或 DOCUMENT 调用遇到超时、限流、瞬态故障时会产生乘法请求、尾延迟放大和重复副作用 | 在 Agent 入口层约束可重试条件，并把检索内部重试交给 `es-query-service`；所有尝试共享总预算与诊断；修改已获授权 | 第 9.3 节已定义唯一重试责任、可重试条件、总 deadline/attempt 预算和禁止乘法重试；第 2 轮复评关闭 |
| `REV-GOV-103` | 正式状态回写后，三份文档顶部为“已评审/已通过”，编写期内部自评表仍以现在时写“仍为未评审” | 期望内部自评明确为正式评审前的历史记录；实际同一文档同时出现互斥的当前状态 | L2 作者或审计者可能误判本 L1 尚未批准，导致门禁状态和引用基线不确定 | 只调整治理状态、修订历史、内部自评说明和正式记录，不改变架构语义；三份文档维护者负责；修改已获授权 | 本 L1 第 16～17 节及另外两份文档已区分历史候选状态和当前正式状态；第 3 轮复评关闭 |

## 7. Agent L1 逐文档评审结果

| 检查域 | 结论 | 主要证据 |
|---|---|---|
| 文档治理、范围与目标 | 通过 | 第 1～4 节定义状态、迁移、阅读导引、能力范围、非目标和验收方向 |
| 上位约束承接 | 通过 | 第 5 节完整映射 L0 原则与决策，并区分本域落实和对等域责任 |
| 组件与责任边界 | 通过 | 第 6 节明确 Java、Python、Adapter、检索和模型端口的单向依赖与非职责 |
| 契约与权限 | 通过 | 第 7 节定义契约权威、外部依赖、模型准入、安全投影、Tool 安全和错误语义 |
| 数据与状态所有权 | 通过 | 第 8 节定义请求、对话、任务、引用、权限投影和业务数据的事实来源 |
| 流程与失败闭环 | 通过 | 第 9～10 节覆盖三类能力、取消、迟到结果、重试、降级、审计和 DOCUMENT 质量门禁 |
| 决策、L2 门禁与风险 | 通过 | 第 11～14 节给出稳定决策、下位交付、迁移回滚和未关闭实施门禁 |
| 可读性与注意力引导 | 通过 | 核心结论、责任边界和关键门禁前置，细节集中于契约、流程和 L2 交付章节 |

## 8. 三文档交叉评审结果

| 交叉检查 | 结论 | 证据与判断 |
|---|---|---|
| L0 约束承接 | 通过 | Agent L1 第 5 节承接 `AD-01～AD-24` 和相关 `DEC`，追踪抽取无缺失或孤立下位 ID |
| Java/Python 权威 | 通过 | L0 与本 L1 均规定 Java 拥有业务语义、持久状态、权限和模型准入，Python 仅执行编排和受控工具调用 |
| 外部业务依赖 | 通过 | 本 L1 的出站依赖表与 L0 的 Auth、员工、交易和模型服务边界一致，未把外部事实复制为本域权威 |
| DOCUMENT 查询交接 | 通过 | 本 L1 只消费检索查询面；检索 L1 独占检索内部执行、索引管理、版本发布和内部重试 |
| 失败与重试 | 通过 | 三份文档使用同一总 deadline/attempt 预算，入口和内部依赖责任不重叠 |
| 评价门禁 | 通过 | 本 L1 与 L0、检索 L1 均区分合成评价、目标系统实跑和生产业务代表性扩展 |
| 状态与生效 | 通过 | 三份文档均已评审/已通过但仍未实施/未生效，不存在以架构通过代替运行验证的表述 |

## 9. 实现就绪与允许的下一步

| 判断 | 结论 |
|---|---|
| 是否建议进入 Agent 域 L2 | 是，但仅限第 12 节列出的 L2 主题和前置契约 |
| 是否已实现 | 否 |
| 是否允许直接生产发布 | 否；仍需代码符合性、契约、测试、目标系统评价和生产审批证据 |

允许的下一步包括：细化 Java/Python 运行时契约、各 Adapter、状态机、模型端口、Runtime→Tool 安全、取消栅栏和验收测试。L2 不得改变 L0/L1 已确定的单一权威和跨域边界。

## 10. 证据限制与未验证项

- 未对 Agent 相关 Java/Python 代码、配置、依赖注入和部署拓扑进行符合性评审；
- 未验证 `auth-service`、`employee-service`、`mq-procedure-service`、检索和模型服务的字段级契约；
- 未验证 `LLM_API_KEY` 生产空值门禁、本地模型制品、网络隔离和凭据扫描的实际配置；
- v3 评价包不能替代真实目标系统和生产业务代表性数据验证；
- 未执行取消竞争、迟到结果、部分成功、重试预算和降级行为的集成测试。

## 11. 评审－修改循环记录

| 轮次 | 动作 | 结果 |
|---|---|---|
| 1 | 独立完成 Agent L1 逐文档评审，并与 L0、检索 L1 交叉核对 | 发现 4 个阻断 `L1→L2` 的 S1 |
| 1 修订 | 修正模型安全权威图文，补回出站依赖、重试预算和生产评价门禁 | 修改范围限于三份已授权目标文档 |
| 2 | 全量重读三份文档并回写正式状态 | 原架构发现关闭；状态同步后的全局扫描发现 1 个 S2 治理表述冲突 |
| 2 修订 | 把编写期内部自评明确为正式评审前的历史记录 | 未改变架构约束、决策或门禁 |
| 3 | 复核三份文档当前状态、历史记录、报告、追踪和证据 | 全部发现关闭，无新增 S0/S1/S2/S3，提前结束 5 轮上限 |
| 状态同步 | 2026-07-21复核目标文档当前指纹 | 更新目标SHA-256；原评审基线提交和结论保持不变 |

## 12. 最终结论

Agent L1 v1.0 的逐文档评审和三文档交叉评审均通过，`L1→L2` 状态为 `ready`。本轮在第 3/5 轮收敛，最终没有未关闭发现；未实施、未生效以及 L2/生产门禁继续有效。
