# 08 验证、回滚、审计、观测与撤权保障能力代码评审报告

## 1. 执行摘要

| 项目 | 内容 |
|---|---|
| 评审模式 | review_and_fix |
| 最大循环次数 | 5 |
| 实际执行轮次 | 1 |
| 依据文档数量 | 主文档 1 份；并按配置读取/检索 L1、P1、P2 关联文档 |
| 评审代码范围 | `agent-service/**`、`agent-runtime/**`、`agent-adapter-api/**`、`common/**`、`**/*.sql`、`**/pom.xml`、`**/*.yml`、`**/*.yaml`、`**/*.properties` |
| 是否修改代码 | 是 |
| 验证结果 | 通过 |
| 最终结论 | 通过；存在范围外生产联调风险 |

## 2. 文档依据清单

| 文档 | 角色 | 优先级 | 是否必需 | 读取结果 | 备注 |
|---|---|---:|---|---|---|
| `docs/design/P2/08_验证回滚审计观测与撤权保障能力_L2实施详细设计_v1.0.md` | detailed_design | 1 | 是 | 已读取 | 主依据 |
| `docs/design/Agent*.md` | architecture | 2 | 是 | 已读取/检索 | 上级架构约束 |
| `docs/design/P1/*L2*.md` | detailed_design | 3 | 是 | 已读取/检索 | P1 安全、生命周期、ResultSecurity、密钥与能力内核约束 |
| `docs/design/P2/*.md` | detailed_design | 4 | 否 | 已读取/检索 | P2 00-07 关联约束 |

## 3. 文档约束追踪

| 约束编号 | 来源文档 | 约束内容 | 对应代码位置 | 评审结果 |
|---|---|---|---|---|
| DOC-C-001 | P2 08 第 8、10.1、10.2、10.3 | validationDigest 只能由 validation report 计算得出；alias switch 校验 task、targetIndex、validationStatus、validationDigest、expected alias target | `es-query-service` SQL、只读核实 `EsIndexAliasService` | 受 scope 限制，仅只读核实；未修改 Java |
| DOC-C-002 | P2 08 第 10.4、14 | rollback target 必须来自可信 alias history，不能接受任意 index | `es-query-service` SQL、只读核实 `AliasOperationAuditRepository` | 受 scope 限制，仅只读核实；未修改 Java |
| DOC-C-003 | P2 08 第 10.6、18 | 观测覆盖检索、Provider、ResultSecurity、撤权命中；指标标签必须低基数 | `DocumentObservabilitySupport`、`DocumentCapabilityHandler`、`DocumentResultSecurityProjector` | 已修复 |
| DOC-C-004 | P2 08 第 10.7、15 | ACL scope 过期、本地 blocklist 命中时检索与输出 fail closed | `DocumentCapabilityHandler`、`DocumentRevocationGuard`、`DocumentResultSecurityProjector` | 已满足并补充观测 |
| DOC-C-005 | P2 08 第 15、18 | 日志/指标不得记录全文 evidence、queryVector、answer/summary、token、sourceUri query/fragment | `DocumentObservabilitySupport`、provider client 脱敏异常 | 已满足 |
| DOC-C-006 | L1/P1 ResultSecurity | ResultSecurity 是成功结果返回和持久化前的统一安全出口 | `DocumentResultSecurityProjector`、`ResultSecurityBoundary` | 已满足 |

## 4. 代码问题清单

| 编号 | 级别 | 类型 | 文件 | 依据文档 | 问题描述 | 影响 | 建议处理 |
|---|---|---|---|---|---|---|---|
| CR-P2-08-001 | medium | implementation_completeness | `agent-service/src/main/java/com/dylan/agent/capability/document/DocumentObservabilitySupport.java` 及调用方 | P2 08 第 10.6、18、21、23 章 | 已存在低基数指标封装和测试，但未装配为 Spring bean，也未被文档检索、Provider、ResultSecurity、撤权路径调用。 | 生产中 provider 失败、HYBRID 降级、ResultSecurity 拒绝和撤权命中不可通过指标观测，违反 08 的编码完成门禁。 | 接入 Handler 和 ResultSecurity 投影链路，补充指标测试。 |

## 5. 文档问题清单

| 编号 | 级别 | 文档 | 问题类型 | 问题描述 | 影响 | 建议 |
|---|---|---|---|---|---|---|
| 无 | - | - | - | 未发现阻断性文档问题。 | - | - |

## 6. 修改摘要

| 轮次 | 修改文件 | 修改内容 | 对应问题 | 结果 |
|---:|---|---|---|---|
| 1 | `agent-service/src/main/java/com/dylan/agent/capability/document/DocumentCapabilityConfiguration.java` | 增加 `DocumentObservabilitySupport` bean，并在文档 Handler 装配时按可用性注入。 | CR-P2-08-001 | 已修复 |
| 1 | `agent-service/src/main/java/com/dylan/agent/capability/document/DocumentCapabilityHandler.java` | 在检索、embedding provider、generation provider、ACL scope 过期和本地 blocklist 命中路径记录低基数指标；保留既有构造器兼容性。 | CR-P2-08-001 | 已修复 |
| 1 | `agent-service/src/main/java/com/dylan/agent/metadata/config/AgentMetadataSecurityConfiguration.java` | 将可选观测封装注入 `DocumentResultSecurityProjector`。 | CR-P2-08-001 | 已修复 |
| 1 | `agent-service/src/main/java/com/dylan/agent/metadata/result/DocumentResultSecurityProjector.java` | 记录 ResultSecurity 过滤成功/失败指标，并记录本地 blocklist 撤权命中指标。 | CR-P2-08-001 | 已修复 |
| 1 | `agent-service/src/test/java/com/dylan/agent/kernel/core/DocumentCapabilityHandlerTest.java` | 增加检索、generation provider、撤权命中指标断言。 | CR-P2-08-001 | 已修复 |
| 1 | `agent-service/src/test/java/com/dylan/agent/metadata/result/DocumentResultSecurityProjectorTest.java` | 增加 ResultSecurity 决策和撤权命中指标断言。 | CR-P2-08-001 | 已修复 |

## 7. 验证结果

| 轮次 | 命令 | 结果 | 摘要 |
|---:|---|---|---|
| 1 | `mvn -pl ../agent-service -am "-Dtest=DocumentCapabilityHandlerTest,DocumentResultSecurityProjectorTest,DocumentObservabilitySupportTest,DocumentRevocationGuardTest,HttpDocumentEmbeddingClientTest,HttpDocumentGenerationClientTest" "-Dsurefire.failIfNoSpecifiedTests=false" test` | 通过 | 38 tests，0 failures，0 errors |
| 1 | `mvn -q -DskipTests compile` | 通过 | `serviceCenter` Reactor 编译通过 |
| 1 | `mvn test` | 通过 | Reactor 28 个模块成功；agent-service 432 tests，0 failures，0 errors |
| 1 | `git diff --check` | 通过 | 未发现空白或补丁格式问题 |

## 8. 剩余风险

| 编号 | 级别 | 风险 | 原因 | 后续建议 |
|---|---|---|---|---|
| RISK-P2-08-001 | medium | `es-query-service` alias switch/rollback Java 不在本轮 YAML include 范围内 | P2 08 的 alias Java 实现落点在 `es-query-service/**`，但当前 code_scope 只允许审查/修改 SQL、pom、配置以及 agent-service/agent-runtime/agent-adapter-api/common。 | 若要完成 08 的 alias switch/rollback 代码级强审查，应追加 `es-query-service/**` 到 code_scope 后单独执行。 |
| RISK-P2-08-002 | medium | 真实 ES、DB migration 和生产 profile 未做环境级验证 | 本轮验证使用 Maven 单元/集成测试，没有连接真实 ES 集群或生产数据库迁移环境。 | 上线前执行真实 migration、alias switch/rollback、rollback history、validation report 持久化和 ES `_aliases` 联调。 |
| RISK-P2-08-003 | low | 观测指标已接入代码路径，但未验证外部指标平台采集和告警规则 | 本轮只验证 Micrometer registry 内的 counter/timer，不涉及 Prometheus/Grafana/告警平台。 | 在 P2 08 生产门禁中补充指标 scrape、低基数标签巡检和告警触发验证。 |
| RISK-P2-08-004 | medium | invocation document result 附加审计仍未扩展 | P2 08 允许优先复用既有 invocation audit/checkpoint，不强制新增第二套事实源；本轮未改 `InvocationAuditJsonCodec`。 | 若生产要求保存 document audit DTO，需要另行确认持久化字段和迁移范围。 |

## 9. 结论

最终结论：通过。

说明：
- 本轮完成 1 次 review-fix 循环，已修复范围内可处理的观测接入缺口。
- P2 08 中 alias switch/rollback 的 Java 服务实现不在本次 YAML include 范围内，本报告不把它计为本轮可修改项。
- 验证命令均已执行并通过；未声明真实 ES/生产 DB/外部指标平台已通过。
