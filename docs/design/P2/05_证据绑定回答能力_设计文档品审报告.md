# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断后续编码：否
- 审查类型：auto，识别为详细设计文档，并执行跨层级一致性核验
- 目标文档：`docs/design/P2/05_证据绑定回答能力_L2实施详细设计_v1.0.md`
- 关联文档：L1 架构设计文档、`docs/design/P1` 下现存全部 L2 详细设计文档、P2 下编号小于 05 的 00/01/02/03/04 详设及对应品审报告、当前仓库相关 Java 契约与实现骨架
- 实际审查轮次：2
- 主要风险摘要：初审发现 7 个 S1 和 2 个 S2。S1 集中在 P2 前序基线不完整、07 被误列为已评审前置、公共 `answerText` 与 `candidateAnswerText` 语义不清、provider 返回模型写错为 `citationIds`、缺少 inline citation 二次校验、`sourceUri`/metadata 进入 LLM provider 的安全边界不足、本地闭环与生产 provider 启用门禁混淆；S2 为测试落点和 claim 级能力边界表述不足。目标文档已修复，复审未发现遗留 S0/S1。

## 2. 文档识别结果

- 识别文档类型：详细设计文档
- 识别依据：文档标题为 L2 实施详细设计，包含实现落点清单、测试设计、API/契约、数据模型、状态流转、风险与完成摘要。
- 文档状态：Draft
- 是否包含修订历史：是，已追加 2026-07-07 品审修订记录。
- 是否存在上级文档：是，目标文档引用 4 份 L1 架构设计文档。
- 是否存在关联文档缺失：否。修订后已覆盖 P1 L2 关联文档集合，以及 P2 下编号小于 05 的 00/01/02/03/04 详设和品审报告。

## 3. 审查范围

| 序号 | 文档 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---|---|
| 1 | `docs/design/P2/05_证据绑定回答能力_L2实施详细设计_v1.0.md` | 目标文档 | 是 | 本次品审与修订对象 |
| 2 | `docs/design/Agent目标架构总览_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Agent 总体边界 |
| 3 | `docs/design/Agent能力执行内核架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Handler/Adapter/Core/ResultSecurity 边界 |
| 4 | `docs/design/Agent契约与规划架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Java 契约源与 OpenAPI/Python 生成方向 |
| 5 | `docs/design/Agent元数据与上下文安全架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验权限、Context、ResultSecurity 边界 |
| 6 | `docs/design/P1` 下现存全部 L2 详细设计文档 | P1 关联文档 | 是，目录与关键约束核验 | 核验 D01-D05、ResultSecurity、安全专项和主链门禁 |
| 7 | `docs/design/P2/00_*` 详设及品审报告 | P2 前置文档 | 是 | 核验能力路线图和进入编码门禁 |
| 8 | `docs/design/P2/01_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 chunk schema、sourceUrl/sourceUri 安全和索引治理 |
| 9 | `docs/design/P2/02_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 domain metadata、证据字段和 `index-by-domain` |
| 10 | `docs/design/P2/03_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 ACL scope/filter 和 ResultSecurity 继承 |
| 11 | `docs/design/P2/04_*` 详设及品审报告 | P2 前置文档 | 是 | 核验检索、citation 字段、metadata 安全和 provider 之前的 evidence 边界 |
| 12 | `agent-api`、`agent-service`、`agent-adapter-api`、`agent-adapter-document` 相关 Java 类 | 仓库契约/代码结构 | 是，关键文件核验 | 核验 DTO、Handler、Verifier、Packer、Projector 和测试落点是否一致 |

## 4. S0 阻断问题

未发现 S0 阻断问题。

## 5. S1 严重问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 1、7 章 | 关联文档只显式写到 P2 04，未完整覆盖 P2 下编号小于 05 的 00-04 详设及品审报告 | 后续实现可能漏掉 00 路线图、01 sourceUri/索引、02 domain、03 ACL、04 metadata 安全结论 | 已补齐 P2 00-04 详设及品审报告，并保留 P1 L2 集合边界 |
| 2 | 第 1、3、7、17、21、24 章 | `07_LLM与EmbeddingProvider接入能力` 被列为前置详设 | 05 会引用未完成/未评审的未来文档作为事实基线，影响本地闭环判断 | 已将 07 改为后续生产启用依赖，本地闭环使用 disabled/fake provider |
| 3 | 第 3、10.8、11.2、17、19、23 章 | `answerText` 与 `candidateAnswerText` 语义不清，文档误写“若新增 answerText” | 可能导致前端/API 消费候选字段或误发公共契约变更 | 已明确 `answerText` 已存在且为用户可见字段，candidate 字段为 `@JsonIgnore`，只能由 projector 投影后清空 |
| 4 | 第 10.5、10.6、12、19 章 | provider 输出字段写成 `citationIds`，与当前 `DocumentGenerationResult.citationBindings` 不一致 | 后续编码会修改错误 DTO 或漏掉 binding 校验 | 已改为 `citationBindings` 和 `CitationBinding(text,citationIds)` |
| 5 | 第 8、10.6、10.8、20、21 章 | 只写 verifier 校验 citation，没有明确 ResultSecurity 对候选文本 inline citation 的二次校验 | provider binding 通过但 answer 文本无 `[citationId]` 时，仍可能输出无引用回答 | 已补充 `candidateCitationsValid(...)` 要求和相关测试 |
| 6 | 第 10.3、10.4、15、18、19、20、21 章 | LLM 输入 metadata 安全边界不足，`sourceUri` query/fragment、签名 token、ACL 字段或 `embedding` 可能进入 provider | 向外部 LLM provider 泄露内部访问参数、权限投影或大字段 | 已要求 `safeMetadata` 白名单、`sourceUri` 净化和测试门禁 |
| 7 | 第 21、23、24 章 | 本地编码门禁与生产 provider 启用门禁混淆 | 可能误判 05 不能推进，或在 07/08 未完成时误启用真实 provider | 已区分本地 fake/disabled 闭环和生产启用依赖 |

以上 S1 均已在目标文档中修复，复审未发现遗留 S1。

## 6. S2 一般问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 20 章 | 测试设计未覆盖 sourceUri 净化、空 `citationBindings`、candidate 字段不外露、citation 过滤后 fallback | 关键安全路径可能只有 happy path 覆盖 | 已补充 `DocumentEvidenceContextPackerTest`、`DocumentCitationVerifierTest`、`DocumentResultSecurityProjectorTest` 和集成测试落点 |
| 2 | 第 8、10.6、21 章 | 文档容易让人理解为首版支持 claim 级语义校验 | 后续验收可能期待当前 verifier 无法证明的事实一致性能力 | 已明确首版只做 citation id 归属和 inline citation 标记校验，claim 级语义校验是后续增强 |

以上 S2 均已修复。

## 7. S3 建议优化

暂无 S3 建议优化。

## 8. 架构设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 架构目标 | 不适用 | 目标文档为 L2 详细设计，不重新定义 L1 架构目标 |
| 系统边界 | 通过 | 修订后未越过 Handler、Adapter、ResultSecurity、Provider 边界 |
| 模块职责 | 通过 | Handler 编排，Packer 打包，Verifier 做引用归属校验，Projector 做最终输出安全 |
| 依赖方向 | 通过 | 保持 Java 契约源、ResultSecurity 后置收口、LLM provider 不可信 |
| 技术选型 | 通过 | 复用现有 Java DTO、generation port、citation verifier 和 projector |
| 一致性模型 | 通过 | evidence package 只在当前 invocation 内使用，digest 用于审计追踪 |
| 幂等与补偿 | 通过 | provider 失败通过 fallback/refuse，不写全局状态 |
| 权限与审计 | 通过 | 继承 03 ACL、04 metadata 安全、ResultSecurity 唯一出口 |
| 非功能需求 | 通过 | 补充预算、timeout、sourceUri 净化和 provider 启用门禁 |
| 风险与取舍 | 通过 | 已明确首版不做 claim 级事实证明 |

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 上级设计承接 | 通过 | 已承接 L1、P1、P2 00-04 约束 |
| 文件路径 | 通过 | 实现和测试路径已具体到模块/类 |
| 类与方法 | 通过 | 已列出 Validator、Handler、Filter、Packer、Verifier、Projector、Composer、Port |
| 入参与返回类型 | 通过 | 关键 DTO 和 provider record 已对齐当前代码 |
| 接口契约 | 通过 | `answerText` 语义已对齐，新增公共字段仍走 D01 |
| 数据结构 | 通过 | `citationBindings`、`CitationBinding`、`AgentDocumentCitationVerification` 已明确 |
| 校验逻辑 | 通过 | bindings 归属校验与 inline citation 校验分层清晰 |
| 异常处理 | 通过 | provider 异常、非法 citation、空 evidence、fallback/refuse 均覆盖 |
| 状态流转 | 通过 | DISABLED/SKIPPED/SUCCEEDED/FALLBACK/FAILED 语义明确 |
| 数据库设计 | 不适用 | 本文不新增数据库结构 |
| 缓存与消息 | 不适用 | 本文不新增缓存或消息机制 |
| 权限、审计、幂等、风控 | 通过 | ACL、ResultSecurity、metadata 净化、digest 和只读幂等均覆盖 |
| 测试设计 | 通过 | 已补充安全门禁、契约和本地集成测试落点 |
| 可编码性 | 通过 | 可进入 05 本地编码与测试闭环 |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 需求到架构一致性 | 通过 | 支撑公司政策、知识库、文献/文学资料的证据绑定问答 |
| 架构到详细设计一致性 | 通过 | 未改变 L1 的 Java 契约源、Runtime/LLM 不可信、ResultSecurity 边界 |
| 接口契约一致性 | 通过 | `answerText` 已存在，candidate 字段不外露，新增字段仍走 D01 |
| 代码结构一致性 | 通过 | 目标类与当前仓库结构一致 |
| 权限与审计一致性 | 通过 | 继承 03/04 安全门禁并补充 provider 输入净化 |
| 一致性模型一致性 | 通过 | evidence package 不跨 invocation 复用 |
| 风控策略一致性 | 通过 | fallback/refuse 和 provider 生产启用门禁明确 |
| 测试范围一致性 | 通过 | 测试清单覆盖主要安全和契约风险 |

## 11. 是否建议进入后续阶段

- 是否建议进入详细设计：已处于详细设计阶段
- 是否建议进入编码实现：是，建议进入 05 本地编码与测试闭环
- 是否建议先修订架构设计：否
- 是否建议先修订详细设计：否，当前修订后已满足进入编码条件
- 是否需要用户确认：暂无

## 12. 用户确认项

暂无需要用户确认的问题。

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 建议修改内容 | 是否阻断 |
|---:|---|---|---|---|
| 1 | S1 | 第 1、7 章 | 补齐 P2 00-04 详设及对应品审报告作为关联基线 | 是，已修复 |
| 2 | S1 | 第 1、3、7、17、21、24 章 | 将 07 改为后续生产启用依赖，不作为 05 已评审前置 | 是，已修复 |
| 3 | S1 | 第 3、10.8、11.2、17、19、23 章 | 明确 `answerText` 已存在且 candidate 字段只允许内部候选 | 是，已修复 |
| 4 | S1 | 第 10.5、10.6、12、19 章 | provider 输出模型改为 `citationBindings` | 是，已修复 |
| 5 | S1 | 第 8、10.6、10.8、20、21 章 | 补充 inline citation 二次校验和测试落点 | 是，已修复 |
| 6 | S1 | 第 10.3、10.4、15、18、19、20、21 章 | 补充 sourceUri 净化、metadata 白名单和 provider 输入安全测试 | 是，已修复 |
| 7 | S1 | 第 21、23、24 章 | 区分本地闭环与生产 provider 启用门禁 | 是，已修复 |
| 8 | S2 | 第 20 章 | 补全空 bindings、candidate 不外露、citation 过滤后 fallback 测试 | 否，已修复 |
| 9 | S2 | 第 8、10.6、21 章 | 明确 claim 级语义校验为后续增强 | 否，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-07 | 初审 | 9 | 0 | 7 个 S1，2 个 S2 |
| 2 | 2026-07-07 | 修正与复审 | 0 | 9 | 无 S0/S1/S2 遗留 |

## 15. 最终结论

目标文档通过品审，不阻断后续编码。修订后，05 已能作为证据绑定回答能力的本地编码依据；生产启用真实 generation provider 仍必须等待 07 Provider 和 08 验证/回滚审计门禁闭合。下一步建议按第 19、20 章进入代码实现与最小相关测试闭环。
