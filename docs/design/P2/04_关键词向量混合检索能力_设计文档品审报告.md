# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断后续编码：否
- 审查类型：auto，识别为详细设计文档，并执行跨层级一致性核验
- 目标文档：`docs/design/P2/04_关键词向量混合检索能力_L2实施详细设计_v1.0.md`
- 关联文档：L1 架构设计文档、`docs/design/P1` 下现存全部 L2 详细设计文档、P2 下编号小于 04 的 00/01/02/03 详设及对应品审报告、当前仓库相关 Java 契约与实现骨架
- 实际审查轮次：2
- 主要风险摘要：初审发现 6 个 S1 和 2 个 S2。S1 集中在 P2 前序基线不完整、ACL 字段名错误、`embedding` 可能通过 `_source/metadata` 泄露、未继承 02 的 `index-by-domain`、降级诊断公共契约边界不清、本地编码与生产启用门禁混淆；S2 为测试落点和响应字段完整性不足。目标文档已修复，复审未发现遗留 S0/S1。

## 2. 文档识别结果

- 识别文档类型：详细设计文档
- 识别依据：文档标题为 L2 实施详细设计，包含实现落点清单、测试设计、API/契约、数据模型、状态流转、风险与完成摘要。
- 文档状态：Draft
- 是否包含修订历史：是，已追加 2026-07-07 品审修订记录。
- 是否存在上级文档：是，目标文档引用 4 份 L1 架构设计文档。
- 是否存在关联文档缺失：否。修订后已覆盖 P1 L2 关联文档集合，以及 P2 下编号小于 04 的 00/01/02/03 详设和品审报告。

## 3. 审查范围

| 序号 | 文档 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---|---|
| 1 | `docs/design/P2/04_关键词向量混合检索能力_L2实施详细设计_v1.0.md` | 目标文档 | 是 | 本次品审与修订对象 |
| 2 | `docs/design/Agent目标架构总览_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Agent 总体边界 |
| 3 | `docs/design/Agent能力执行内核架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Handler/Adapter/Core 边界 |
| 4 | `docs/design/Agent契约与规划架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Java 契约源与 OpenAPI/Python 生成方向 |
| 5 | `docs/design/Agent元数据与上下文安全架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验权限、Context、ResultSecurity 边界 |
| 6 | `docs/design/P1` 下现存全部 L2 详细设计文档 | P1 关联文档 | 是，目录与关键约束核验 | 核验 D01-D05、ResultSecurity、安全专项和主链门禁 |
| 7 | `docs/design/P2/00_*` 详设及品审报告 | P2 前置文档 | 是 | 核验能力路线图和进入编码门禁 |
| 8 | `docs/design/P2/01_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 chunk schema、mapping、embedding 维度和 alias 治理 |
| 9 | `docs/design/P2/02_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 domain metadata、证据字段和 `index-by-domain` |
| 10 | `docs/design/P2/03_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 ACL scope/filter 和 ResultSecurity 继承 |
| 11 | `agent-api`、`agent-adapter-api`、`agent-adapter-document`、`es-query-api`、`es-query-service` 相关 Java 类 | 仓库契约/代码结构 | 是，关键文件核验 | 核验 DTO、Mapper、Adapter、Service 和测试落点是否存在 |

## 4. S0 阻断问题

未发现 S0 阻断问题。

## 5. S1 严重问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 1、7 章 | 关联文档只显式写到 P2 03，未完整覆盖 P2 下编号小于 04 的 00/01/02/03 详设及品审报告 | 后续实现可能漏掉 00 路线图、01 mapping、02 domain/index、03 ACL 的已评审结论 | 已补齐 P2 00-03 详设及品审报告，并保留 P1 L2 集合边界 |
| 2 | 第 12 章 | ACL 字段写为 `aclVisibility`、`allowedSubjectIds`、`allowedGroupIds`，与 01/03 和代码字段不一致 | 编码时会生成错误 filter 或错误 mapping，导致授权过滤失效 | 已改为 `tenantId/corpusId/status/aclRef/aclVersion/visibility/userIds/departmentIds/roleIds/attributeKeys` |
| 3 | 第 8、10、15、19、20 章 | `sourceExcludes` 与 metadata 安全要求不足，HYBRID keyword 分支和 KEYWORD 路径可能返回 `embedding` | 向量字段可能进入 `_source`、metadata、日志或最终响应，带来敏感数据和容量风险 | 已要求 KEYWORD、VECTOR、HYBRID 全路径排除 `embedding`，并补 mapper、ES service、evidence mapper 测试落点 |
| 4 | 第 8、14、19、21、23 章 | 未继承 02 的 `agent.document-adapter.index-by-domain`，实现清单仍容易沿用 `indexPrefix + domain` | 生产可能访问错误索引或绕过 read alias/alias 切换治理 | 已明确生产不得依赖 `indexPrefix + domain`，缺少 domain alias 时 fail closed |
| 5 | 第 10.5、17、18、19、23 章 | 降级诊断被写成直接修改 `DocumentRetrievalRequest` 或 `AgentDocumentParameters` 的实现项 | 可能绕过 D01 契约治理，造成 Java/OpenAPI/Python generated model 漂移 | 已改为 04 默认只使用 adapter 诊断和日志；公共响应字段变更必须转 D01 |
| 6 | 第 3、21、24 章 | 将 Provider/mapping 生产启用前提写成统一阻塞实施 | 会误判本地闭环无法推进，或混淆 KEYWORD/HYBRID 降级验证与生产 VECTOR/HYBRID 启用 | 已区分本地编码门禁与生产启用门禁 |

以上 S1 均已在目标文档中修复，复审未发现遗留 S1。

## 6. S2 一般问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 20 章 | 测试设计未覆盖 `index-by-domain`、`sourceExcludes`、metadata 二次剔除和 hybrid 缺少 filters | 后续编码可能只覆盖 happy path，遗漏安全门禁 | 已补充 `DocumentAgentAdapterTest`、`DocumentRetrievalMapperTest`、`DocumentEvidenceMapperTest`、`EsDocumentServiceTest` 和集成测试落点 |
| 2 | 第 11.3 章 | `HybridSearchHit` 响应字段未列出 `charStart`、`charEnd` | citation 定位字段容易被实现或测试遗漏 | 已补充 `charStart`、`charEnd` 说明 |

以上 S2 均已修复。

## 7. S3 建议优化

暂无 S3 建议优化。

## 8. 架构设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 架构目标 | 不适用 | 目标文档为 L2 详细设计，不重新定义 L1 架构目标 |
| 系统边界 | 通过 | 复审后未越过 Agent Core、Adapter、es-query-service 边界 |
| 模块职责 | 通过 | Handler 负责模式与 embedding，Adapter/Mapper 负责请求转换，es-query-service 负责 ES 查询和 RRF |
| 依赖方向 | 通过 | 保持 Java 契约源、Adapter 下游调用、Core 不直接依赖 ES client |
| 技术选型 | 通过 | 继续使用 ES keyword/vector、RRF、现有 Java DTO |
| 一致性模型 | 通过 | 继承 01 alias/read index 与 02 index-by-domain，检索不写全局状态 |
| 幂等与补偿 | 通过 | 检索只读；降级只影响当前 invocation |
| 权限与审计 | 通过 | 继承 03 ACL filter 与 ResultSecurity，补齐日志禁止项 |
| 非功能需求 | 通过 | 补充 source excludes、参数上限、指标和性能风险 |
| 风险与取舍 | 通过 | 已区分本地闭环和生产启用门禁 |

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 上级设计承接 | 通过 | 已承接 L1、P1、P2 00-03 约束 |
| 文件路径 | 通过 | 实现和测试路径已具体到模块/类 |
| 类与方法 | 通过 | 已列出 Validator、Handler、Adapter、Mapper、Service、Merger、DTO |
| 入参与返回类型 | 通过 | 关键 DTO 和方法出入参已明确 |
| 接口契约 | 通过 | 公共契约变更边界已收敛到 D01 |
| 数据结构 | 通过 | ACL 字段和 `HybridSearchHit` 字段已对齐 |
| 校验逻辑 | 通过 | mode、embedding、filter、index alias、参数上限均有门禁 |
| 异常处理 | 通过 | VECTOR fail closed、缺 filter fail closed、缺 index alias fail closed |
| 状态流转 | 通过 | planned/effective/failed closed 状态清晰 |
| 数据库设计 | 不适用 | 本文不新增数据库结构 |
| 缓存与消息 | 不适用 | 本文不新增缓存或消息机制 |
| 权限、审计、幂等、风控 | 通过 | ACL filter、ResultSecurity、日志禁止项和只读幂等已覆盖 |
| 测试设计 | 通过 | 已补充安全门禁、契约和端到端测试落点 |
| 可编码性 | 通过 | 可进入 04 本地编码与测试闭环 |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 需求到架构一致性 | 通过 | 支撑公司政策、知识库、文献/文学资料查阅或总结的检索底座 |
| 架构到详细设计一致性 | 通过 | 未改变 L1 的 Java 契约源、Runtime 不可信、Adapter 边界和 ResultSecurity 边界 |
| 接口契约一致性 | 通过 | 公共响应字段新增已设为 D01 门禁 |
| 代码结构一致性 | 通过 | 目标类与当前仓库结构一致，未引用不存在的主路径作为已实现事实 |
| 权限与审计一致性 | 通过 | ACL 字段已对齐 01/03，日志禁止项已覆盖敏感数据 |
| 一致性模型一致性 | 通过 | alias/read index、index-by-domain 和检索只读语义一致 |
| 风控策略一致性 | 通过 | fail closed 与生产启用门禁已明确 |
| 测试范围一致性 | 通过 | 测试清单已覆盖主要安全和契约风险 |

## 11. 是否建议进入后续阶段

- 是否建议进入详细设计：已处于详细设计阶段
- 是否建议进入编码实现：是，建议进入 04 本地编码与测试闭环
- 是否建议先修订架构设计：否
- 是否建议先修订详细设计：否，当前修订后已满足进入编码条件
- 是否需要用户确认：暂无

## 12. 用户确认项

暂无需要用户确认的问题。

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 建议修改内容 | 是否阻断 |
|---:|---|---|---|---|
| 1 | S1 | 第 1、7 章 | 补齐 P2 00-03 详设及对应品审报告作为关联基线 | 是，已修复 |
| 2 | S1 | 第 12 章 | ACL 字段改为 01/03 定义和代码实际使用的字段集合 | 是，已修复 |
| 3 | S1 | 第 8、10、15、19、20 章 | 明确三通道 `_source/metadata` 排除 `embedding`，补测试落点 | 是，已修复 |
| 4 | S1 | 第 8、14、19、21、23 章 | 明确 `index-by-domain` 为生产 read alias 事实源 | 是，已修复 |
| 5 | S1 | 第 10.5、17、18、19、23 章 | 降级诊断默认留在 adapter/log，公共响应字段变更转 D01 | 是，已修复 |
| 6 | S1 | 第 3、21、24 章 | 区分本地编码门禁与生产启用门禁 | 是，已修复 |
| 7 | S2 | 第 20 章 | 补全安全门禁和端到端测试文件路径 | 否，已修复 |
| 8 | S2 | 第 11.3 章 | 补充 `charStart`、`charEnd` 响应字段 | 否，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-07 | 初审 | 8 | 0 | 6 个 S1，2 个 S2 |
| 2 | 2026-07-07 | 修正与复审 | 0 | 8 | 无 S0/S1/S2 遗留 |

## 15. 最终结论

目标文档通过品审，不阻断后续编码。修订后，04 已能作为关键词、向量、混合检索能力的本地编码依据；生产启用仍必须等待 01 mapping、02 `index-by-domain`、03 ACL、07 Provider 和 08 验证/回滚门禁闭合。下一步建议按第 19、20 章进入代码实现与最小相关测试闭环。
