# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断后续编码：否
- 审查类型：auto，识别为详细设计文档，并执行跨层级一致性核验
- 目标文档：`docs/design/P2/06_摘要能力_L2实施详细设计_v1.0.md`
- 关联文档：L1 架构设计文档、`docs/design/P1` 下现存全部 L2 详细设计文档、P2 下编号小于 06 的 00/01/02/03/04/05 详设及对应品审报告、当前仓库相关 Java 契约与实现骨架
- 实际审查轮次：2
- 主要风险摘要：初审发现 8 个 S1 和 3 个 S2。S1 集中在 P2 前序基线不完整、07/08 与本地闭环关系不清、公共 `summaryText`/`summaryBullets` 与 candidate 字段语义不清、`summaryScope.documentIds` 过滤合并设计不足、生成摘要只写 citation verifier 未写 ResultSecurity inline citation 二次校验、状态枚举与当前代码不一致、provider 输入 metadata/sourceUri 安全边界不足、coverage 可能泄漏未授权文档存在性；S2 为测试路径不具体、empty evidence/refuse 语义混杂、契约扩展边界表述不够清晰。目标文档已修复，复审未发现遗留 S0/S1。

## 2. 文档识别结果

- 识别文档类型：详细设计文档
- 识别依据：文档标题为 L2 实施详细设计，包含实现落点清单、测试设计、API/契约、数据模型、状态流转、风险与完成摘要。
- 文档状态：Draft
- 是否包含修订历史：是，已追加 2026-07-07 品审修订记录。
- 是否存在上级文档：是，目标文档引用 4 份 L1 架构设计文档。
- 是否存在关联文档缺失：否。修订后已覆盖 P1 L2 关联文档集合，以及 P2 下编号小于 06 的 00/01/02/03/04/05 详设和品审报告。

## 3. 审查范围

| 序号 | 文档 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---|---|
| 1 | `docs/design/P2/06_摘要能力_L2实施详细设计_v1.0.md` | 目标文档 | 是 | 本次品审与修订对象 |
| 2 | `docs/design/Agent目标架构总览_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Agent 总体边界 |
| 3 | `docs/design/Agent能力执行内核架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Handler/Adapter/Core/ResultSecurity 边界 |
| 4 | `docs/design/Agent契约与规划架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Java 契约源与 OpenAPI/Python 生成方向 |
| 5 | `docs/design/Agent元数据与上下文安全架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验权限、Context、ResultSecurity 边界 |
| 6 | `docs/design/P1` 下现存全部 L2 详细设计文档 | P1 关联文档 | 是，目录与关键约束核验 | 核验 D01-D05、ResultSecurity、安全专项和主链门禁 |
| 7 | `docs/design/P2/00_*` 详设及品审报告 | P2 前置文档 | 是 | 核验能力路线图和进入编码门禁 |
| 8 | `docs/design/P2/01_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 chunk schema、sourceUrl/sourceUri 安全和索引治理 |
| 9 | `docs/design/P2/02_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 domain metadata、证据字段和 `index-by-domain` |
| 10 | `docs/design/P2/03_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 ACL scope/filter 和 ResultSecurity 继承 |
| 11 | `docs/design/P2/04_*` 详设及品审报告 | P2 前置文档 | 是 | 核验检索、citation 字段、metadata 安全和 provider 前 evidence 边界 |
| 12 | `docs/design/P2/05_*` 详设及品审报告 | P2 前置文档 | 是 | 核验 generation、`citationBindings`、inline citation、candidate 字段和 provider 输入安全边界 |
| 13 | `agent-api`、`agent-service` 相关 Java 类 | 仓库契约/代码结构 | 是，关键文件核验 | 核验 DTO、Handler、Verifier、Packer、Projector 和测试落点是否一致 |

## 4. S0 阻断问题

未发现 S0 阻断问题。

## 5. S1 严重问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 1、7 章 | 关联文档只显式写到 P2 05，未完整覆盖 P2 下编号小于 06 的 00-05 详设及品审报告 | 后续实现可能漏掉 00 路线图、01 sourceUri/索引、02 domain、03 ACL、04 检索 metadata、05 generation 安全结论 | 已补齐 P2 00-05 详设及品审报告，并保留 P1 L2 集合边界 |
| 2 | 第 1、3、7、17、21、24 章 | 真实 provider 与本地摘要闭环关系不清 | 可能误判 06 不能推进，或在 07/08 未完成时误启用真实 provider | 已将 07/08 改为后续生产启用依赖，本地闭环使用 disabled/fake provider |
| 3 | 第 3、11、12、17、19、23 章 | `summaryText`/`summaryBullets` 与 `candidateSummaryText`/`candidateSummaryBullets` 语义不清 | 可能导致 API 或前端消费候选字段，绕过 ResultSecurity | 已明确公共字段与 `@JsonIgnore` candidate 字段边界，并要求投影后清空 |
| 4 | 第 8、10.1、19、20、21、23 章 | `summaryScope.documentIds` 只写“转为 filter”，未说明受保护合并、AND 语义和用户 filter 不能放宽 | 指定文档摘要可能越界总结其他文档，或被用户 filter 覆盖 | 已明确内部受保护 filter 合并，不新增公共契约字段，必须与用户 filters 和 ACL filters 取 AND |
| 5 | 第 8、10.4、11、12、13、20、21 章 | 只写 citation verifier，未明确 provider `citationBindings` 与 ResultSecurity inline citation 二次校验的职责分层 | provider binding 通过但摘要文本无 `[citationId]` 时，仍可能输出无引用摘要 | 已补充 `citationBindings` 归属校验、empty bindings/PARTIAL 处理和 `candidateCitationsValid(...)` 二次校验 |
| 6 | 第 13 章 | 状态流转使用 `GENERATION_DISABLED`、`GENERATION_SUCCEEDED` 等非当前 enum 名称 | 后续编码会新增错误枚举或与 `DocumentGenerationStatus` 不一致 | 已改为当前 `DISABLED`、`SKIPPED`、`PACKED`、`GENERATING`、`SUCCEEDED`、`FALLBACK`、`FAILED` 和 `GroundingStatus` |
| 7 | 第 10.3、15、18、19、20、21、23 章 | provider 输入 metadata 安全边界不足，未明确 `sourceUri` query/fragment、签名 token、ACL 字段、`embedding` 禁止进入 provider | 向外部 LLM provider 泄露内部访问参数、权限投影或大字段 | 已要求 `safeMetadata` 白名单、`sourceUri` 净化和测试门禁 |
| 8 | 第 10.3、15、21、23 章 | coverage 未明确只按授权可见 evidence 计算 | 指定 documentIds 包含无权文档时，用户可能通过 coverage 推断未授权文档存在性 | 已明确 `requestedDocumentCount`、`coveredDocumentCount`、`evidenceCount`、`truncated` 计算和不泄漏规则 |

以上 S1 均已在目标文档中修复，复审未发现遗留 S1。

## 6. S2 一般问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 20 章 | 集成测试路径写成“新增”，测试覆盖不具体 | 后续实现无法直接落到测试文件 | 已补充 `DocumentSummarizeFlowIntegrationTest` 完整路径，并扩展 validator、handler、packer、verifier、projector 测试点 |
| 2 | 第 10.6、13 章 | evidence 为空、provider 失败、refuse/fallback 语义混杂 | 可能把无证据误记为 provider failure，或生成无证据摘要 | 已明确 evidence 为空为 `SKIPPED`，provider/校验失败按 `FALLBACK` 或 `FAILED` 处理 |
| 3 | 第 10.1、17、21、24 章 | 契约扩展边界不够清楚 | 可能为内部 filter 合并错误引入公共契约变更 | 已明确 `documentIds` 内部合并不需要 D01 公共字段变更；新增公共字段才走 D01 |

以上 S2 均已修复。

## 7. S3 建议优化

暂无 S3 建议优化。

## 8. 架构设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 架构目标 | 不适用 | 目标文档为 L2 详细设计，不重新定义 L1 架构目标 |
| 系统边界 | 通过 | 修订后未越过 Handler、Adapter、ResultSecurity、Provider 边界 |
| 模块职责 | 通过 | Validator 做摘要范围校验，Handler 编排，Packer 打包，Verifier 做引用归属校验，Projector 做最终输出安全 |
| 依赖方向 | 通过 | 保持 Java 契约源、Runtime/LLM 不可信、ResultSecurity 后置收口 |
| 技术选型 | 通过 | 复用现有 Java DTO、generation port、citation verifier 和 projector |
| 一致性模型 | 通过 | evidence package 只在当前 invocation 内使用，digest 用于审计追踪 |
| 幂等与补偿 | 通过 | provider 失败通过 fallback/refuse，不写全局状态 |
| 权限与审计 | 通过 | 继承 03 ACL、04 metadata 安全、05 provider 输入安全、ResultSecurity 唯一出口 |
| 非功能需求 | 通过 | 补充预算、timeout、sourceUri 净化、coverage 不泄漏和 provider 启用门禁 |
| 风险与取舍 | 通过 | 已明确首版不做 claim 级事实证明，摘要生成安全依赖 citation id 与 inline citation 校验 |

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 上级设计承接 | 通过 | 已承接 L1、P1、P2 00-05 约束 |
| 文件路径 | 通过 | 实现和测试路径已具体到模块/类 |
| 类与方法 | 通过 | 已列出 Validator、Handler、Filter、Packer、Verifier、Projector、Composer |
| 入参与返回类型 | 通过 | 关键 DTO 和 provider record 已对齐当前代码 |
| 接口契约 | 通过 | `summaryText`/candidate 字段语义已对齐，新增公共字段仍走 D01 |
| 数据结构 | 通过 | `DocumentSummaryScope`、`DocumentGenerationResult`、`CitationBinding`、`AgentDocumentCoverage` 已明确 |
| 校验逻辑 | 通过 | scope/filter 合并、bindings 归属校验、inline citation 校验分层清晰 |
| 异常处理 | 通过 | provider 异常、非法 citation、空 evidence、fallback/refuse 均覆盖 |
| 状态流转 | 通过 | 对齐 `DocumentGenerationStatus` 与 `GroundingStatus` |
| 数据库设计 | 不适用 | 本文不新增数据库结构 |
| 缓存与消息 | 不适用 | 本文不新增缓存或消息机制 |
| 权限、审计、幂等、风控 | 通过 | ACL、ResultSecurity、metadata 净化、digest、coverage 不泄漏和只读幂等均覆盖 |
| 测试设计 | 通过 | 已补充安全门禁、契约和本地集成测试落点 |
| 可编码性 | 通过 | 可进入 06 本地编码与测试闭环 |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 需求到架构一致性 | 通过 | 支撑公司政策、知识库、文献/文学资料的证据绑定摘要 |
| 架构到详细设计一致性 | 通过 | 未改变 L1 的 Java 契约源、Runtime/LLM 不可信、ResultSecurity 边界 |
| 接口契约一致性 | 通过 | `summaryText`/`summaryBullets` 已存在，candidate 字段不外露，新增字段仍走 D01 |
| 代码结构一致性 | 通过 | 目标类与当前仓库结构一致 |
| 权限与审计一致性 | 通过 | 继承 03/04/05 安全门禁并补充 summary coverage 不泄漏规则 |
| 一致性模型一致性 | 通过 | evidence package 不跨 invocation 复用，scope 合并只收窄不放宽 |
| 风控策略一致性 | 通过 | fallback/refuse 和 provider 生产启用门禁明确 |
| 测试范围一致性 | 通过 | 测试清单覆盖主要安全和契约风险 |

## 11. 是否建议进入后续阶段

- 是否建议进入详细设计：已处于详细设计阶段
- 是否建议进入编码实现：是，建议进入 06 本地编码与测试闭环
- 是否建议先修订架构设计：否
- 是否建议先修订详细设计：否，当前修订后已满足进入编码条件
- 是否需要用户确认：暂无；若要新增公共摘要字段、修改 P1/L1 契约、扩大到 07/08 实施或启用真实 provider，需要另行授权

## 12. 用户确认项

暂无需要用户确认的问题。

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 建议修改内容 | 是否阻断 |
|---:|---|---|---|---|
| 1 | S1 | 第 1、7 章 | 补齐 P2 00-05 详设及对应品审报告作为关联基线 | 是，已修复 |
| 2 | S1 | 第 1、3、7、17、21、24 章 | 将 07/08 改为后续生产启用依赖，不作为 06 已评审前置 | 是，已修复 |
| 3 | S1 | 第 3、11、12、17、19、23 章 | 明确公共摘要字段与 candidate 字段边界 | 是，已修复 |
| 4 | S1 | 第 8、10.1、19、20、21、23 章 | 补充 `summaryScope.documentIds` 受保护 filter 合并 | 是，已修复 |
| 5 | S1 | 第 8、10.4、11、12、13、20、21 章 | 补充 `citationBindings` 与 inline citation 二次校验 | 是，已修复 |
| 6 | S1 | 第 13 章 | 状态枚举对齐当前 `DocumentGenerationStatus` 和 `GroundingStatus` | 是，已修复 |
| 7 | S1 | 第 10.3、15、18、19、20、21、23 章 | 补充 sourceUri 净化、metadata 白名单和 provider 输入安全测试 | 是，已修复 |
| 8 | S1 | 第 10.3、15、21、23 章 | 明确 coverage 只按授权可见 evidence 计算，不泄漏未授权文档 | 是，已修复 |
| 9 | S2 | 第 20 章 | 补全测试路径和关键测试矩阵 | 否，已修复 |
| 10 | S2 | 第 10.6、13 章 | 区分 empty evidence、provider failure、fallback/refuse | 否，已修复 |
| 11 | S2 | 第 10.1、17、21、24 章 | 明确内部 filter 合并不需要公共契约变更 | 否，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-07 | 初审 | 11 | 0 | 8 个 S1，3 个 S2 |
| 2 | 2026-07-07 | 修正与复审 | 0 | 11 | 无 S0/S1/S2 遗留 |

## 15. 最终结论

目标文档通过品审，不阻断后续编码。修订后，06 已能作为摘要能力的本地编码依据；生产启用真实 generation provider 仍必须等待 07 Provider 和 08 验证/回滚审计门禁闭合。下一步建议按第 19、20 章进入代码实现与最小相关测试闭环。
