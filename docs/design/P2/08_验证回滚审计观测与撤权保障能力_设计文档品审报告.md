# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断后续编码：否，可进入 08 本地编码与测试闭环
- 审查类型：auto，识别为详细设计文档，并执行跨层级一致性核验
- 目标文档：`docs/design/P2/08_验证回滚审计观测与撤权保障能力_L2实施详细设计_v1.0.md`
- 关联文档：L1 架构设计文档、`docs/design/P1` 下现存全部 L2 详细设计文档、P2 下编号小于 08 的 00/01/02/03/04/05/06/07 详设及对应品审报告、当前仓库相关 Java 契约与实现骨架
- 实际审查轮次：2
- 主要风险摘要：初审发现 8 个 S1 和 3 个 S2。S1 集中在 P2 前序基线和品审结论引用不完整、当前代码字段/状态与文档不一致、validationDigest 的生产安全语义不足、rollback 缺少可信 alias history、生产持久化与本地闭环边界不清、运行审计可能形成第二套 invocation 事实、撤权 target 表述超出当前模型能力、测试清单不可直接指导编码。S2 为观测指标不够具体、状态枚举容易被误认为公共契约、DB/API/Policy 扩展授权边界不够明确。目标文档已修复，复审未发现遗留 S0/S1。

## 2. 文档识别结果

- 识别文档类型：详细设计文档
- 识别依据：文档标题为 L2 实施详细设计，包含实现落点清单、测试设计、API/契约、数据模型、状态流转、风险与完成摘要。
- 文档状态：Draft
- 是否包含修订历史：是，已追加 2026-07-07 品审修订记录。
- 是否存在上级文档：是，目标文档引用 4 份 L1 架构设计文档。
- 是否存在关联文档缺失：否。修订后已覆盖 P1 L2 关联文档集合，以及 P2 下编号小于 08 的 00-07 详设和品审报告。

## 3. 审查范围

| 序号 | 文档/代码 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---|---|
| 1 | `docs/design/P2/08_验证回滚审计观测与撤权保障能力_L2实施详细设计_v1.0.md` | 目标文档 | 是 | 本次品审与修订对象 |
| 2 | `docs/design/Agent目标架构总览_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Agent Service 授权/执行/审计边界、Runtime 不可信、扩展门禁 |
| 3 | `docs/design/Agent能力执行内核架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Handler、Adapter、Execution Core、Invocation Record、deadline 边界 |
| 4 | `docs/design/Agent契约与规划架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Java 契约源、Runtime 输入安全和公共 OpenAPI 边界 |
| 5 | `docs/design/Agent元数据与上下文安全架构设计_v1.0.md` | L1 上级文档 | 是，关键约束核验 | 核验 Authorization Snapshot、Execution recheck、ResultSecurity、撤权和观测边界 |
| 6 | `docs/design/P1` 下现存全部 L2 详细设计文档 | P1 关联文档 | 是，目录与关键约束核验 | 核验 D01-D05、统一密钥、ResultSecurity、安全专项和主链门禁 |
| 7 | `docs/design/P2/00_*` 至 `docs/design/P2/07_*` 详设及品审报告 | P2 前置文档 | 是，关键约束核验 | 核验文档能力拆分、索引治理、ACL、混合检索、证据、摘要、Provider 约束 |
| 8 | `es-query-api/src/main/java/com/dylan/esquery/api/model/AliasSwitchRequest.java` | 当前代码契约 | 是 | 核验 alias API 字段 |
| 9 | `es-query-api/src/main/java/com/dylan/esquery/api/model/RebuildTask.java` | 当前代码契约 | 是 | 核验 rebuild/validation 字段 |
| 10 | `es-query-service/src/main/java/com/dylan/esquery/service/DocumentIndexValidationService.java` | 当前实现骨架 | 是 | 核验 validation digest 当前能力 |
| 11 | `es-query-service/src/main/java/com/dylan/esquery/service/EsIndexAliasService.java` | 当前实现骨架 | 是 | 核验 alias switch/rollback 门禁 |
| 12 | `es-query-service/src/main/java/com/dylan/esquery/service/RebuildTaskRepository.java` | 当前实现骨架 | 是 | 核验 task repository 是否持久化 |
| 13 | `agent-service/src/main/java/com/dylan/agent/capability/document/DocumentCapabilityHandler.java` | 当前实现骨架 | 是 | 核验 ACL scope expired fail closed |
| 14 | `agent-service/src/main/java/com/dylan/agent/metadata/result/DocumentResultSecurityProjector.java` | 当前实现骨架 | 是 | 核验文档输出安全边界 |
| 15 | `agent-service/src/main/java/com/dylan/agent/metadata/policy/model/EmergencyRevocation*.java` | 当前策略模型 | 是 | 核验 emergency revocation target 能力 |

## 4. S0 阻断问题

未发现 S0 阻断问题。

## 5. S1 严重问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 1、7 章 | 关联文档只显式写到 P2 07，未完整覆盖 P2 00-07 及对应品审报告 | 08 是生产保障面，如果漏掉 00 路线图、01 mapping/model、03 ACL、05/06/07 generation/provider 安全结论，后续实现会缺门禁 | 已补齐 P2 00-07 详设及品审报告，并保留 P1 L2 集合边界 |
| 2 | 第 3、10、11、17、19 章 | 文档使用 `expectedCurrentIndex`、`SUCCEEDED` 等表述，与当前代码 `expectedPreviousIndex`、`SUCCESS` 不一致 | 编码人员会改错 DTO 或写出无法通过当前测试的状态判断 | 已统一为当前代码字段和状态，并说明后续重命名需 API 兼容性评审 |
| 3 | 第 3、8、10、21、23 章 | validationDigest 只写“绑定 task/mapping/sample”，未明确生产必须由持久化 validation report 生成 | 可能把本地非空 digest 当成生产门禁，绕过真实 mapping/sample/ACL/vector 校验 | 已新增 `DocumentIndexValidationReport`、digest canonical 规则、validatorVersion 和 report 持久化要求 |
| 4 | 第 10.3、10.4、12、14、21 章 | rollback 只描述 expected current/previous，不要求 alias history 和已验证 rollback target | 调用方可请求回滚到污染、未验证或已撤销索引 | 已补充 alias history、blocklist、validatorVersion 和 rollback target 白名单 |
| 5 | 第 3、12、17、19、21、24 章 | 当前内存 `RebuildTaskRepository` 与生产持久化门禁混在一起 | 服务重启后 task/report 丢失，alias switch/rollback 无法可靠审计和恢复 | 已明确内存仅本地闭环，生产持久化和 DB migration 需编码前授权 |
| 6 | 第 10.4、12、19 章 | 运行审计可能被实现成第二套 invocation 事实源 | 违反 P1 D02/L1 Invocation Record 权威审计边界，造成状态与审计漂移 | 已要求优先复用 invocation audit/checkpoint，新增 alias/validation 审计仅保存管理操作事实 |
| 7 | 第 8、10.7、10.8、11、21 章 | 细粒度 domain/index/documentId 撤权表述超出当前 `EmergencyRevocationTarget` 枚举能力 | 实现可能把 index/domain 伪装成现有 target，形成不可解释的撤权语义 | 已改为首版使用现有 target + 本地 document blocklist；扩展枚举需另行授权 |
| 8 | 第 19、20 章 | 实现落点和测试路径中存在“生产新增/新增”等泛化描述，部分测试无法直接指导编码 | 后续实现容易漏掉 digest/report、alias history、敏感日志和撤权 guard 断言 | 已补齐具体文件、类、方法、测试类和核心断言 |

以上 S1 均已在目标文档中修复，复审未发现遗留 S1。

## 6. S2 一般问题

| 序号 | 位置 | 问题 | 风险 | 修改建议 |
|---:|---|---|---|---|
| 1 | 第 10.5、18 章 | 观测只写领域类别，缺少具体低基数指标名和禁止标签 | 实现阶段可能把 userId、documentId、digest 等高基数字段放入 label | 已补充指标名、类型、标签和禁止字段 |
| 2 | 第 13 章 | `REBUILD_*`、`VALIDATION_*`、`ROLLBACK_*` 容易被误解为公共 Runtime 枚举 | 可能引入不必要的跨服务契约变更 | 已注明为内部 task/operation 状态，不新增公共 Runtime 枚举 |
| 3 | 第 15、17、21、24 章 | DB migration、公开 API 字段、policy target 枚举扩展的授权边界不够清楚 | 编码阶段可能越权修改 P1/L1 或公共契约 | 已补充需要用户进一步授权的范围 |

以上 S2 均已修复。

## 7. S3 建议优化

暂无 S3 建议优化。

## 8. 架构设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 架构目标 | 不适用 | 目标文档为 L2 详细设计，不重新定义 L1 架构目标 |
| 系统边界 | 通过 | 08 修订后保持 Agent Service 为最终授权、执行、审计边界 |
| 模块职责 | 通过 | validation/alias 属于 es-query 管理面；检索、ResultSecurity、撤权 guard 属于 agent-service 执行安全面 |
| 依赖方向 | 通过 | 未让 Runtime、Provider 或 Adapter 反向决定授权、切换或审计结果 |
| 技术选型 | 通过 | 复用当前 Java 类和 ES `_aliases` 原子操作，不引入新生产依赖 |
| 一致性模型 | 通过 | 通过 expected alias target、validation report、alias history 和持久化 repository 保证可恢复 |
| 幂等与补偿 | 通过 | switch/rollback 均要求 expected 校验，幂等成功和 audit 补偿边界已写明 |
| 权限与审计 | 通过 | 复用 Execution recheck、ACL scope、ResultSecurity 和 invocation audit，不新增平行权限体系 |
| 非功能需求 | 通过 | 补充低基数指标、脱敏日志、deadline、blocklist 和生产门禁 |
| 风险与取舍 | 通过 | 内存 repository 仅本地闭环，生产 DB/migration 另行授权，范围清楚 |

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 上级设计承接 | 通过 | 已承接 L1、P1、P2 00-07 约束 |
| 文件路径 | 通过 | 实现和测试路径已具体到模块/类 |
| 类与方法 | 通过 | 已列出 validation service、alias service、repository、revocation guard、projector、audit、observability |
| 入参与返回类型 | 通过 | `AliasSwitchRequest` 字段已对齐当前 Java；report/audit/decision DTO 为内部模型 |
| 接口契约 | 通过 | 公共 API 不强行重命名；新增公开字段需另行授权 |
| 数据结构 | 通过 | validation report、alias history、alias audit、document blocklist 均有字段和存储建议 |
| 校验逻辑 | 通过 | task/report/digest/expected alias/history/blocklist/revocation 均有门禁 |
| 异常处理 | 通过 | 安全拒绝、rollback 拒绝、audit 失败、deadline 迟到结果均覆盖 |
| 状态流转 | 通过 | 内部状态与公共 Runtime 枚举边界清楚 |
| 数据库设计 | 通过但需授权 | 生产持久化表为编码前确认项，本文未直接修改 schema |
| 缓存与消息 | 不适用 | 本文不新增消息机制；缓存只作为撤权 SLA 风险处理 |
| 权限、审计、幂等、风控 | 通过 | ACL、ResultSecurity、emergency revocation、本地 blocklist、alias expected 校验均覆盖 |
| 测试设计 | 通过 | 已补充 service、handler、projector、observability、audit、integration 测试落点 |
| 可编码性 | 通过 | 可进入 08 本地编码；生产闭环需按授权处理持久化和 migration |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 需求到架构一致性 | 通过 | 08 支撑 P2 文档能力生产运行保障，不新增用户能力边界 |
| 架构到详细设计一致性 | 通过 | 未改变 L1 的 Java 契约源、Runtime 不可信、Agent Service 审计边界 |
| 接口契约一致性 | 通过 | 保持当前 `AliasSwitchRequest` 字段；新公开字段需走契约变更 |
| 代码结构一致性 | 通过 | 当前代码已有本地闭环骨架，文档如实标注生产缺口 |
| 权限与审计一致性 | 通过 | 不修改 auth-service 权限模型，不新增第二套 invocation fact |
| 一致性模型一致性 | 通过 | validation report + alias history + expected target 支撑 switch/rollback 一致性 |
| 风控策略一致性 | 通过 | 撤权、blocklist、provider 风险和索引污染均 fail closed |
| 测试范围一致性 | 通过 | 测试覆盖主要安全、契约、持久化边界和失败路径 |

## 11. 是否建议进入后续阶段

- 是否建议进入详细设计：已处于详细设计阶段
- 是否建议进入编码实现：是，建议进入 08 本地编码与测试闭环
- 是否建议先修订架构设计：否
- 是否建议先修订详细设计：否，当前修订后已满足进入编码条件
- 是否需要用户确认：暂无；若要落地生产 DB migration、公开 API 字段重命名、扩展 `EmergencyRevocationTarget`、修改 P1/L1 文档或引入外部审计平台，需要另行授权

## 12. 用户确认项

暂无需要立即确认的问题。

编码阶段若从本地闭环升级到生产闭环，需要确认：

| 序号 | 确认项 | 触发条件 |
|---:|---|---|
| 1 | 是否允许新增生产持久化表和 migration | 实施 `PersistentRebuildTaskRepository`、validation report、alias history |
| 2 | 是否允许修改公开 alias API 字段 | 将 `expectedPreviousIndex` 重命名或新增 `expectedCurrentIndex`/`reason` |
| 3 | 是否允许扩展 emergency revocation target | 将 domain/index/documentId 纳入统一 policy 模型 |
| 4 | 是否允许修改 P1/L1 文档 | 发现上级文档与 08 生产实现无法兼容 |

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 建议修改内容 | 是否阻断 |
|---:|---|---|---|---|
| 1 | S1 | 第 1、7 章 | 补齐 P2 00-07 详设及对应品审报告 | 是，已修复 |
| 2 | S1 | 第 3、10、11、17、19 章 | 字段和状态对齐当前 Java：`expectedPreviousIndex`、`SUCCESS` | 是，已修复 |
| 3 | S1 | 第 3、8、10、21、23 章 | 补充 validation report、digest canonical 和 validatorVersion | 是，已修复 |
| 4 | S1 | 第 10.3、10.4、12、14、21 章 | rollback 增加 alias history 和可信 target 校验 | 是，已修复 |
| 5 | S1 | 第 3、12、17、19、21、24 章 | 区分本地内存闭环和生产持久化门禁 | 是，已修复 |
| 6 | S1 | 第 10.5、12、19 章 | 运行审计复用 invocation audit，不新增第二套事实源 | 是，已修复 |
| 7 | S1 | 第 8、10.7、10.8、11、21 章 | emergency revocation target 不越权表达 domain/index/documentId | 是，已修复 |
| 8 | S1 | 第 19、20 章 | 补齐具体实现和测试落点 | 是，已修复 |
| 9 | S2 | 第 10.6、18 章 | 增加具体低基数指标和禁止标签 | 否，已修复 |
| 10 | S2 | 第 13 章 | 内部状态与公共 Runtime 枚举分层 | 否，已修复 |
| 11 | S2 | 第 15、17、21、24 章 | 明确 DB/API/Policy 扩展授权边界 | 否，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-07 | 初审 | 11 | 0 | 8 个 S1，3 个 S2 |
| 2 | 2026-07-07 | 修正与复审 | 0 | 11 | 无 S0/S1/S2 遗留 |

## 15. 最终结论

目标文档通过品审，不阻断后续编码。修订后，08 已能作为验证、回滚、审计、观测与撤权保障能力的本地编码依据；真实生产启用仍必须在编码阶段按授权闭合持久化 task/report、alias history、DB migration、撤权联动和目标环境回归验证。下一步建议按第 19、20 章进入代码实现与最小相关测试闭环。
