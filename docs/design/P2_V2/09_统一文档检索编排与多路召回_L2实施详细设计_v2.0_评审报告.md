# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断后续编码：否
- 审查类型：auto
- 目标文档：`docs/design/P2_V2/09_统一文档检索编排与多路召回_L2实施详细设计_v2.0.md`
- 关联文档：P2 00-08、P2_V2 01/02/03/04/07/08、当前 `agent-service`、`agent-adapter-document`、`es-query-api`、`es-query-service` 文档检索相关结构
- 实际审查轮次：4
- 主要风险摘要：初审及本轮复核发现文档状态仍为 Draft、`MultiChannelSearchRequest`/新 API 与 P2_V2 04 及当前 hybrid 契约冲突、RRF/去重阶段使用 `rerankScore` 存在时序错误、实现落点偏离现有仓库结构；均已修复。本次串行复审未发现新增问题。

## 2. 文档识别结果

- 识别文档类型：详细设计文档 / 横向总编排详细设计
- 识别依据：包含 Java 统一编排、接口字段、数据结构、实现落点、测试设计、状态流转和风险项。
- 文档状态：Approved
- 是否包含修订历史：是
- 是否存在上级文档：是
- 是否存在关联文档缺失：否

## 3. 审查范围

| 序号 | 文档 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---|---|
| 1 | `09_统一文档检索编排与多路召回_L2实施详细设计_v2.0.md` | 目标文档 | 是 | 主审查对象 |
| 2 | P2 00-08 | 关联文档 | 是 | v1 基线和能力边界 |
| 3 | P2_V2 01/02/03/04/07/08 | 关联文档 | 是 | 子能力详设一致性 |
| 4 | 文档检索相关 Java 模块 | 代码结构 | 是 | 只读核对接口和落点 |

## 4. S0 阻断问题

未发现 S0 阻断问题。

## 5. S1 严重问题

未发现 S1 严重问题。

## 6. S2 一般问题

未发现 S2 一般问题。

## 7. S3 建议优化

暂无 S3 建议优化。

## 8. 架构设计审查结果

不适用。本文为 L2 实施详细设计，但承担 P2_V2 横向编排说明职责。

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 上级设计承接 | 通过 | 保持 L0/L1 边界：Java 权威、Runtime 不可信、ACL 不下放 |
| 文件路径 | 通过 | 公共 DTO、Handler、Adapter、ES service、测试路径已对齐 |
| 类与方法 | 通过 | `DocumentRetrievalProfileResolver`、`DocumentRetrievalMapper.toHybridRequest`、`EsDocumentService.hybridSearch` 等明确 |
| 入参与返回类型 | 通过 | `DocumentRetrievalOptions`、`DocumentRetrievalRequest`、`HybridSearchRequest` 字段明确 |
| 接口契约 | 通过 | 不新增 `/multi-channel-search`，扩展现有 `/hybrid-search` |
| 数据结构 | 通过 | `DocumentHybridOptions`、`HybridSearchChannelRequest`、`HybridRetrievalDiagnostics` 已纳入 |
| 校验逻辑 | 通过 | profile、ACL、alias、必需通道、Runtime 候选均 fail closed |
| 异常处理 | 通过 | 可选通道和 rerank 可降级，必需通道失败拒绝 |
| 状态流转 | 通过 | profile、query、channels、RRF、dedup、rerank、failed closed 状态完整 |
| 数据库设计 | 不适用 | 不新增业务表 |
| 缓存与消息 | 不适用 | 不涉及缓存和消息 |
| 权限、审计、幂等、风控 | 通过 | 所有通道共享 ACL filter，审计不记录敏感内容 |
| 测试设计 | 通过 | 单元、契约、集成、安全、质量验证项明确 |
| 可编码性 | 通过 | 可作为 P2_V2 代码实施总编排依据 |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 需求到架构一致性 | 通过 | 覆盖 Java 编排、profile、多通道、RRF、dedup、rerank、验证目标 |
| 架构到详细设计一致性 | 通过 | 未修改 L0/L1 核心边界 |
| 接口契约一致性 | 通过 | 与 P2_V2 03/04 的 `HybridSearchRequest` 扩展方向一致 |
| 代码结构一致性 | 通过 | 对齐当前 `DocumentSearchClient.hybridSearch` 和 `EsDocumentService.hybridSearch` |
| 权限与审计一致性 | 通过 | ACL filter 缺失 fail closed，诊断禁记敏感字段 |
| 一致性模型一致性 | 通过 | 单次 invocation 冻结 profile/alias/filter/queryVector |
| 风控策略一致性 | 通过 | production rerank/LLM 外部依赖仍需单独授权 |
| 测试范围一致性 | 通过 | 覆盖契约 drift、Java 单测、ES 测试和 gold query |

## 11. 是否建议进入后续阶段

- 是否建议进入详细设计：已处于详细设计阶段
- 是否建议进入编码实现：是
- 是否建议先修订架构设计：否
- 是否建议先修订详细设计：否
- 是否需要用户确认：否

## 12. 用户确认项

暂无需要用户确认的问题。

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 建议修改内容 | 是否阻断 |
|---:|---|---|---|---|
| 1 | S1 | 第 1、3、24 章 | 将文档状态从 Draft 修正为 Approved，并同步实现依据说明 | 否，已修复 |
| 2 | S1 | 第 9、10、11、12、19、20、23 章 | 移除新增 `MultiChannelSearchRequest`/`multi-channel-search` 口径，统一为扩展现有 hybrid 契约 | 否，已修复 |
| 3 | S1 | 第 10.4 节 | 修正 RRF/去重阶段使用 `rerankScore` 的时序错误 | 否，已修复 |
| 4 | S2 | 第 11、12、19 章 | 将不存在的 adapter DTO 字段收敛到 `ruleKeywords`、`rewriteCandidates`、`DocumentHybridOptions` | 否，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-09 | 初审/修正 | 1 | 1 | 0 |
| 2 | 2026-07-09 | 复审/修正 | 4 | 4 | 0 |
| 3 | 2026-07-09 | 最终复审 | 0 | 0 | 0 |
| 4 | 2026-07-09 | 串行复审 | 0 | 0 | 0 |

## 15. 最终结论

文档通过品审，不阻断后续编码。09 已从 Draft 修正为 Approved，并与 01/02/03/04/07/08 的字段、权限、召回、rerank 和验证边界保持一致；后续可按本文件进入代码实施或执行 code-review-against-docs。
