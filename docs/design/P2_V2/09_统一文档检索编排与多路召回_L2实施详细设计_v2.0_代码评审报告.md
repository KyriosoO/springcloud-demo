# 代码评审报告

## 1. 执行摘要

| 项目 | 内容 |
|---|---|
| 评审模式 | review_and_fix |
| 最大循环次数 | 5 |
| 实际执行轮次 | 2 |
| 依据文档数量 | 1 |
| 评审代码范围 | `agent-api/**`、`agent-adapter-api/**`、`agent-service/**`、`agent-adapter-document/**`、`es-query-api/**`、`es-query-service/**`、`**/*.sql`、`pom.xml`、`*/pom.xml` |
| 是否修改代码 | 是 |
| 验证结果 | Java 六模块编译/测试通过；Runtime 契约生成检查受环境依赖阻断 |
| 最终结论 | 有剩余问题 |

## 2. 文档依据清单

| 文档 | 角色 | 优先级 | 是否必需 | 读取结果 | 备注 |
|---|---|---:|---|---|---|
| `docs/design/P2_V2/09_统一文档检索编排与多路召回_L2实施详细设计_v2.0.md` | detailed_design | 1 | 是 | 已读取 | 文档状态为 Approved，可作为实现依据 |

## 3. 文档约束追踪

| 约束编号 | 来源文档 | 约束内容 | 对应代码位置 | 评审结果 |
|---|---|---|---|---|
| DOC-C-091 | 09 第 8、10.1、12、19 章 | Java profile 解析后必须冻结 `domain/materialType/retrievalProfile/profileVersion/indexAlias/hybridOptions`，DENSE_VECTOR profile 的 embedding provider/model/dimension 不应丢失 | `agent-service/src/main/java/com/dylan/agent/capability/document/DocumentRetrievalProfileResolver.java`、`DocumentPlanValidator.java` | 已修复后符合 |
| DOC-C-092 | 09 第 10.1、10.3、15、17 章 | 请求级通道不能绕过 profile；所有召回通道必须由 Java profile 裁剪并共享 ACL filter | `agent-service/src/main/java/com/dylan/agent/capability/document/DocumentPlanValidator.java`、`agent-adapter-document/src/main/java/com/dylan/agent/adapter/document/DocumentRetrievalMapper.java` | 已修复后符合 |
| DOC-C-093 | 09 第 8、10.6、19.1 章 | Adapter 优先使用请求内 `indexAlias`，但必须与 domain read alias 映射校验，避免 domain/profile/index 错配 | `agent-adapter-document/src/main/java/com/dylan/agent/adapter/document/DocumentAgentAdapter.java` | 已修复后符合 |
| DOC-C-094 | 09 第 10.3、10.6、12 章 | EXACT 通道应覆盖 v2 mapping 的文号、标题、机关等精确字段 | `agent-adapter-document/src/main/java/com/dylan/agent/adapter/document/DocumentRetrievalMapper.java` | 已修复后符合 |
| DOC-C-095 | 09 第 10.4、14 章 | N 路 RRF 和文档级去重排序必须稳定，tie-break 为 `rrfScore desc`、`bestRank asc`、`channelCount desc`、`documentId asc`、`chunkIndex asc`；hit 保留通道和命中字段诊断 | `es-query-service/src/main/java/com/dylan/esquery/service/HybridSearchMerger.java` | 已修复后符合 |
| DOC-C-096 | 09 第 11、19、20、21 章 | `DocumentRetrievalOptions` 新字段需要同步 OpenAPI 契约；Runtime 生成模型需要后续同步 | `agent-api/src/main/resources/openapi/agent-runtime-openapi.json`、`agent-runtime/app/contracts/generated_models.py` | Java/OpenAPI 已修复；Runtime 生成物超出本轮范围，剩余风险 |

## 4. 代码问题清单

| 编号 | 级别 | 类型 | 文件 | 依据文档 | 问题描述 | 影响 | 建议处理 |
|---|---|---|---|---|---|---|---|
| CR-091 | high | api_contract_consistency | `agent-service/src/main/java/com/dylan/agent/capability/document/DocumentPlanValidator.java` | DOC-C-091 | profile resolver 已解析 embedding provider/model/dimension，但 validator 重新构造 `DocumentHybridOptions` 时未透传 | DENSE_VECTOR 通道可能使用全局默认 embedding 配置，导致模型或维度校验偏离 profile | 已改为透传 profile 的 embedding provider/model/dimension，并补测试 |
| CR-092 | high | security / design_consistency | `DocumentPlanValidator.java` | DOC-C-092 | 请求级 `retrievalChannels` 可指定 profile 未声明的通道 | 用户候选计划可能绕过 profile 通道裁剪，启用未授权或未验证的召回路径 | 已限制请求级通道必须是 profile 通道子集 |
| CR-093 | medium | architecture_boundary | `agent-adapter-document/src/main/java/com/dylan/agent/adapter/document/DocumentAgentAdapter.java` | DOC-C-093 | 请求内 `indexAlias` 会无条件覆盖 domain 映射 | 错配的 profile/indexAlias 可能访问错误资料域索引 | 已增加 domain 映射一致性校验 |
| CR-094 | medium | functional_correctness | `agent-adapter-document/src/main/java/com/dylan/agent/adapter/document/DocumentRetrievalMapper.java` | DOC-C-094 | EXACT 通道只包含旧字段 `documentNumber`、`issuingAuthority`，未覆盖 v2 mapping 的 `documentNo`、`issuer` | 税务政策文号/机关型 query 在 ES v2 索引上召回质量下降 | 已补充 `documentNo`、`issuer`，同时保留旧字段兼容 |
| CR-095 | medium | transaction_consistency / diagnostics | `es-query-service/src/main/java/com/dylan/esquery/service/HybridSearchMerger.java` | DOC-C-095 | RRF tie-break 最后一项按 `chunkId` 排序，不符合 `chunkIndex asc`；同一 chunk 多通道命中时 `hitFields` 未合并 | 排序稳定性和多通道诊断不符合 09，gold query 排查会丢失 exact/phrase 命中字段 | 已改为按 `chunkIndex` 再按 `chunkId` 稳定排序，并合并多通道 `hitFields` |
| CR-096 | medium | api_contract_consistency | `agent-api/src/main/resources/openapi/agent-runtime-openapi.json` | DOC-C-096 | 主 OpenAPI artifact 缺少 `materialType/retrievalProfile/rerankEnabled/retrievalChannels` | Runtime 或外部生成方无法接受 09 新增可选字段 | 已同步主 OpenAPI artifact 与 agent-api 测试快照 |
| CR-097 | medium | implementation_completeness | `agent-runtime/app/contracts/generated_models.py` | DOC-C-096 | Python 生成模型仍未包含 09 新字段，且 source hash 与主 OpenAPI 不一致 | Runtime 如果接收带新字段的 plan，仍可能因 `extra='forbid'` 拒绝 | 未修复；`agent-runtime/**` 不在本轮继承评审范围内，需单独授权或扩展范围 |

## 5. 文档问题清单

| 编号 | 级别 | 文档 | 问题类型 | 问题描述 | 影响 | 建议 |
|---|---|---|---|---|---|---|
| 无 | - | - | - | 未发现阻断性文档问题 | - | - |

## 6. 修改摘要

| 轮次 | 修改文件 | 修改内容 | 对应问题 | 结果 |
|---:|---|---|---|---|
| 1 | `DocumentPlanValidator.java`、`DocumentPlanValidatorTest.java` | 透传 profile embedding provider/model/dimension；限制请求级通道为 profile 子集 | CR-091、CR-092 | 已修复 |
| 1 | `DocumentAgentAdapter.java`、`DocumentAgentAdapterTest.java` | 增加请求 alias 与 domain read alias 映射一致性校验 | CR-093 | 已修复 |
| 1 | `DocumentRetrievalMapper.java`、`DocumentRetrievalMapperTest.java` | EXACT 通道补齐 `documentNo`、`issuer` v2 字段 | CR-094 | 已修复 |
| 1 | `HybridSearchMerger.java`、`HybridSearchMergerTest.java` | RRF tie-break 改为使用 `chunkIndex`；合并同 chunk 多通道 `hitFields` | CR-095 | 已修复 |
| 2 | `agent-api/src/main/resources/openapi/agent-runtime-openapi.json` | 同步 `DocumentRetrievalOptions` 09 新字段到主 OpenAPI artifact | CR-096 | 已修复 |

## 7. 验证结果

| 轮次 | 命令 | 结果 | 摘要 |
|---:|---|---|---|
| 1 | `mvn -q -f agent-service/pom.xml "-Dtest=DocumentPlanValidatorTest" "-DfailIfNoTests=false" test` | 通过 | 验证 profile embedding 透传和通道子集校验 |
| 1 | `mvn -q -f agent-adapter-document/pom.xml "-Dtest=DocumentAgentAdapterTest,DocumentRetrievalMapperTest" "-DfailIfNoTests=false" test` | 通过 | 验证 alias/domain 校验和 exact 字段映射 |
| 1 | `mvn -q -f es-query-service/pom.xml "-Dtest=HybridSearchMergerTest,EsDocumentServiceTest" "-DfailIfNoTests=false" test` | 通过 | 验证 RRF、去重、通道执行和诊断 |
| 2 | `mvn -q -f agent-api/pom.xml "-Dtest=AgentRuntimeContractOpenApiGenerationTest" test` | 通过 | 验证 agent-api OpenAPI 生成快照 |
| 2 | `mvn -q -f agent-api/pom.xml -DskipTests install`、`mvn -q -f agent-adapter-api/pom.xml -DskipTests install`、`mvn -q -f es-query-api/pom.xml -DskipTests install` | 通过 | 更新公共 API 模块到本地仓库 |
| 2 | 六模块逐个执行 `mvn -q -f <module>/pom.xml -DskipTests compile` | 通过 | 覆盖 `agent-api`、`agent-adapter-api`、`agent-service`、`agent-adapter-document`、`es-query-api`、`es-query-service` |
| 2 | 六模块逐个执行 `mvn -q -f <module>/pom.xml test` | 通过 | 六个模块测试通过 |
| 2 | `python agent-runtime/scripts/check_contract_drift.py` | 失败 | 当前 Python 环境缺少 `inflect`，`datamodel-code-generator` 无法启动 |
| 2 | Python 只读 hash 检查 | 失败 | `agent-runtime/app/contracts/generated_models.py` 的 `source_sha256` 与主 OpenAPI 新 hash 不一致 |
| 2 | `git diff --check` | 通过 | 未发现空白错误 |

## 8. 剩余风险

| 编号 | 级别 | 风险 | 原因 | 后续建议 |
|---|---|---|---|---|
| RISK-091 | medium | Runtime 生成模型仍不接受 09 新字段 | `agent-runtime/**` 未包含在本轮继承评审范围内；当前 `generated_models.py` hash 与主 OpenAPI 不一致 | 单独授权修改 `agent-runtime/app/contracts/generated_models.py` 和 `agent-runtime/tests/test_contracts.py`，或扩展下一轮 code scope 后执行生成链 |
| RISK-092 | medium | Runtime 契约生成可复现性检查未完成 | 本机 Python 环境缺少 `inflect`，`datamodel-code-generator` 无法启动 | 修复 Python 生成环境后重跑 `python agent-runtime/scripts/check_contract_drift.py` |
| RISK-093 | low | 真实 gold query 与 ES 集成质量未在本轮验证 | 本轮验证以 Java 单元测试和模块测试为主，未连接真实 ES v2 索引 | 在灰度环境执行 08/09 gold query 批跑和 alias 切换验证 |

## 9. 结论

最终结论：
- 有剩余问题。

说明：
- 本轮允许范围内的 Java 统一编排、多通道请求、RRF/去重、alias 校验和 OpenAPI 主 artifact 已完成修复，并通过六模块编译/测试。
- 剩余问题集中在 `agent-runtime` 生成模型同步和 Python 生成环境，不应在未扩展授权范围的情况下由本轮直接修改。
