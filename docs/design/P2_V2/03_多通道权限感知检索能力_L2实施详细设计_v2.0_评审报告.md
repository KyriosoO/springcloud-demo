# 设计文档品审报告

## 1. 审查结论

- 结论：通过
- 是否阻断后续编码：否
- 审查类型：auto
- 目标文档：`docs/design/P2_V2/03_多通道权限感知检索能力_L2实施详细设计_v2.0.md`
- 关联文档：P2 03、P2_V2 01/02/04/07/08/09、`DocumentAclFilterFactory`、`DocumentEvidencePreSecurityFilter`、`DocumentResultSecurityProjector`
- 实际审查轮次：3
- 主要风险摘要：初审发现 rerank 前候选级安全投影与最终 ResultSecurity 职责混用，以及 ACL filter 工厂方法签名不一致；已修复。本次串行复审未发现新增问题。

## 2. 文档识别结果

- 识别文档类型：详细设计文档
- 识别依据：包含权限边界、接口字段、实现落点、状态流转和安全测试。
- 文档状态：Approved
- 是否包含修订历史：是
- 是否存在上级文档：是
- 是否存在关联文档缺失：否

## 3. 审查范围

| 序号 | 文档 | 类型 | 是否已读取 | 作用 |
|---:|---|---|---|---|
| 1 | `03_多通道权限感知检索能力_L2实施详细设计_v2.0.md` | 目标文档 | 是 | 主审查对象 |
| 2 | `P2/03_权限感知检索能力_L2实施详细设计_v1.0.md` | 关联文档 | 是 | 权限基线 |
| 3 | P2_V2 01/02/04/07/08/09 | 关联文档 | 是 | mapping、profile、召回、provider、验证和编排边界 |
| 4 | 相关 ACL/ResultSecurity 代码结构 | 代码结构 | 是 | 只读核对落点 |

## 4. S0 阻断问题

未发现 S0 阻断问题。

## 5. S1 严重问题

未发现 S1 严重问题。

## 6. S2 一般问题

未发现 S2 一般问题。

## 7. S3 建议优化

暂无 S3 建议优化。

## 8. 架构设计审查结果

不适用。本文为 L2 实施详细设计。

## 9. 详细设计审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 上级设计承接 | 通过 | Java 仍为权限和执行权威 |
| 文件路径 | 通过 | handler、ACL port、filter factory、安全投影和测试路径明确 |
| 类与方法 | 通过 | `DocumentAclFilterFactory.build(...)` 等方法已对齐 |
| 入参与返回类型 | 通过 | request 权限字段和 ES filter 字段明确 |
| 接口契约 | 通过 | `DocumentRetrievalRequest`、`HybridSearchRequest` 权限字段清楚 |
| 数据结构 | 通过 | permission evidence、filter digest、profileVersion 等字段定义完整 |
| 校验逻辑 | 通过 | 缺权限、profile 越权、alias 越权均 fail closed |
| 异常处理 | 通过 | Runtime 伪造 ACL/alias/filter 不被采信 |
| 状态流转 | 通过 | permission 到 projected/context 状态清楚 |
| 数据库设计 | 不适用 | 不新增表 |
| 缓存与消息 | 不适用 | 不涉及缓存和消息 |
| 权限、审计、幂等、风控 | 通过 | 所有通道同 filter，审计不记录 ACL 明细 |
| 测试设计 | 通过 | 覆盖多通道 filter、Runtime 伪造、安全投影和撤权复检 |
| 可编码性 | 通过 | 可指导权限感知多通道检索实现 |

## 10. 跨层级一致性审查结果

| 检查项 | 结论 | 说明 |
|---|---|---|
| 需求到架构一致性 | 通过 | 满足 Runtime 不可信和 ACL 不下放要求 |
| 架构到详细设计一致性 | 通过 | 未改变权限权威源 |
| 接口契约一致性 | 通过 | 与 P2_V2 04/09 的 hybrid channels 边界一致 |
| 代码结构一致性 | 通过 | ACL filter 和安全投影落点可定位 |
| 权限与审计一致性 | 通过 | 权限漏出 fail closed，审计只记摘要 |
| 一致性模型一致性 | 通过 | 单次请求冻结 permission/profile/alias |
| 风控策略一致性 | 通过 | blocklist、expired status、revoked 均收紧 |
| 测试范围一致性 | 通过 | 安全测试覆盖核心失败路径 |

## 11. 是否建议进入后续阶段

- 是否建议进入详细设计：已处于详细设计阶段
- 是否建议进入编码实现：是
- 是否建议先修订架构设计：否
- 是否建议先修订详细设计：否
- 是否需要用户确认：否

## 12. 用户确认项

暂无需要用户确认的问题。

## 13. 修订建议汇总

| 序号 | 优先级 | 目标位置 | 建议修改内容 | 是否阻断 |
|---:|---|---|---|---|
| 1 | S1 | 第 8、10、19 章 | 区分 rerank 前候选级安全投影和 rerank 后最终 ResultSecurity | 否，已修复 |
| 2 | S2 | 第 19 章 | 修正 `DocumentAclFilterFactory` 方法为 `build` 并更新返回类型 | 否，已修复 |

## 14. 复审记录

| 轮次 | 日期 | 操作 | 发现问题数 | 修复问题数 | 剩余问题 |
|---:|---|---|---:|---:|---|
| 1 | 2026-07-09 | 初审/修正 | 2 | 2 | 0 |
| 2 | 2026-07-09 | 复审 | 0 | 0 | 0 |
| 3 | 2026-07-09 | 串行复审 | 0 | 0 | 0 |

## 15. 最终结论

文档通过品审，不阻断后续编码。多通道 ACL filter、一致权限边界、rerank 安全输入、最终 ResultSecurity 和撤权复检均已闭合。
